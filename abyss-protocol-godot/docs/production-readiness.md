# 正式版本交付标准

用户目标：把 Godot 重制版做到 production quality，提升审美，支持 Windows、双人手柄同屏共享与 2560×1440 全屏。保留全中文、合理剧情、十八种武器；新救援扩展为五区域三十舱室，旧版十二舱存档保持原路线和结局。此文是工作清单，不是已经完成的质量声明。

用户最初确认交付平台为 Mac，2026-09-12 将 Windows 支持加入目标；当前保留 Mac，同时增加 Windows 10/11 x86_64。正式交付必须包含可直接运行的发行包、玩家说明和实机验收；不能用脚本测试通过替代游戏体验验收。

## 本次交付状态

2026-09-12：0.11.1 已交付，用户在 Windows 桌面执行的 176 项检查全部通过，报告已核对并保存到 Games 根目录。用户随后要求其余未通过或未完成的测试先跳过。以下标注“本次跳过”的专项不再阻塞本次交付，但保持未验证状态，不以勾选代替实测。历史实施记录保留原时点状态。


- [x] 完整循环：开局引导、武器选择、战斗、分支、商店、成长、五场区域守卫战、胜负与再次游玩；剧情线索因果一致。
- [ ] **本次真人/实体手柄专项跳过**。操作体验：键鼠和手柄可完成全流程；按键提示准确；支持重绑主要键盘输入；瞄准模式清晰；菜单导航和焦点切换可靠。
- [x] 玩家设置：音量、全屏、屏幕震动、闪光强度、瞄准辅助；设置跨启动保存；无法写入时提示。
- [x] 持续游玩：安全点自动存档、退出再继续；避免重复奖励/购买/永久成长；损坏/未来版本保护；窗口关闭、失焦与退出行为一致。
- [x] 可读性：常见桌面比例与窗口尺寸下中文不裁切；战斗危险与命中可辨；首领耐久和阶段清晰；弱视/运动敏感选项有效。
- [x] 视听表现：角色有连贯行动与攻击反馈、敌人有动作和前摇反馈、场景分区可辨、音效有层次和环境氛围；无裸露调试信息。
- [ ] **本次真人专项跳过**。难度与节奏：真实操作验证新玩家可理解且首局有合理容错，武器各有用途，后期/首领无软锁或必受伤组合。
- [x] 性能与稳定性：真实渲染下测帧时间、密集战斗和长流程；退出/重开资源回收；无运行错误、无卡死、无无限增长的声音或特效。
- [x] Mac 发行交付：版本号、本地依赖与素材许可、可重复导出脚本、发行包；在独立用户数据目录冷启动和完成流程，记录验证平台及限制。
- [x] Windows 发行与原生窗口自动验收：原生 x86_64 发行包、中文说明、图标/版本/许可、可重复导出和独立存档验证；Windows 原生窗口战役、存档写入、冷启动续档合计 176 项通过。实体输入、音频主观体验等未单独记录的专项按用户要求跳过；Mac/Wine 证据单独标明。
- [ ] **本次实体专项跳过**。双人手柄同屏：两只实体手柄分别配对、独立操作、共享镜头和完整合作流程；断线恢复、队友修复、共用强化、双人续档和重开均可用。已实现与自动验证，尚待实体双手柄验收。
- [ ] **本次 Windows 专项跳过**。2560×1440 全屏：16:9 画面及双人 HUD 铺满显示区域；镜头容纳两人和首领；在目标 Windows 设备验收 DPI、全屏切换与帧时间。Mac 原生 QHD 输出已验证，Windows 实机仍待验收。
- [x] 自动验证：对新增的持久化、输入和设置边界补充有效回归检查；所有原有流程、武器、中文与物理检查继续通过。

## 起点记录

2026-09-11 开始 production 质量阶段。现有源代码有完整单人键鼠流程和四套场景验证，但缺少设置菜单、手柄、输入重绑、中途存档、发行包。音效每次攻击同步合成并创建节点；窗口关闭没有专门处理；暂停中的“重新派遣”会直接丢弃本局。菜单按 1440×900 设计，尚无多尺寸实测证据。

原浏览器项目与其他游戏保持独立。用户已授权本任务提交、合并、推送和 Windows SMB 交付；Godot 项目不接入 Pages。

## 实施记录

后续在此记录已完成项的代码位置、命令和实机证据；只有证据足够时才勾选上面的交付标准。


### 2026-09-11 · 玩家服务与续玩

已实现设置菜单、主要键盘重绑、手柄菜单/战斗映射、动态按键提示、操作指南、退出/重开确认、失焦及断线暂停。音效使用十二路固定声音池与预缓存；增加本地环境声。Mac 原生渲染完成设置、指南、战斗和可继续救援主菜单的截图检查，支持 960×600、1280×800、1440×900、1920×1080 窗口，按固定画幅留黑边。实体手柄尚未实测。

存档升级至 schema 2：永久核心与安全点同一次写入，支持舱室入口、剧情、强化、路线、商店和维修状态恢复。两个分支在每个安全点自动写盘、销毁实例再恢复到胜利，913 项检查通过。旧版存档迁移、未来版本保护、非法数据与备份恢复均有针对性覆盖。战斗中退出会回到本舱入口，界面明确说明；死亡与胜利清除本局记录。

最近完整回归通过：导入、基础场景 41、武器 123、战役 1672、中文剧情 1011、玩家服务 48、安全点 913。之后新增的多窗口/续玩主菜单原生检查为 59 项通过。后续修改后仍需重跑对应检查；以上不代表完整 production 目标已完成。

下一阶段：Mac 发行包独立冷启动；角色动作和场景/战斗读图；首领与新手节奏；真实渲染压力与完整游玩验收。导出模板已从 Godot 官方 4.7.2 发布下载，当前仅做本地发行包，不上传。


### Mac 发行候选链路

`export_presets.cfg` 与 `export_macos.py` 已建立。发现并修正“仅主场景”漏掉脚本预加载依赖的问题，改为全部运行资源导出并排除开发目录。官方 4.7.2 macOS 模板生成的 universal 应用通过 `codesign --verify --deep --strict`；冷启动离开项目源码目录进行，确保不能从源文件补全漏打包的资源。导出脚本还在验证完整发行战役。

候选包：`builds/深渊协议-0.9.0-macOS.zip`。当前为 ad-hoc 本地签名，未 Apple 公证或上传。`builds/macos-0.9.0/` 是供本机验证的解压件；源码后续变化后需要重新构建，不能把旧包当作当前完成证据。

已找到原作八方向分部位角色图集，可用于下一阶段动作移植，避免重新生成素材。源图在 `output/imagegen/abyss-protocol-runtime/rig{East,SouthEast,South,SouthWest,West,NorthWest,North,NorthEast}.webp`；骨骼标定与实际步态算法位于原 `abyss-protocol/index.html` 的 `SOUTH_RIG_PARTS`、`drawSouthRigPart`、`drawSideRig`、`drawNorthRig`、`drawRearDiagonalRig`、`drawFrontDiagonalRig` 及 `rigGroundLevel`。移植时复制资源进 Godot 项目，不能运行时依赖 `output/` 或旧 HTML。相关历史决策见 2026-09-06/11 的 articulated player、gait、two-hand poses Notes。此处为历史发现；关节移植已在下一节完成。


### 角色动作与武器美术

已移植原作八方向独立关节素材与标定到本地资源，GDScript 实现站立/移动混合、脚步接触和抬脚、碰撞后距离驱动步态、撞墙停步、冲刺期间锁定步态、挥砍动作及枪/弓双手握持。枪械和弓已替换为校准过握把/护木/枪口/弓弦的绘制图集。姿态预览 `builds/qa/android-poses.png` 在 Mac 原生渲染检查通过；动画检查覆盖八朝向与八移动方向、正常游戏的真实调用点以及各蓄力/后坐力阶段，6412 项通过。

下一步仍需实际战斗视觉验收，尤其是弹体从武器发射的位置、首领/敌人动作与危险辨识、场景道具层次、教程与难度节奏、真实渲染下的帧时间与长时间资源回收。最新源码完成后必须重新导出；0.9.0 的早先构建不能证明这些新增项。


### 2026-09-11 · 工业科幻改版与本地验收

该阶段源码为 0.9.1。本轮统一原创轨道站主视觉、Noto Serif CJK 中文标题、暖白与救援橙 UI、低噪声甲板、检修道、舱壁与设备材质。设置分组、指南加入攻击形状示意，首舱加入可关闭的三步提示。首舱敌人从五个减至三个并延迟首次攻击；这只增加学习容错，不修改后续守卫机制。

武器弹体在统一地面碰撞坐标外增加绘制偏移，八方向的枪口/箭头与可见发射点连接。掩体会在遮挡玩家时淡化。首领镜头同时跟随双方并按距离缩放，保留 HUD 下的可见空间；不会改变物理伤害、射程或碰撞。小屏 Mac 启动窗口根据可用桌面区域缩放、居中，原生检查通过。

增加九种离线合成音效，使用已有十二路池播放。原生压力场景采集九百个帧时间样本，实际平台 Apple M4 Pro / Godot 4.7.2 / OpenGL Compatibility / 1440×900：中位 16.67 ms、95 分位 22.78 ms、99 分位 32.87 ms，峰值 293 节点与 33 枚弹体。四十次重开后节点数回到基线，孤立节点为零。该数据说明本机压力范围，不声称所有帧锁定 60 FPS，也不代表 Intel Mac。

普通规则输入机器人使用速射枪完成十二舱室和全部六段剧情；没有修改玩家血量、伤害、冷却或删除敌人。它使用四倍仿真速度与自动路线选择，是可达性和节奏基线，不是新玩家研究。原生 UI 经系统鼠标/键盘操作检查，已进入开场、战斗与失败界面；完整的人类通关和长时间体验反馈尚缺。

设计审查与落实项见 `docs/design-review.md`。最新原生玩家服务为 60 项通过；presentation 检查覆盖八方向发射点、遮挡、镜头与首领阶段。该阶段构建记录位于 `builds/深渊协议-0.9.1-macOS.json`；最新候选包见下文 0.9.2 记录。

## 尚未完成的验收

- 实体手柄完整游玩；现有覆盖是实际输入事件分发、映射、菜单焦点和模拟输入。
- 真实新玩家完整通关与长时间体验反馈；现有普通规则输入机器人不能替代该项。
- Intel Mac 独立实机验证；交付包包含 x86_64，但本机只有 Apple Silicon。
- 公开发行所需 Apple Developer 身份签名、公证与分发；当前仅本地 ad-hoc 签名候选包，没有上传。

因此 production 总目标仍在进行，不把本地候选包等同于已完成全部商业发行验收。


### 历史本地构建 · 0.9.1

`python3 export_macos.py` 完整执行：导入，smoke 41、weapons 123、campaign 1672、story 940、production 59、checkpoints 913、animation 6412、presentation 376，均零失败。原生 production 为 60 项通过（另含真实桌面适配），原生 presentation 为 376 项通过。发行模板独立自检 28 项通过，压缩包校验和 ad-hoc 签名校验通过。

候选包：`builds/深渊协议-0.9.1-macOS.zip`。

SHA-256：`18f160807f3a9fdd6a094cfb059cc2b934c440d61a222985ef71dd1152b09e84`。

当时的源代码、资源与候选包一致；没有提交、推送、上传或部署。浏览器原作、Pages 与 westworld 的已有更改未参与本次验证。

独立解压后的 `.app` 另以原生 OpenGL 窗口运行发行自检，28 项零失败；随后通过正常启动路径打开 0.9.1 主菜单，确认中文、字体、轨道站插画、版本号和已有永久进度正常显示。应用已保持打开供试玩。


### 历史本地候选 · 0.9.2

鼠标持续射击、长蓄力和松开射击保持手动瞄准，闲置辅助、摇杆和鼠标模式分别在 HUD 显示。关闭辅助后不再让移动方向抢走鼠标瞄准；战斗准星与菜单指针切换，退出前清理自定义准星，修复原生 OpenGL 纹理泄漏。新增六项真实输入检查，修改前复现四项失败，修改后全部通过。

新增可随时切换的高对比文字，设置跨启动保存，已有菜单与后续界面同步生效。原生 production 63 项、aiming 6 项通过，日志无 `ERROR:`，高对比指南截图已检查。

`python3 export_macos.py` 完整执行：资源导入，smoke 41、weapons 123、campaign 1672、story 943、production 62、checkpoints 913、animation 6412、presentation 376、aiming 6，九套全部零失败。发行自检 28 项、独立存档写入 11 项和第二进程读取 19 项均通过，压缩包完整性及 ad-hoc 签名检查通过。

存档验收使用显式带标记的临时目录，首次进程从空进度开始，选择轨道炮、修改音量/震动/按键，到第三舱购买武器调校后退出。第二进程冷启动，恢复设置与实际 InputMap、武器和商店安全点，逐项比较核心、次数、废料、伤害、耐久、购买和路线，确认没有重复购买，再推进到救援结局并从磁盘验证胜利和安全点清除。该检查用于验证打包与持久化，不用于难度验收。

解压后的 `.app` 在源码目录之外，另外以真实 OpenGL 窗口完成相同的 28 + 11 + 19 项检查；三次独立进程全部正常退出，日志无脚本、渲染或资源泄漏错误。测试未读取或覆盖正常玩家存档。

候选包：`builds/深渊协议-0.9.2-macOS.zip`。解压件：`builds/macos-0.9.2/深渊协议.app`。

SHA-256：`27d459270da75ce3fc91eae66a0ec2b4685c81b290eccad93e1ac3e15e1f3300`。

构建记录：`builds/深渊协议-0.9.2-macOS.json`；原生验收记录：`builds/qa/release-0.9.2-native.json`。实际平台 Apple M4 Pro / Godot 4.7.2 / OpenGL Compatibility，二进制包含 arm64 与 x86_64。当时的运行代码与资源和本包一致。本地发行交付项已完成；实体手柄、Intel 硬件、完整新玩家体验和公开发行公证仍按前述边界保留，不能据自动检查勾选全部 production 目标。


### 历史本地候选 · 0.9.3

本轮完成两项实际体验修复：近战伤害范围超过画出的预警，以及 Retina 默认窗口按物理像素计算导致窗口与文字只有预期的一半大小。

近战预警现在画出与判定一致的完整扇区，保持原有伤害和时机。首领扇形与环形攻击的箭头和实际发射共用方向列表；地面锁定用三环提示，随后产生的伤害区域仍保留完整 0.95 秒预警。血条显示正在准备的攻击名称，指南同步解释各类提示。presentation 通过 523 项检查，包含八方向近战边界、实际伤害、12/16 发弹道、三处地面延迟和首领标签，并检查了对应原生截图。

本机屏幕实测 scale 2.0、DPI 220。默认窗口现在约为 2550×1594 物理像素（1275×797 桌面点），根据当前屏幕可用范围居中；此前 1280×800 像素只有 640×400 点。原生 production 64 项通过，含桌面点数下限及屏幕边界检查，默认菜单截图确认中文尺寸与布局。现有多窗口布局、中文字体、对比度、关闭震动/闪光、危险几何和首领状态检查共同满足当前 Mac 可读性工程验收；真人首次游玩体验仍单列待验收。

两组原生性能证据均通过 44 项检查，各含 900 帧与 40 次重开，零孤立节点增长。1440×900：p50 14.517 ms、p95 22.707 ms、p99 23.818 ms；Retina 默认 2550×1594：p50 16.727 ms、p95 23.143 ms、p99 25.478 ms，最大 32.883 ms，峰值 294 节点 / 33 弹体。记录分别在 `builds/qa/performance-0.9.3-1440.json` 与 `builds/qa/performance-0.9.3-retina.json`。这些是本机压力场景数据，不声称全部帧锁定 60 FPS。

最终 `python3 export_macos.py` 已完整通过资源导入和九套源码检查：smoke 41、weapons 123、campaign 1672、story 943、production 62、checkpoints 913、animation 6412、presentation 523、aiming 6，全部零失败。独立发行包自检 28 项、首次存档写入 11 项、第二进程冷启动读取与继续救援 19 项通过，压缩包和 ad-hoc 签名验证通过。

候选包：`builds/深渊协议-0.9.3-macOS.zip`。解压件：`builds/macos-0.9.3/深渊协议.app`。

SHA-256：`53340781a8383917ed6994b8e0813cf4297c03a6c4d5493d07ae848b19babf30`。

最终解压件又在源码目录之外，用真实原生窗口完成 28 + 11 + 19 项发行与跨进程存档检查，全部正常退出，完整日志没有 `ERROR:`。记录在 `builds/qa/release-0.9.3-native.json`，其包校验值与上述最终 ZIP 相同。运行资源与本包一致；后续文档更新不会改变打包内容。

目前连接的实体手柄数量实测为 0。键鼠/控制器事件与菜单路径有自动和系统 UI 操作证据；不能据此宣称完成实体手柄验收，也不能把普通规则机器人通关等同于真人首次游玩的难度与节奏反馈。已向用户征求具体试玩反馈。剩余操作体验、真人节奏验收与外部硬件边界仍然开放，production 总目标保持进行中。


### 历史本地候选 · 0.9.4

控制器菜单审查复现了两个遗漏：等待键盘新绑定时，手柄右键和菜单键都无法取消。已修复输入分发顺序，取消后保留原绑定并停留在设置；录入期间其他手柄导航/确认不会误触菜单。焦点留在等待新键的操作上，中文提示说明如何取消。

新增控制器菜单流程通过 Godot 的方向焦点图规划路径，然后真实分发每个十字键事件并验证焦点，使用确认键激活按钮，不通过直接抓取焦点或发射按钮回调绕过输入。覆盖十八项武器目录、最后一项装备选择、音量滑杆、绑定取消、防误触、滚动指南、永久升级购买、商店购买、维修、强化/路线选择、六段剧情、结局及重新开局。初次 203 项检查复现两项取消失败；修复及补充断言后 headless production 211 项、原生 production 213 项全部通过。已查看 `builds/qa/controller-key-capture.png`。

该流程禁用玩家存档，给测试货币并直接清理战斗，用来验证菜单导航，不作为正常难度通关或实体手柄实测的证据。原生运行日志没有脚本、渲染或退出错误。

最终 `python3 export_macos.py` 全部完成：资源导入，smoke 41、weapons 123、campaign 1672、story 943、production 211、checkpoints 913、animation 6412、presentation 523、aiming 6，九套均零失败。独立发行程序自检 28 项、存档写入 11 项与第二进程冷启动读取 19 项通过，压缩包完整性与本地签名通过。

候选包：`builds/深渊协议-0.9.4-macOS.zip`。解压件：`builds/macos-0.9.4/深渊协议.app`。

SHA-256：`2a43dde647180314a6c3675437ea7ada7eb24247274607c4d36c4b5dd1d07d7d`。

最终解压的 `.app` 又在项目目录之外完成真实窗口下的 28 + 11 + 19 项发行/续档检查，全部零失败、正常退出且无 `ERROR:`。`builds/qa/release-0.9.4-native.json` 的归档校验值与最终包一致。

本轮不改动战斗绘制、敌人或动作热路径，保留 0.9.3 的 1440×900 和 Retina 2550×1594 性能证据，未无故重复压力测试。`westworld/`、旧浏览器游戏、Pages 和已有 `output/` 更改仍未参与本轮修改或验证；没有提交、推送、部署或公证。

### 剩余验收审计

- 核心流程、持久化、设置、中文与可读性、视听升级、当前 Mac 稳定性、本地发行和自动回归，均有上文代码、原生图像与执行记录支持。
- 操作体验的自动菜单覆盖已补齐；实体控制器仍未连接，之前实测数量为 0，没有可代替实物映射、连接、漂移和人体操作的证据。
- 难度与节奏仍缺真人首次游玩的完整反馈。普通规则输入机器人、加速战役检查和菜单用战斗夹具不能证明新玩家理解和体验。
- 已向用户提出试玩反馈问题，尚未收到答复。当前已知可自主修复的问题均已处理；下一步由实物控制器与真人反馈提供新的验收证据或具体问题。保留 production 总目标，不以增加无关功能代替这些验收。
- Intel 实机与公开 Apple 签名/公证仍是交付边界；本机只有 Apple Silicon，当前交付是本地候选包，没有擅自进行公开分发。

### 2026-09-11：等待外部验收

0.9.4 完成本地验证后，连续三轮复核均未收到真人试玩反馈，原生引擎每次检测到的实体手柄数量均为 0。当前没有新的已知代码问题或尚在运行的验证任务可以继续处理。总目标标记为受阻而非完成；收到试玩反馈或连接实体手柄后，继续剩余的操作体验及难度与节奏验收。现有候选包与验证记录保留。

### 2026-09-12：恢复后的复核与试玩交付

恢复目标后重新核对工作树：运行源码、资源与测试均没有晚于已验证 0.9.4 归档的修改，ZIP 校验值仍与构建及原生检查记录一致。未重复执行已经通过且范围未变的回归套件。重新通过原生引擎检测，连接的实体手柄数量仍为 0。

通过正常应用启动路径打开并显示 `builds/macos-0.9.4/深渊协议.app`，确认实际主菜单为 0.9.4，中文、插画和窗口尺寸正常，并显示第 7 舱续玩入口。应用保留打开供用户继续试玩；没有操作继续、重开或更改已有进度。此项提供当前候选版普通启动与既有续玩入口的实机证据，真人体验反馈及实体手柄验收仍未完成。

恢复后的连续三轮复核仍缺相同的真人体验反馈与实体控制器验收证据，源码没有新增修改或待处理故障。总目标再次标记为受阻，保留完整验收范围；收到新的体验反馈、设备实测结果或明确的验收范围调整后继续。后续复核没有重启或操作用户的游戏会话。

### 2026-09-12 · 最新本地候选 0.9.5

用户要求再次尝试后，重新审查实际输入绑定发现此前自动检查的盲点：新建 Godot 手柄事件默认绑定设备 0。编号 1、3 的输入虽能切换 HUD 提示，却无法移动、攻击或操作菜单。系统为重连手柄分配其他编号时可能触发该问题。此前“没有新的已知问题”的判断不覆盖这个遗漏。

游戏按键、菜单按键和摇杆轴现统一用 `device = -1` 接受系统分配的任意编号。保持单人玩法及原有操作方式。新增检查通过 `Input.parse_input_event` 分发编号 1、3 的真实事件，验证角色位移、右摇杆瞄准、五项战斗动作、暂停、焦点确认恢复与按键释放；完整控制器菜单流程改用编号 3。原有键鼠与编号 0 检查保留。修改前 231 项检查中有 18 项失败；修改后 headless 231 项、原生 OpenGL 233 项全部通过。原生按键录入截图确认中文提示与焦点正常。

发行程序自检的移动改用编号 3 的摇杆事件，并增加暂停与确认恢复，避免通过 `Input.action_press` 绕开需要验证的设备映射。

最终 `python3 export_macos.py` 完成资源导入及九套源码检查：smoke 41、weapons 123、campaign 1672、story 943、production 231、checkpoints 913、animation 6412、presentation 523、aiming 6，全部零失败。独立发行自检 30 项、首次存档写入 11 项与第二进程读取/续玩 19 项通过；压缩包完整性和 ad-hoc 签名通过。

候选包：`builds/深渊协议-0.9.5-macOS.zip`。解压件：`builds/macos-0.9.5/深渊协议.app`。

SHA-256：`de7fd81d062e1737a37b96cd95e6159cfdb30c00a21829b09ef641ded7a3ac90`。

最终解压件又在源码目录之外，以原生 OpenGL 窗口完成 30 + 11 + 19 项验证，均正常退出且无 `ERROR:`。构建记录 `builds/深渊协议-0.9.5-macOS.json` 与原生记录 `builds/qa/release-0.9.5-native.json` 的校验值一致；源码、资源和导出配置没有晚于此归档的修改。实际平台为 Apple M4 Pro，二进制同时含 arm64 与 x86_64。没有操作或终止既有游戏进程，没有读写正常玩家存档。

本次仅修改输入绑定和验证入口，继续沿用前述视听设计与 0.9.3 的两组原生性能证据，未重复无关压力测试。没有提交、推送、公开分发或公证；其他游戏的已有修改保持不变。

实体控制器连接/映射/漂移与真人首次游玩的难度、节奏反馈仍缺证据，前述两个必选验收项继续保持未完成。模拟非零编号事件证明本次缺陷已修复，不能代替实体手柄或真人体验验收。

### 2026-09-12 · 0.9.5 交付后的受阻复核

完成非零手柄编号修复与新包交付后，连续三轮复核仍未收到新的真人试玩结果。每轮通过 macOS GameController 框架在无窗口进程中检测，连接的实体控制器数量均为 0。源码与测试没有晚于 0.9.5 归档的修改，包校验值仍与构建、原生验证记录一致；已知验证进程均已完成，没有可继续等待的验证任务。

剩余阻碍仍是实体手柄全流程与真人难度/节奏验收。目标标记为受阻，保留上述未完成项；新的试玩反馈或设备实测证据可提供下一步工作。未重复已通过的回归，没有操作用户游戏会话。

### 2026-09-12 · 0.9.6 增加 Windows 发行

用户将 Windows 支持加入目标后，新增 Windows Desktop x86_64 预设、官方模板准备脚本、Windows 导出脚本、中文玩家说明和独立 PowerShell 验证入口。Mac 保留，两个平台版本号同步为 0.9.6。游戏普通运行内容未改变；发行自检在已验证的临时存档目录内增加原生截图，用于核对跨平台字体、菜单与续玩界面。

`prepare_windows_templates.py` 使用固定 SHA-256 校验 Godot 官方 4.7.2 完整模板包，再提取 Windows 模板并记录文件校验值。`export_windows.py` 校验版本、运行九套检查、导出原生 PE32+ x86_64 程序，确认图标和中文产品/版本资源，排除开发文件，打包中文说明与完整引擎/库/字体许可。Windows 包无 Python/Godot/Wine 运行依赖。Mac ZIP 同时增加完整第三方许可文件，放在签名应用旁，不改动签名封装。

Windows 验证通过包内 `Verify-Windows.ps1` 执行，兼容 Windows 原生和明确标记的 Wine 测试；后者使用独立且带标记的 prefix，从未使用默认用户 prefix。每次游戏的存档与报告位于单独临时目录，存档路径包含空格；导出测试目录及程序路径包含中文和空格。脚本检查退出码、引擎错误和各阶段总结，并将已测 EXE/PCK 的 SHA-256 写入报告。

本轮 `python3 export_windows.py --wine ... --wine-prefix ... --rendered` 已完成资源导入、smoke 41、weapons 123、campaign 1672、story 943、production 231、checkpoints 913、animation 6412、presentation 523、aiming 6，九套零失败。随后 Windows 发行程序在 Wine 11.0 / Rosetta / Apple M4 Pro 下完成：headless 战役 30、存档写入 11、第二进程读取 19；OpenGL 窗口战役 30、写入 13、读取 22，全部零失败。后两项增加了五次保存截图检查；这不增加真人易用性证据。

Windows 原生 exe 的渲染截图已查看：标题、中文插画/文字、第三舱商店和购买后的续档信息正常。图像位于 `builds/qa/abyss-windows-791719a959e847bbb8baafae7d8e9e07/storage with spaces/`。Wine 日志存在 SDR 白电平查询警告，无引擎 `ERROR:`；此平台不能证明 Windows 显卡、DPI、音频输出或实体控制器行为。

Windows 候选包：`builds/深渊协议-0.9.6-Windows-x64.zip`（约 73.8 MiB）。

SHA-256：`4e63a8b529f2ee3b993b211d0491766dfc017de8f495f0c04acb26328d50c101`。

构建/运行记录：`builds/深渊协议-0.9.6-Windows-x64.json`；包含各文件校验值、官方模板来源以及 headless/渲染两组 PowerShell 报告路径。ZIP 完整性、所有打包文件与报告校验值均已复核。

从最终 Windows ZIP 解压后，另测试了 `ANGLE-compatibility.cmd` 对应的 `opengl3_angle` 渲染参数。本机 Wine 无法初始化 EGL display，未完成该运行，已停止这一个失败的隔离测试进程。记录见 `builds/qa/windows-0.9.6-angle-limitation.json` 与其引用日志。默认 OpenGL 路径通过；Windows 真实 Direct3D 11 兼容路径仍待实机验收，不能把 Wine 失败推断为所有 Windows 的结果，也不能宣称该路径已通过。

Mac 候选包：`builds/深渊协议-0.9.6-macOS.zip`。

SHA-256：`68446c1aded5e5f7c0bfab65033ba121f4bfb924e5d20b53d3c5d526811427cc`。

Mac 导出复用本轮刚通过的九套源码检查；独立发行 headless 30 + 11 + 19 通过，包签名/完整性通过。最终解压件又在源码之外通过原生 OpenGL 30 + 13 + 22 项，均零失败、正常退出且无 `ERROR:`；原生记录 `builds/qa/release-0.9.6-native.json` 的校验值与最终 ZIP 一致。保留 arm64/x86_64 通用二进制，本机实测 Apple Silicon。

两个候选包都没有晚于其归档的运行源码、资源或测试修改；没有提交、推送、上传或公证。已有视听设计和性能热路径保持不变。Windows 工程交付已有证据，Windows 实机验收、实体手柄和真人难度/节奏反馈继续保持未完成，production 总目标仍未达到。

### 2026-09-12 · 0.9.6 交付后的受阻复核

连续三轮复核未出现 Windows 原生验证报告或真人试玩反馈，macOS GameController 每轮均检测到 0 个实体手柄。本机虽有 QEMU 可执行程序，但未找到可用于本轮验收的 Windows 系统或正在运行的 Windows 虚拟机；已询问用户可提供的 Windows 验收条件，尚未收到答复。

0.9.6 的两个归档校验值仍与各自验证记录一致，运行源码和测试没有新增修改，已启动的验证任务均已结束。剩余条件需要 Windows 实机/设备与真人反馈提供新证据，目标标记为受阻，未标记完成。所有候选包、成功验证与 ANGLE/Wine 失败记录保留。

### 2026-09-12 · Windows SMB 交付核验

用户授权 Windows 游戏部署到 `smb://Rog-xx/Games`。已确认 `/Volumes/Games` 是该主机与共享的可写 SMB 挂载。检查时共享中已有 0.9.6 ZIP 和完整解压目录；全部内容与本地验证过的发行包一致，因此沿用现有文件，没有重复复制或覆盖。

启动路径：`\\Rog-xx\Games\深渊协议-0.9.6-Windows-x64\深渊协议 0.9.6\AbyssProtocol.exe`。

共享根目录中的 `深渊协议-0.9.6-Windows-x64.zip` 为 77,427,933 字节，SHA-256 为 `4e63a8b529f2ee3b993b211d0491766dfc017de8f495f0c04acb26328d50c101`。从 SMB 重新读取 ZIP 和全部七个解压文件，逐一核对构建记录、本地文件与归档内容，均一致。核验报告保存于 `builds/deployments/windows-0.9.6-rog-xx.json`。

本次仅核验交付文件，没有通过 SMB 执行程序，没有修改玩家存档。已有 Wine 运行证据保持有效；Rog-xx 原生 Windows、实体手柄与真人试玩验收仍待完成。


### 2026-09-12 · 0.10.0 双人手柄同屏与 1440p

新增双人手柄配对页、独立操作/武器/耐久/能量、同屏共享镜头、双人 HUD、倒地修复、断线换编号恢复和两人同一事务的安全点存档。共享菜单只接受当前主控席位，任一玩家可暂停；两人都离线时一号席恢复主控并可直接重开双人救援。配对页也支持通过手柄取消和鼠标返回。

窗口从固定比例留边改为扩展画幅，2560×1440 真实帧缓冲为完整 16:9。原生截图确认两人分处舱室两端时均在画面内，双人耐久分别位于左右上角。Mac 全屏实际输出 2992×1876，与系统报告的安全区域相符；显示器物理分辨率为 2992×1934，差异来自系统刘海安全区。未修改系统显示设置。

实现过程中修复了有续档时主菜单高度溢出，以及双人全灭后主控席位仍指向二号导致一号无法重开的缺陷。增加了对应回归检查；新增双人场景套件使用 InputEvent 设备 1、3、7，覆盖席位隔离、独立瞄准/五项技能、蓄力暂停、实际弹体/地面/近战伤害、修复、共享奖励、换编号重连、磁盘续档与完整剧情流程。

Mac 双人 2560×1440 压力场景已通过 44 项检查：900 帧，p50 16.797 ms、p95 21.204 ms、p99 21.9 ms，最大 30.09 ms，峰值 309 节点/37 弹体；40 次重开，零孤立节点。报告 `builds/qa/performance-coop.json`。完整普通数值 Input-action bot 以两把速射枪通关十二舱、六段剧情，记录 `builds/qa/playthrough-coop.json`；这是自动操作证据，不是真人体验研究。

发行程序自检增加双设备配对/独立移动/双人战役，并在同一个带标记的临时目录中用两个独立 profile 分别验证单人和双人的跨进程续玩。正式玩家存档不参与验证。构建资源的 SHA-256 清单记录于 `builds/qa/source-0.10.0.json`。本轮完整构建和共享交付结果在下方追加；Windows 实机与两只实体手柄验收仍未完成。


0.10.0 最终源码十套检查：smoke 41、weapons 123、campaign 1672、story 945、production 233、checkpoints 913、animation 6412、presentation 523、aiming 6、coop 81，全部零失败。最终源码原生双人套件 91 项通过，包含三张 2560×1440 截图、系统安全区全屏与退出全屏。相关日志 `/tmp/abyss-0100-windows-export.log`、`/tmp/abyss-0100-native-coop.log`。

Windows 0.10.0 最终原生 PE 在 Wine 11.0 下完成 headless 34 + 13 + 24 项、窗口 OpenGL 34 + 16 + 28 项，全部零失败。报告目录分别为 `builds/qa/abyss-windows-5aefca081dd24bff81d0d2ea60a7baa9/` 和 `builds/qa/abyss-windows-fa021a724de547468435ebae57e889df/`。窗口存档阶段同时保存了双人购买与续档截图。成功日志仅有 Wine SDR 白电平警告，无引擎错误；没有把兼容层证据当作 Windows 实机验收。沿用官方引擎的 ANGLE/Wine 初始化限制，未重复声明 ANGLE 通过。

Windows ZIP SHA-256：`e6f293196bebb203356aa1591bee7a503f7c1977fb5edeea503bdde005082b58`。
Mac ZIP SHA-256：`8b7139f9cad47d8d6606f0b9e1873b3e287a1fbabc3b6735c19d8d443ba4e24f`。
Mac 包为 arm64/x86_64 通用、ad-hoc 签名，导出包签名与独立 headless 34 + 13 + 24 项通过。

Windows 已交付至 `\\Rog-xx\Games\深渊协议-0.10.0-Windows-x64\深渊协议 0.10.0\AbyssProtocol.exe`；共享根目录另有 77,443,196 字节 ZIP。完整 ZIP 与七个文件从 SMB 重读并通过 SHA-256 校验，旧版 0.9.6 保留。共享端拒绝文件夹重命名，因此使用独占逐文件复制、EXE 最后写入的方式；临时数据文件已清理，服务器仍拒绝删除两个空的隐藏暂存目录。交付记录 `builds/deployments/windows-0.10.0-rog-xx.json` 包含精确路径和校验值。没有修改系统配置、正式玩家存档、其他游戏，未提交或公开发布。

最终 Mac 解压应用又通过原生窗口 34 + 16 + 28 项独立发行检查；报告 `builds/qa/release-0.10.0-native.json`，对应 ZIP 校验值与上述最终归档一致。两个系统的打包程序都已覆盖单人和双人续档。Windows 硬件、两只实体手柄和真人难度反馈仍未完成，production 总目标保持未完成。


### 2026-09-12 · 0.10.1 合作菜单收尾

检查最终发行截图发现补给站只显示一号耐久，双人时维修可用状态不够明确。现已在补给、路线和维修界面显示两人的实际耐久，商店卡片明确标注全队效果，强化页说明两人各自恢复耐久与能量。游戏内操作指南加入双人配对、救援、共享资源和断线恢复说明。

结算页增加返回船坞，双人结算可重新选择双人装备；重新派遣保留合作模式及所选装备。一号武器选择同步到下次开局的默认装备，避免武器库显示与实际出发不一致。新增检查验证第二人的数值展示与布局、以及手柄从合作结算返回模式选择。

源码完整十套再次通过，production 235 项；双人套件在完整批次通过 83 项，随后加入结算返回检查后单独通过 85 项。运行资源清单 `builds/qa/source-0.10.1.json`。本次修改菜单与开局装备选择，没有改变战斗、镜头或声音热路径，沿用 0.10.0 的 QHD 双人压力与正常数值自动通关证据。


最终 0.10.1 原生双人套件 95 项通过。Windows 发行程序在 Wine 下通过 headless 34 + 13 + 24 项、OpenGL 窗口 34 + 16 + 28 项，报告为 `builds/qa/abyss-windows-63ac7b9e147e4620b507c890ea05cf03/report.json` 与 `builds/qa/abyss-windows-e7d491f949ea4c0393bb33a35e8805a2/report.json`。窗口截图已确认双人耐久、全队补给说明与已购买状态一致。

Windows 最终 ZIP SHA-256：`f0ab8ffb438a560176ef09c255823aed4ec77fbecd95b927a2564fa88a7b469b`。
Mac 最终 ZIP SHA-256：`6f9bad2a39a7006403830affc3d06ab3b1376ffbd68779bab498c8097629b0d6`。
Mac 复用刚完成的源码回归，导出/签名和独立 headless 34 + 13 + 24 项均通过；原生窗口跨进程续档记录为 `builds/qa/release-0.10.1-native.json`。

最终 Windows 交付：`\\Rog-xx\Games\深渊协议-0.10.1-Windows-x64\深渊协议 0.10.1\AbyssProtocol.exe`。共享根目录的 ZIP 为 77,444,090 字节，七个文件和 ZIP 均重读校验通过。因已知共享端不允许目录重命名，直接采用新版本目录和 EXE 最后写入方式，未产生新的暂存目录。部署报告 `builds/deployments/windows-0.10.1-rog-xx.json`。旧版本和正式玩家存档保留，未提交、推送或公开发布。Windows 实机、实体双手柄和真人体验验收继续保持未完成。

### 2026-09-12 · Windows 实机验收交接

续轮核对：67 项运行资源与 0.10.1 清单相同，SMB 上七个游戏文件仍匹配部署记录；未发现新的 Windows 原生运行报告。本机 GameController 当前检测到 0 个实体手柄。上一轮完成了实现、构建和交付，属于实际进展；本次不重复已通过的整套游戏测试。

在共享盘版本外层添加 `一键Windows验收.cmd`、`Run-Windows-Acceptance.ps1` 与 `Windows验收说明.txt`，源文件分别位于 `tools/run_windows_acceptance.cmd`、`tools/run_windows_acceptance.ps1` 和 `docs/windows-acceptance.txt`。游戏 ZIP 和内层七个文件保持原样。

入口核对 EXE、PCK 和现有验证脚本的固定 SHA-256，要求在 Windows 执行，在本地 TEMP 下使用已有隔离自检；成功后验证报告平台、渲染标记、文件哈希和三个阶段的结果，仅复制本轮日志、七张截图和报告到同版本外层的 `验收记录/<运行编号>/`。不复制 profile 或 settings。复制校验成功后才写 `handoff.json`，以免把半份报告当成完成。

已通过 PowerShell AST 语法检查、非 Windows 启动保护以及部署字节/哈希校验。PowerShell 文件按 UTF-8 BOM 和 CRLF 交付，以兼容 Windows PowerShell 5.1 的中文路径；入口只对当前进程传入执行策略参数，没有修改机器执行策略。此处没有执行 Windows 程序，也没有声称入口已在真实 Windows 上验证。交付记录 `builds/deployments/acceptance-0.10.1-rog-xx.json`。

说明文件列出两只实体手柄的独立操作、断线重连、修复、共享镜头及 2560×1440 全屏验收步骤，并给出 `试玩反馈.txt` 的记录方式。仍需 Windows 实际运行与实体设备/真人反馈，目标未标记完成。

### 2026-09-12 · 实机验收受阻复核

交付实现、准备验收交接入口、本轮检查回传结果三个连续目标轮次，Windows 实机与实体双手柄/真人体验证据一直缺失。本轮实际检查时间为 05:18 UTC：共享盘 `验收记录/` 为空、没有 `试玩反馈.txt`，GameController 检测为 0 个实体手柄，当前工具没有 Windows 远程执行入口。源码运行资源与 0.10.1 清单一致，没有新的代码缺陷或运行报告需要处理。上一轮创建并交付验收入口属于进展；本轮的重复核对未产生新的验收证据。

目标标记为受阻而非完成。需要在 Rog 上运行一键验收入口并提供实体双手柄及 1440p 全屏试玩结果后再继续；报告尚未回传不等于已确认远端测试没有运行。没有可追踪的远端运行句柄，因此不把当前状态称为已验证的等待，也不重复启动本机的已通过套件。

### 2026-09-12 · 修复验收工具权限错误处理

用户反馈 Windows 游戏测试窗口已打开，随后终端提示对 `abyss-windows-…` 的访问被拒绝。完整路径尚未提供；现有信息倾向报告回传阶段，但不能据此认定 Windows 测试已全部通过。检查共享盘时尚无完整回传报告。

验收工具修订 2 增加本地目录创建、写入和重命名预检，TEMP 不可用时尝试当前用户 LocalAppData；成功测试的报告、日志和截图先保留到本地独立诊断目录，再尝试共享盘回传。回传失败单独提示，不再将其混同于游戏测试失败；终端显示具体阶段、本地目录和回传目标，便于继续诊断。无需管理员权限，不修改 ACL 或正式玩家存档。

原脚本在隔离只读目录中复现权限异常；修改后 6 项 PowerShell 文件系统回归通过，包含失败回传保留本地证据、缺失证据不生成凭据、已有远端报告不覆盖。Windows 编码版本的语法检查与非 Windows 保护通过，修订 2 已回传到原 SMB 入口并读回校验。详见 `builds/qa/acceptance-r2/` 与更新后的 `builds/deployments/acceptance-0.10.1-rog-xx.json`。

本轮仅修改外层验收工具、针对性测试和说明；67 项运行资源清单、EXE/PCK 和原始游戏验证脚本均未改变，因此没有重复完整游戏回归或重建发行包。目标 Windows 的权限错误是否已解除、自动验收是否通过，仍需新运行结果确认；实体双手柄和真人体验验收保持未完成。

修订 2 交付轮及随后两轮均仍缺目标机完整结果。交付轮有实际修复进展；随后两轮仅复核，未取得改变下一步行动的新证据。最后核对为 2026-09-12 05:28 UTC：共享盘修订 2 哈希一致，`验收记录/` 为空，无试玩反馈，本机实体手柄为 0；当前工具及可操作应用未提供 Windows 远程执行入口，没有已确认存活的远端测试句柄。目标再次标记受阻，需 Rog 重试结果、完整错误路径或实体设备试玩反馈才能继续。缺少回传不代表远端测试一定未运行，不再无据重建或重复本机测试。


### 2026-09-12 · 0.11.0 五区域三十舱候选

新救援包含五区域、三十舱和五场守卫战，十张本地探索/守卫插画使用对应实体碰撞，配合手工房型池、分支奖励预告与完整预警的分批增援。旧十二舱存档继续使用冻结的地图生成器与路线随机序列。祝福卡显示实际前后数值，攻击、耐久、机动和回复设有成长边界；已封顶的效果不再入选。合法奖励允许一至三项有效候选，并覆盖写盘与冷启动续玩。详细参数与局限见 `docs/balance.md`。

最终源码验证 `python3 verify.py` 完成导入与十六套场景，共 21,061 项、零失败：smoke 56、weapons 123、campaign 5228、story 1466、production 265、checkpoints 2129、animation 6412、presentation 523、aiming 6、coop 136、level_design 1897、campaign_expansion 1059、legacy_campaign 99、balance 937、reward_checkpoints 30、map_art 695。Windows 交付与验收工具的 18 项 Python/PowerShell 测试通过。

Mac 原生图册 53 项、双人输入/全屏 146 项、玩家服务/菜单 267 项通过。图册覆盖全部十张地图、共同战斗、祝福、路线与设置，帧缓冲为 2560×1440；全屏往返恢复原窗口尺寸。Mac 全屏输出为系统报告的 2992×1876 安全区域。检查过程中用户曾手动调整测试窗口，导致部分尺寸断言失败；确认来源后撤去临时窗口处理，玩家窗口实现没有因此改变。性能测试保留尺寸变化即判本次采样无效的保护。

在固定 2560×1440、双人、最终插画首领舱加 28 敌人的原生压力场景中，900 帧样本 p50 16.674 ms、p95 17.125 ms、p99 19.619 ms、最大 21.361 ms；40 次重开后无孤立节点增长，44 项检查通过。实际平台为 Apple M4 Pro / macOS / Godot 4.7.2 / OpenGL Compatibility，报告 `builds/qa/performance-coop-expanded.json`。这不是 Windows GPU 或所有硬件的性能保证。

普通规则输入机器人单人和双人都完成三十舱、168 次击杀与六段剧情，分别耗时 343.4 与 340.3 游戏秒，无导航软锁。它使用精准远程攻击及补给路线，首领约六至八秒，结果不能代替新玩家难度验收；`hp_after` 是下一舱入口值，不能当作战斗最低耐久。原始结果与该边界见 `builds/qa/playthrough-summary.md`。不依据这一条精准自动路线贸然增加伤害或缩短预警。

Windows 验收入口已改为从匹配的构建记录与 ZIP 生成，核对版本、大小、全部文件及逐文件哈希，保留已有版本。中文导出文本统一为显式 UTF-8，PowerShell 策略只影响验证进程。实际发行包、共享交付及其校验值见本节后续记录。Windows 实机、两只实体手柄与真人难度反馈继续保持未完成。

0.11.0 最终发行包验证完成。Mac universal 包通过 CRC、ad-hoc 签名、独立 headless 战役 84 项、首次存档写入 15 项和第二进程续档 70 项。Windows x86_64 包验证 PE 图标/版本、完整资源和许可，在隔离 Wine prefix 中通过 headless 84 + 15 + 70 项，以及 OpenGL 窗口 84 + 18 + 74 项。Windows 包的中文双人补给/续档截图已核对；成功日志只有 Wine SDR 白电平警告，无引擎错误。Wine 不代表 Windows 硬件，ANGLE 仍沿用已记录的 Wine 环境限制，未宣称该路径通过。

Windows ZIP：`builds/深渊协议-0.11.0-Windows-x64.zip`，121,510,029 字节。SHA-256：`a8ff8e4199f3cbcd492fbce5c8d128cc333583ebc522359f046cd5256fb7d8f0`。

Mac ZIP：`builds/深渊协议-0.11.0-macOS.zip`。SHA-256：`887d755c4621f37c0120091311d1db406cf32eec4bd2df2b923c2e479a8fa097`。Mac 未公证、Windows 未签发行者证书；二者没有上传公共发行渠道。181 项运行源码/资源清单为 `builds/qa/source-0.11.0.json`，导出后重新核对无差异。

Windows 已部署到 `\\Rog-xx\Games\深渊协议-0.11.0-Windows-x64\深渊协议 0.11.0\AbyssProtocol.exe`，同版本 ZIP 位于共享根目录。部署前核实 SMB 挂载目标，采用新目录、独占写入与 EXE 最后复制；七个游戏文件、四个验收附件和 ZIP 全部从共享盘读回，SHA-256 与本地验证产物一致。记录为 `builds/deployments/windows-0.11.0-rog-xx.json`。旧版本、其他游戏和正式玩家存档保留。

本次源代码交付采用用户授权的 GitHub PR 提交与合并流程，Godot 不在 Pages allowlist 中；Westworld 与 output 的未完成改动不纳入提交。Windows 实机、两只实体手柄、DPI/Alt-Tab 和真人难度反馈仍需新证据，未把候选包和自动检查称为 production 总目标完成。

### 2026-09-12 · Windows 通过反馈与报告补传

用户明确反馈 Windows 测试通过，但回传报告存在权限问题。本轮共享盘仍未收到该报告，完整权限错误路径尚缺，因此记录为用户通过反馈，尚不能列出已审查的原生报告哈希或断言具体 ACL 根因；实体双手柄、DPI/Alt-Tab 与真人难度反馈继续单独验收。

验收工具修订 4 增加“仅回传验收报告.cmd”，复用本版最近一次通过的本地原生报告，不启动游戏。支持指定原报告目录及可写回传目标；保留旧的完整或半截回传目录，重试写入新目录；回传前生成仅含白名单证据的本地 ZIP，网络权限失败时在资源管理器中选中 ZIP 供转交。没有更改系统/共享盘权限，也没有重打游戏包。

23 项 Python/PowerShell 工具测试通过，包含真实只读文件系统失败、旧目录重试保护、ZIP 精确内容与压缩依赖失败、当前版本报告恢复、Wine/旧版本/失败阶段拒绝、生成编码和非 Windows 入口保护。独立审查确认报告补传分支不调用游戏验证器；Mac 的函数及流程测试不作为 Windows PowerShell 5.1 实机结果。

修订 4 的五个外层附件已部署至 `smb://Rog-xx/Games/深渊协议-0.11.0-Windows-x64/`，逐字节及 SHA-256 回读一致；七个游戏文件与原 ZIP 哈希仍匹配 0.11.0，其他版本保留。交付记录为 `builds/deployments/acceptance-0.11.0-r4-rog-xx.json`。运行源码/资源未变，沿用已通过的 21,061 项场景检查及发行包验证；未重复游戏回归。目标 Windows 的新补传入口仍需运行确认。

### 2026-09-12 · 0.11.1 祝福卡按住态对比度

原生输入复现了祝福卡按住时的浅底浅字问题：子 Label 保持浅色，而 Button 切换到通用浅金背景，说明文字最低 1.10:1、标题 1.81:1。修复仅为卡片的 `pressed` 和 `hover_pressed` 设置不透明深色背景，保留元素边框；普通按钮、祝福数值与选择逻辑未变。九类祝福在普通与高对比文字下的静止/焦点、悬停、鼠标按住和设备 3 手柄按住状态均由真实输入事件验证，按住后松开才提交选择。新增对比度段 475 项，修改前 108 项对比度失败，修改后零失败；所测文字最低 5.48:1、按住态最低 6.11:1。

最终 `python3 verify.py` 完成资源导入与十六套场景，共 21,524 项、零失败；其中 production 728 项，其余套件数量同 0.11.0。Mac 原生 production 738 项通过，含上述 475 项对比度检查，未发现脚本错误。鼠标与手柄按住的 1440×900 截图位于 `builds/qa/blessing-mouse-pressed.png`、`blessing-controller-pressed.png`；只生成并查看项目外 1024×640、各约 39 KB 的 JPEG 预览。卡片标题、收益与说明在按住状态清晰，未发现裁切。23 项 Python/PowerShell 工具测试亦通过。

181 项源码/资源哈希相对 0.11.0 仅 `scripts/hud.gd`、`project.godot` 和 `export_presets.cfg` 变化，后两者为版本元数据；清单为 `builds/qa/source-0.11.1.json`。地图与战斗实现未改，沿用前版原生 QHD、双人性能与完整流程的证据；没有将静态样式修复扩展为重复压力采样。

Mac universal 发行包通过归档 CRC、ad-hoc 签名、独立战役 84 项、首次写盘 15 项和第二进程续档 70 项。Windows x86_64 发行包通过 PE 身份/图标/版本与资源检查；在带标记的隔离 Wine prefix 中通过 headless 84 + 15 + 70 项及 OpenGL 窗口 84 + 18 + 74 项。两次导出使用 `--skip-checks` 复用刚完成的全套源码验证，发行包自身的战役与存档检查均完整执行；181 项源码/资源哈希在导出后再次核对一致。Wine 不作为 Windows 硬件或 ANGLE 通过证据。

Windows ZIP 为 `builds/深渊协议-0.11.1-Windows-x64.zip`，121,510,199 字节，SHA-256 `549022a0cdefac42b78259aa499512a33c28d88559e3f99e72f296cf9a022684`。Mac ZIP 为 `builds/深渊协议-0.11.1-macOS.zip`，144,306,589 字节，SHA-256 `a1abecc71404787e1a0e163110688620cfd03789dc58fc6c46c1c7efd3dba29e`。Mac 未公证、Windows 未签发行者证书，均未上传公共发行渠道。

已核实 `//macshare@Rog-xx/Games` 挂载并部署到新的 `深渊协议-0.11.1-Windows-x64/`，七个游戏文件、修订 4 的五个验收附件及共享根目录 ZIP 均从目标读回并验证 SHA-256。部署记录为 `builds/deployments/windows-0.11.1-rog-xx.json`。旧版 ZIP 与验收附件基线保持一致，没有覆盖旧版本或其他游戏。旧版已通过报告应使用 0.11.0 外层的“仅回传验收报告.cmd”补传；0.11.1 入口只接受与新包匹配的结果。

本次交付时，0.11.0 的 Windows 通过反馈仍有效，但原生报告尚未取回；0.11.1 Windows 实机、实体双手柄、DPI/Alt-Tab 与真人难度体验仍待对应结果。本次完成可读性修复和候选包交付，不将这些自动检查称为 production 总目标完成。

### 2026-09-12 · 取回 Windows 原生报告与 0.11.1 无界面验证

已通过 SSH 取回用户先前运行的 0.11.0 原生报告、六份日志和七张截图，14 个证据文件均与 Windows 端 SHA-256 一致。EXE/PCK 哈希匹配 0.11.0 交付记录；战役 84 项、存档写入 18 项、第二进程读取 74 项全部零失败，日志无 `ERROR:`。实际平台为 Windows 11 64 位、系统版本 10.0.26200、AMD Radeon 890M，渲染器为原生 OpenGL。七张截图均为 1280×800；只查看了项目外 1024×640 的合作续档 JPEG 预览，不能据此认定 QHD 或实体手柄验收完成。

原始证据位于 `builds/qa/windows-0.11.0-rog-xx-recovered/`。白名单证据 ZIP `Windows验收报告-0.11.0-7b7de0fa.zip` 已放回共享盘 0.11.0 版本外层，与验收入口同目录，回读 SHA-256 为 `9c9a90efbae8a3f21d631b1f25a33af3150384e7b542c65fb9efa9c8ffe59214`。归档不包含测试存档或正常玩家数据。本次通过 SSH 取回后由 Mac 写回共享盘，没有改变 Windows/SMB 权限，也没有把此次恢复认定为原回传入口的权限问题已修复。

随后在同一 Windows 设备核对 0.11.1 的七个发行文件，并通过原包 `Verify-Windows.ps1` 完成战役 84 项、独立存档写入 15 项和冷启动读取 70 项，合计 169 项、零失败，日志无 `ERROR:`。该次原生执行通过 SSH，进程位于 Session 0，使用无界面模式和新的隔离存档目录。原始报告、日志及会话范围说明位于 `builds/qa/windows-0.11.1-rog-xx-headless/`；对应证据 ZIP 已放回 0.11.1 版本外层。

181 项运行源码/资源哈希保持不变，因此本轮没有重新构建发行包或重复 Mac/Wine 场景套件。文档变更通过差异检查；Windows 实际运行是本轮新增证据。0.11.1 桌面窗口渲染与输入/音频、两只实体手柄、2560×1440 无边框全屏、DPI/Alt-Tab、目标机帧时间及真人难度体验仍待验收，production 总目标保持未完成。


### 2026-09-12 · 0.11.1 Windows 报告确认与 Games 根目录回传

用户从 Windows 复制回 `diagnostics-09b77a25a2244c92b83d2cd7dbcad59e.zip`，检查报告及六份日志：原生 OpenGL 战役 84 项、存档写入 18 项、冷启动读取 74 项，合计 176 项、零失败，无引擎错误。EXE/PCK 与已交付 0.11.1 哈希匹配，`handoff.json` 绑定的报告 SHA-256 为 `0d0244befcabddc99463baa976b5137389c2059976cc1f35a7c0ea37f8241e41`。七张截图为 1280×800；只读取文件头确认尺寸，没有加载原始 PNG。用户同时明确“全部通过了，其他没有通过的测试先跳过”，本次按该验收范围收尾，实体双手柄、Windows QHD/DPI/Alt-Tab 与真人难度专项不再阻塞，不记为自动验证通过。

原始报告 ZIP 已复制到 `Games/Windows验收报告-0.11.1-7473adb3.zip`，SHA-256 为 `2d6883c28aed1a9e3cc09aa54566dd92467d7aada178d5d15f3fa31fef5e9224`。旧版 0.11.0 原生和 0.11.1 无界面报告也复制到 Games 根目录；旧文件均保留。

验收入口修订 5 将白名单证据压缩成单个 ZIP，直接写到指定已有目录；本次绑定 Games 共享对应的 Windows 本地根目录。不再创建 `验收记录/运行编号` 或要求重命名权限，重名使用新 ZIP 文件名，复制后回读 SHA-256 匹配才成功。0.11.0 和 0.11.1 五个外层附件均更新并回读核对；两版本的七个游戏文件保持不变，没有重新打包。

23 项 PowerShell/Python 工具回归通过，覆盖只读目标、本地诊断保留、ZIP 白名单、已有完整/半截报告保护、版本匹配补传和生成编码。另在 Windows 原生 PowerShell 执行修订 5 的 `-ReportOnly`，成功将已有报告直接写入 Games 根目录；Mac 经 SMB 读回同一 ZIP，完整性及报告凭据匹配，ZIP SHA-256 为 `6e50af90737980e721b00c97d804cc76286ba5556e47c5b65e42f362cee15b18`。这次执行只回传报告，没有重跑游戏。首次远端回传调用在附件更新完成前仍执行修订 4；修订 5 实测前增加脚本哈希检查并确认已更新，未将旧版调用计为新版验证。

本轮 181 项运行资源哈希仍与 0.11.1 构建时一致，因此沿用已有源码/原生/发行验证，不重复游戏测试。没有修改 Windows/SMB 权限或正式玩家数据。原生报告、恢复记录与根目录回传记录分别保存在 `builds/qa/windows-0.11.1-rog-xx-rendered/` 和 `builds/qa/windows-report-root-r5.json`。


### 2026-09-13 · 0.12.0 局内构筑与升级对比

新增局内构筑总览：实际单击/散弹/蓄力伤害、机体状态、当前符文、自带元素及战斗联动；使用原创 SVG 图标与等级条。单人和双人均可查看，奖励与商店按每席的武器和现有属性显示升级前后收益。Tab、界面按钮与手柄均可进入，返回时恢复原菜单/焦点；战斗中查看后回到暂停。查看、切换席位、重配对均不刷新随机选择，不消费、治疗或改写安全点。

源码十八套检查合计 **21,694 项、零失败**：原验证入口完成十七套，测试拆分后的 `build_menu.gd` 以同一严格验证器单独补跑，现已一并列入 `verify.py`。Mac 原生玩家服务 **742 项**、最终构筑菜单 **57 项**通过；测试使用 Dummy 音频驱动，不改动正式玩家设置。检查了 960×600 与 2560×1440 菜单、双席不同收益、文字边界、Tab 原有设置导航和非零手柄编号的重连/返回。压缩截图保存在本地验证记录，不提交原图。

Mac universal 包先完成 CRC、ad-hoc 签名、无界面战役 **87 项**、首次写盘 **15 项**和第二进程续档 **70 项**。随后增加静音原生窗口验收，发现自检退出早于音频线程释放播放资源；在完整游戏销毁后等待混音线程收尾，消除了相关资源占用错误。导出在上述源码验证后使用 `--skip-checks`，发行包自身检查完整执行。最终 Mac 发行包的原生窗口战役 **87 项**、写盘 **18 项**、第二进程续档 **74 项**全部通过，无引擎错误。归档：`builds/深渊协议-0.12.0-macOS.zip`，SHA-256 `03f4f62e5b31baed277aa2b1a360ddc9e6d960e50b86bc2e1b214b1541fdc9b6`。

打包/报告工具 **24 项**通过；Windows 验证器也指定 Dummy 音频驱动，避免开发验收发声。执行命令为 `python3 -m unittest discover -s tools -p 'test_*.py'`。

该版没有调整敌人、武器或祝福的战斗数值。用户新增的每战斗房约两分钟、设置界面改版与更醒目的全屏入口将作为后续独立改动；不把本次构筑信息改进当作这些目标的完成证据。0.11.1 经用户确认跳过的实体手柄、QHD/DPI/Alt-Tab 与真人难度专项保留原记录；不改写为通过。

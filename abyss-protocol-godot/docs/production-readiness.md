# 正式版本交付标准

用户目标：把 Godot 重制版做到 production quality，提升审美，支持 Windows、双人手柄同屏共享与 2560×1440 全屏。保留全中文、合理剧情、十八种武器；新救援扩展为五区域三十舱室，旧版十二舱存档保持原路线和结局。此文是工作清单，不是已经完成的质量声明。

用户最初确认交付平台为 Mac，2026-09-12 将 Windows 支持加入目标；当前保留 Mac，同时增加 Windows 10/11 x86_64。正式交付必须包含可直接运行的发行包、玩家说明和实机验收；不能用脚本测试通过替代游戏体验验收。

## 本次交付状态

2026-09-14 当前候选为 **0.30.0 雷霆残影与工业字体**。冲刺起点延迟放电可接自己或队友的冻结霜链，沿用普通冲刺冷却；中文标题采用庆科黄油体，HUD 仪表使用 Oxanium 等宽数字，正文保留 Noto Sans CJK，三套字体完整随包提供并有本地中文回退。字体源文件净减少 16,328,036 字节。

最终源码回归 **38 套 / 29,449 项**全部通过，373 个构建/验证输入哈希一致。字体前版本为 29,455 项；差异集中在按实际焦点路径计数的 production（743→739）和按波次采样计数的 encounter_pacing（1089→1087），两套脚本未变，全部套件和成功条件保留。三套字体各覆盖脚本语料 838 字，26px 仪表数字十个宽度均为 15px。Mac 原生设置 1,004、HUD 57、玩家服务 742、菜单对比度 475、雷霆残影 63 项通过；仅查看核过大小的 JPEG 预览。

保留两次原生诊断异常：雷霆残影初次“单次命中并清理”断言失败，原样复核 63 项通过，原因未确定；本轮 HUD 首次退出有两项 ObjectDB 泄漏警告，未改源码的 verbose 复核没有重现。原始日志均保留。Windows 导出日志包装脚本首次缺少项目模块路径，修正包装脚本后才开始成功导出，游戏源码未为此改变。

Mac 独立原生包 **战役 123 / 写入 18 / 读取 79** 项通过，严格 ad-hoc 签名、ZIP CRC、8 个文件哈希及完整字体声明通过，三个原始引擎日志没有脚本错误或泄漏。Windows x86_64 包在 Wine 下分别通过 **无界面 123 / 15 / 75** 与 **窗口 123 / 18 / 79** 项；7 个 ZIP 文件、版本/PE、模板来源、六份引擎日志及完整字体许可核对通过。窗口日志保留已知 SDR 白电平警告。Windows 原生硬件未在本轮运行，Wine 结果不能替代实机验收。

两个本地包：

- macOS：`builds/深渊协议-0.30.0-macOS.zip`，127,012,343 字节，SHA-256 `ffd744b9bcbf781fe321de6cdadcc3ca18a9cf8197553ec07b84edf09ae2db0b`。
- Windows：`builds/深渊协议-0.30.0-Windows-x64.zip`，104,216,614 字节，SHA-256 `c1522f0cf7ac4ff1a117064a8c97ed1b30df8a10ac68ae0f572661cf7418dd67`。

`builds/acceptance-0.30.0/` 的五个验收附件已生成并验证哈希，报告直接写入 `C:\Program Files (x86)\Games` 根目录。交付前复查 Games 未挂载，**0.30.0 尚未复制到 Games**，保留所有已有版本。373 个输入、双平台报告、包清单与原始日志见 `builds/qa/source-0300-final.json`、`dash-type-0300-evidence.json` 和两个 `*-0300-archive-audit.json`。

正常输入冰电刀锋单/双人三十舱、五守卫与六段剧情完成，战斗舱中位 144.45 / 146.95 秒；部分守卫仍超过三分钟。该样本在字体更换前运行，字体阶段只改变 HUD 字体，战斗脚本未变；不能把不同构筑的前后样本当作单变量调校证据。真人/实体手柄/Windows 显示专项继续按授权跳过；0.24.0 P95 29.331 ms 超过 25 ms 的失败及 0.29.1 首次 Mac 战役失败继续保留。整体产品目标仍开启。

### 上一版记录 · 0.29.1


2026-09-14 的上一版本为 **0.29.1 原生发行验证修复**。最小化窗口会停止自动绘制，存档截图改为主动绘制当前帧；退出自检先等待场景实际离开，再留足 200 毫秒真实时间让音频播放实例清理。存档隔离与 55 秒整体截止时间保持不变。37 套源码 / 29,392 项、原生截图 11 项及退出清理 6 项通过。

旧截图方法在最小化时两次超时，修复后确认红 / 绿 / 蓝像素按新场景更新。旧退出等待在加速时钟下只有 12 毫秒，环境音频实例仍被持有；修复后正常 / 加速时钟为 224 / 209 毫秒，两种状态的弱引用均已释放。完整最小化写入 18 项、读取 78 项通过，场景退出至进程结束为 221 / 222 毫秒，没有脚本错误或音频泄漏。压缩预览确认双人商店与购买状态完整。

Mac 首次导出的独立战役为 108 项 / 3 项失败，原始日志和 ZIP 保留。源码只读观察随后完成单/双人三十舱，同一 ZIP 独立战役 118 项、写入 18 项和冷启动读取 78 项通过；没有更改二进制或进行窗口置前操作。三个复核引擎日志及独立解压后的 8 个文件哈希、版本和严格签名通过。初次战役失败原因仍未确定，不能归因于已修复的截图或退出等待。Windows Wine 无界面 118 + 15 + 74 项、窗口 118 + 18 + 78 项通过，六份引擎日志无脚本错误或泄漏；窗口日志保留已知 SDR 白电平警告。双平台 ZIP、逐文件清单和验收附件哈希已核对。

0.27.0–0.29.0 的历史超时、以及本轮修复退出等待前的音频警告均保留。旧日志未记录具体停顿点，不能把所有历史失败都归为本次找到的原因。此轮只改变自检与退出流程，战斗和成长沿用 0.29.0；该版重返飞刃机器人战斗中位为 118.95 / 122.85 秒，末战 57.7 / 52.9 秒仍待调校，未把旧样本写成本版新通关证据。

Games 未挂载，**0.29.1 尚未交付到 Games**；本地验收入口把报告直接写到 `C:\Program Files (x86)\Games` 根目录。真人、实体手柄和 Windows 显示专项按授权暂跳过，Windows 原生硬件证据仍属于 0.11.1。0.24.0 P95 29.331 ms 超出 25 ms 的失败继续保留，本轮未重跑性能验收。整体产品目标保持开启。

2026-09-12：0.11.1 已交付，用户在 Windows 桌面执行的 176 项检查全部通过，报告已核对并保存到 Games 根目录。用户随后要求其余未通过或未完成的测试先跳过。以下标注“本次跳过”的专项不再阻塞本次交付，但保持未验证状态，不以勾选代替实测。历史实施记录保留原时点状态。


- [x] 完整循环：开局引导、武器选择、战斗、分支、商店、成长、五场区域守卫战、胜负与再次游玩；剧情线索因果一致。
- [ ] **本次真人/实体手柄专项跳过**。操作体验：键鼠和手柄可完成全流程；按键提示准确；支持重绑主要键盘输入；瞄准模式清晰；菜单导航和焦点切换可靠。
- [x] 玩家设置：音量、全屏、屏幕震动、闪光强度、瞄准辅助；设置跨启动保存；无法写入时提示。
- [x] 持续游玩：安全点自动存档、退出再继续；避免重复奖励/购买/永久成长；损坏/未来版本保护；窗口关闭、失焦与退出行为一致。
- [x] 可读性：常见桌面比例与窗口尺寸下中文不裁切；战斗危险与命中可辨；首领耐久和阶段清晰；弱视/运动敏感选项有效。
- [x] 视听表现：角色有连贯行动与攻击反馈、敌人有动作和前摇反馈、场景分区可辨、音效有层次和环境氛围；无裸露调试信息。
- [ ] **本次真人专项跳过**。难度与节奏：真实操作验证新玩家可理解且首局有合理容错，武器各有用途，后期/首领无软锁或必受伤组合。
- [ ] **帧时间波动未达标项按授权暂跳过**。性能与稳定性：真实渲染密集战斗、长流程和资源回收已有证据，但默认配置压力测试出现 P95 超过 25 ms 的失败，后续通过不能覆盖旧失败。不能声明全面性能验收完成；详见 0.23.0 帧节奏诊断与后续版本记录。
- [x] Mac 发行交付：版本号、本地依赖与素材许可、可重复导出脚本、发行包；在独立用户数据目录冷启动和完成流程，记录验证平台及限制。
- [ ] **当前候选 Windows 实机专项跳过**。原生 x86_64 发行包、中文说明、图标/版本/许可、可重复导出和独立存档验证已建立；176 项 Windows 原生窗口检查属于 0.11.1，不能作为后续候选的实机通过证据。新版 Mac/Wine 结果按版本单独记录，实体输入与音频主观体验仍未验收。
- [ ] **本次实体专项跳过**。双人手柄同屏：两只实体手柄分别配对、独立操作、共享镜头和完整合作流程；断线恢复、队友修复、共用强化、双人续档和重开均可用。已实现与自动验证，尚待实体双手柄验收。
- [ ] **本次 Windows 专项跳过**。2560×1440 全屏：16:9 画面及双人 HUD 铺满显示区域；镜头容纳两人和首领；在目标 Windows 设备验收 DPI、全屏切换与帧时间。Mac 原生 QHD 输出已验证，Windows 实机仍待验收。
- [x] 自动验证：对新增的持久化、输入和设置边界补充有效回归检查；所有原有流程、武器、中文与物理检查继续通过。

## 起点记录

2026-09-11 开始 production 质量阶段。现有源代码有完整单人键鼠流程和四套场景验证，但缺少设置菜单、手柄、输入重绑、中途存档、发行包。音效每次攻击同步合成并创建节点；窗口关闭没有专门处理；暂停中的“重新派遣”会直接丢弃本局。菜单按 1440×900 设计，尚无多尺寸实测证据。

原浏览器项目与其他游戏保持独立。用户已授权本任务提交、合并、推送和 Windows SMB 交付；Godot 项目不接入 Pages。

## 实施记录

后续在此记录已完成项的代码位置、命令和实机证据；只有证据足够时才勾选上面的交付标准。


### 2026-09-11 · 玩家服务与续玩

已实现设置菜单、主要键盘重绑、手柄菜单/战斗映射、动态按键提示、操作指南、退出/重开确认、失焦及断线暂停。音效使用十二路固定声音池与预缓存；增加本地环境声。Mac 原生渲染完成设置、指南、战斗和可继续救援主菜单的截图检查，支持 960×600、1280×800、1440×900、1920×1080 窗口，按固定画幅留黑边。实体手柄尚未实测。

存档升级至 schema 2：永久核心与安全点同一次写入，支持舱室入口、剧情、强化、路线、商店和维修状态恢复。两个分支在每个安全点自动写盘、销毁实例再恢复到胜利，913 项检查通过。旧版存档迁移、未来版本保护、非法数据与备份恢复均有针对性覆盖。战斗中退出会回到本舱入口，界面明确说明；死亡与胜利清除本局记录。

最近完整回归通过：导入、基础场景 41、武器 123、战役 1672、中文剧情 1011、玩家服务 48、安全点 913。之后新增的多窗口/续玩主菜单原生检查为 59 项通过。后续修改后仍需重跑对应检查；以上不代表完整 production 目标已完成。

下一阶段：Mac 发行包独立冷启动；角色动作和场景/战斗读图；首领与新手节奏；真实渲染压力与完整游玩验收。导出模板已从 Godot 官方 4.7.2 发布下载，当前仅做本地发行包，不上传。


### Mac 发行候选链路

`export_presets.cfg` 与 `export_macos.py` 已建立。发现并修正“仅主场景”漏掉脚本预加载依赖的问题，改为全部运行资源导出并排除开发目录。官方 4.7.2 macOS 模板生成的 universal 应用通过 `codesign --verify --deep --strict`；冷启动离开项目源码目录进行，确保不能从源文件补全漏打包的资源。导出脚本还在验证完整发行战役。

候选包：`builds/深渊协议-0.9.0-macOS.zip`。当前为 ad-hoc 本地签名，未 Apple 公证或上传。`builds/macos-0.9.0/` 是供本机验证的解压件；源码后续变化后需要重新构建，不能把旧包当作当前完成证据。

已找到原作八方向分部位角色图集，可用于下一阶段动作移植，避免重新生成素材。源图在 `output/imagegen/abyss-protocol-runtime/rig{East,SouthEast,South,SouthWest,West,NorthWest,North,NorthEast}.webp`；骨骼标定与实际步态算法位于原 `abyss-protocol/index.html` 的 `SOUTH_RIG_PARTS`、`drawSouthRigPart`、`drawSideRig`、`drawNorthRig`、`drawRearDiagonalRig`、`drawFrontDiagonalRig` 及 `rigGroundLevel`。移植时复制资源进 Godot 项目，不能运行时依赖 `output/` 或旧 HTML。相关历史决策见 2026-09-06/11 的 articulated player、gait、two-hand poses Notes。此处为历史发现；关节移植已在下一节完成。


### 角色动作与武器美术

已移植原作八方向独立关节素材与标定到本地资源，GDScript 实现站立/移动混合、脚步接触和抬脚、碰撞后距离驱动步态、撞墙停步、冲刺期间锁定步态、挥砍动作及枪/弓双手握持。枪械和弓已替换为校准过握把/护木/枪口/弓弦的绘制图集。姿态预览 `builds/qa/android-poses.png` 在 Mac 原生渲染检查通过；动画检查覆盖八朝向与八移动方向、正常游戏的真实调用点以及各蓄力/后坐力阶段，6412 项通过。

下一步仍需实际战斗视觉验收，尤其是弹体从武器发射的位置、首领/敌人动作与危险辨识、场景道具层次、教程与难度节奏、真实渲染下的帧时间与长时间资源回收。最新源码完成后必须重新导出；0.9.0 的早先构建不能证明这些新增项。


### 2026-09-11 · 工业科幻改版与本地验收

该阶段源码为 0.9.1。本轮统一原创轨道站主视觉、Noto Serif CJK 中文标题、暖白与救援橙 UI、低噪声甲板、检修道、舱壁与设备材质。设置分组、指南加入攻击形状示意，首舱加入可关闭的三步提示。首舱敌人从五个减至三个并延迟首次攻击；这只增加学习容错，不修改后续守卫机制。

武器弹体在统一地面碰撞坐标外增加绘制偏移，八方向的枪口/箭头与可见发射点连接。掩体会在遮挡玩家时淡化。首领镜头同时跟随双方并按距离缩放，保留 HUD 下的可见空间；不会改变物理伤害、射程或碰撞。小屏 Mac 启动窗口根据可用桌面区域缩放、居中，原生检查通过。

增加九种离线合成音效，使用已有十二路池播放。原生压力场景采集九百个帧时间样本，实际平台 Apple M4 Pro / Godot 4.7.2 / OpenGL Compatibility / 1440×900：中位 16.67 ms、95 分位 22.78 ms、99 分位 32.87 ms，峰值 293 节点与 33 枚弹体。四十次重开后节点数回到基线，孤立节点为零。该数据说明本机压力范围，不声称所有帧锁定 60 FPS，也不代表 Intel Mac。

普通规则输入机器人使用速射枪完成十二舱室和全部六段剧情；没有修改玩家血量、伤害、冷却或删除敌人。它使用四倍仿真速度与自动路线选择，是可达性和节奏基线，不是新玩家研究。原生 UI 经系统鼠标/键盘操作检查，已进入开场、战斗与失败界面；完整的人类通关和长时间体验反馈尚缺。

设计审查与落实项见 `docs/design-review.md`。最新原生玩家服务为 60 项通过；presentation 检查覆盖八方向发射点、遮挡、镜头与首领阶段。该阶段构建记录位于 `builds/深渊协议-0.9.1-macOS.json`；最新候选包见下文 0.9.2 记录。

## 尚未完成的验收

- 实体手柄完整游玩；现有覆盖是实际输入事件分发、映射、菜单焦点和模拟输入。
- 真实新玩家完整通关与长时间体验反馈；现有普通规则输入机器人不能替代该项。
- Intel Mac 独立实机验证；交付包包含 x86_64，但本机只有 Apple Silicon。
- 公开发行所需 Apple Developer 身份签名、公证与分发；当前仅本地 ad-hoc 签名候选包，没有上传。

因此 production 总目标仍在进行，不把本地候选包等同于已完成全部商业发行验收。


### 历史本地构建 · 0.9.1

`python3 export_macos.py` 完整执行：导入，smoke 41、weapons 123、campaign 1672、story 940、production 59、checkpoints 913、animation 6412、presentation 376，均零失败。原生 production 为 60 项通过（另含真实桌面适配），原生 presentation 为 376 项通过。发行模板独立自检 28 项通过，压缩包校验和 ad-hoc 签名校验通过。

候选包：`builds/深渊协议-0.9.1-macOS.zip`。

SHA-256：`18f160807f3a9fdd6a094cfb059cc2b934c440d61a222985ef71dd1152b09e84`。

当时的源代码、资源与候选包一致；没有提交、推送、上传或部署。浏览器原作、Pages 与 westworld 的已有更改未参与本次验证。

独立解压后的 `.app` 另以原生 OpenGL 窗口运行发行自检，28 项零失败；随后通过正常启动路径打开 0.9.1 主菜单，确认中文、字体、轨道站插画、版本号和已有永久进度正常显示。应用已保持打开供试玩。


### 历史本地候选 · 0.9.2

鼠标持续射击、长蓄力和松开射击保持手动瞄准，闲置辅助、摇杆和鼠标模式分别在 HUD 显示。关闭辅助后不再让移动方向抢走鼠标瞄准；战斗准星与菜单指针切换，退出前清理自定义准星，修复原生 OpenGL 纹理泄漏。新增六项真实输入检查，修改前复现四项失败，修改后全部通过。

新增可随时切换的高对比文字，设置跨启动保存，已有菜单与后续界面同步生效。原生 production 63 项、aiming 6 项通过，日志无 `ERROR:`，高对比指南截图已检查。

`python3 export_macos.py` 完整执行：资源导入，smoke 41、weapons 123、campaign 1672、story 943、production 62、checkpoints 913、animation 6412、presentation 376、aiming 6，九套全部零失败。发行自检 28 项、独立存档写入 11 项和第二进程读取 19 项均通过，压缩包完整性及 ad-hoc 签名检查通过。

存档验收使用显式带标记的临时目录，首次进程从空进度开始，选择轨道炮、修改音量/震动/按键，到第三舱购买武器调校后退出。第二进程冷启动，恢复设置与实际 InputMap、武器和商店安全点，逐项比较核心、次数、废料、伤害、耐久、购买和路线，确认没有重复购买，再推进到救援结局并从磁盘验证胜利和安全点清除。该检查用于验证打包与持久化，不用于难度验收。

解压后的 `.app` 在源码目录之外，另外以真实 OpenGL 窗口完成相同的 28 + 11 + 19 项检查；三次独立进程全部正常退出，日志无脚本、渲染或资源泄漏错误。测试未读取或覆盖正常玩家存档。

候选包：`builds/深渊协议-0.9.2-macOS.zip`。解压件：`builds/macos-0.9.2/深渊协议.app`。

SHA-256：`27d459270da75ce3fc91eae66a0ec2b4685c81b290eccad93e1ac3e15e1f3300`。

构建记录：`builds/深渊协议-0.9.2-macOS.json`；原生验收记录：`builds/qa/release-0.9.2-native.json`。实际平台 Apple M4 Pro / Godot 4.7.2 / OpenGL Compatibility，二进制包含 arm64 与 x86_64。当时的运行代码与资源和本包一致。本地发行交付项已完成；实体手柄、Intel 硬件、完整新玩家体验和公开发行公证仍按前述边界保留，不能据自动检查勾选全部 production 目标。


### 历史本地候选 · 0.9.3

本轮完成两项实际体验修复：近战伤害范围超过画出的预警，以及 Retina 默认窗口按物理像素计算导致窗口与文字只有预期的一半大小。

近战预警现在画出与判定一致的完整扇区，保持原有伤害和时机。首领扇形与环形攻击的箭头和实际发射共用方向列表；地面锁定用三环提示，随后产生的伤害区域仍保留完整 0.95 秒预警。血条显示正在准备的攻击名称，指南同步解释各类提示。presentation 通过 523 项检查，包含八方向近战边界、实际伤害、12/16 发弹道、三处地面延迟和首领标签，并检查了对应原生截图。

本机屏幕实测 scale 2.0、DPI 220。默认窗口现在约为 2550×1594 物理像素（1275×797 桌面点），根据当前屏幕可用范围居中；此前 1280×800 像素只有 640×400 点。原生 production 64 项通过，含桌面点数下限及屏幕边界检查，默认菜单截图确认中文尺寸与布局。现有多窗口布局、中文字体、对比度、关闭震动/闪光、危险几何和首领状态检查共同满足当前 Mac 可读性工程验收；真人首次游玩体验仍单列待验收。

两组原生性能证据均通过 44 项检查，各含 900 帧与 40 次重开，零孤立节点增长。1440×900：p50 14.517 ms、p95 22.707 ms、p99 23.818 ms；Retina 默认 2550×1594：p50 16.727 ms、p95 23.143 ms、p99 25.478 ms，最大 32.883 ms，峰值 294 节点 / 33 弹体。记录分别在 `builds/qa/performance-0.9.3-1440.json` 与 `builds/qa/performance-0.9.3-retina.json`。这些是本机压力场景数据，不声称全部帧锁定 60 FPS。

最终 `python3 export_macos.py` 已完整通过资源导入和九套源码检查：smoke 41、weapons 123、campaign 1672、story 943、production 62、checkpoints 913、animation 6412、presentation 523、aiming 6，全部零失败。独立发行包自检 28 项、首次存档写入 11 项、第二进程冷启动读取与继续救援 19 项通过，压缩包和 ad-hoc 签名验证通过。

候选包：`builds/深渊协议-0.9.3-macOS.zip`。解压件：`builds/macos-0.9.3/深渊协议.app`。

SHA-256：`53340781a8383917ed6994b8e0813cf4297c03a6c4d5493d07ae848b19babf30`。

最终解压件又在源码目录之外，用真实原生窗口完成 28 + 11 + 19 项发行与跨进程存档检查，全部正常退出，完整日志没有 `ERROR:`。记录在 `builds/qa/release-0.9.3-native.json`，其包校验值与上述最终 ZIP 相同。运行资源与本包一致；后续文档更新不会改变打包内容。

目前连接的实体手柄数量实测为 0。键鼠/控制器事件与菜单路径有自动和系统 UI 操作证据；不能据此宣称完成实体手柄验收，也不能把普通规则机器人通关等同于真人首次游玩的难度与节奏反馈。已向用户征求具体试玩反馈。剩余操作体验、真人节奏验收与外部硬件边界仍然开放，production 总目标保持进行中。


### 历史本地候选 · 0.9.4

控制器菜单审查复现了两个遗漏：等待键盘新绑定时，手柄右键和菜单键都无法取消。已修复输入分发顺序，取消后保留原绑定并停留在设置；录入期间其他手柄导航/确认不会误触菜单。焦点留在等待新键的操作上，中文提示说明如何取消。

新增控制器菜单流程通过 Godot 的方向焦点图规划路径，然后真实分发每个十字键事件并验证焦点，使用确认键激活按钮，不通过直接抓取焦点或发射按钮回调绕过输入。覆盖十八项武器目录、最后一项装备选择、音量滑杆、绑定取消、防误触、滚动指南、永久升级购买、商店购买、维修、强化/路线选择、六段剧情、结局及重新开局。初次 203 项检查复现两项取消失败；修复及补充断言后 headless production 211 项、原生 production 213 项全部通过。已查看 `builds/qa/controller-key-capture.png`。

该流程禁用玩家存档，给测试货币并直接清理战斗，用来验证菜单导航，不作为正常难度通关或实体手柄实测的证据。原生运行日志没有脚本、渲染或退出错误。

最终 `python3 export_macos.py` 全部完成：资源导入，smoke 41、weapons 123、campaign 1672、story 943、production 211、checkpoints 913、animation 6412、presentation 523、aiming 6，九套均零失败。独立发行程序自检 28 项、存档写入 11 项与第二进程冷启动读取 19 项通过，压缩包完整性与本地签名通过。

候选包：`builds/深渊协议-0.9.4-macOS.zip`。解压件：`builds/macos-0.9.4/深渊协议.app`。

SHA-256：`2a43dde647180314a6c3675437ea7ada7eb24247274607c4d36c4b5dd1d07d7d`。

最终解压的 `.app` 又在项目目录之外完成真实窗口下的 28 + 11 + 19 项发行/续档检查，全部零失败、正常退出且无 `ERROR:`。`builds/qa/release-0.9.4-native.json` 的归档校验值与最终包一致。

本轮不改动战斗绘制、敌人或动作热路径，保留 0.9.3 的 1440×900 和 Retina 2550×1594 性能证据，未无故重复压力测试。`westworld/`、旧浏览器游戏、Pages 和已有 `output/` 更改仍未参与本轮修改或验证；没有提交、推送、部署或公证。

### 剩余验收审计

- 核心流程、持久化、设置、中文与可读性、视听升级、当前 Mac 稳定性、本地发行和自动回归，均有上文代码、原生图像与执行记录支持。
- 操作体验的自动菜单覆盖已补齐；实体控制器仍未连接，之前实测数量为 0，没有可代替实物映射、连接、漂移和人体操作的证据。
- 难度与节奏仍缺真人首次游玩的完整反馈。普通规则输入机器人、加速战役检查和菜单用战斗夹具不能证明新玩家理解和体验。
- 已向用户提出试玩反馈问题，尚未收到答复。当前已知可自主修复的问题均已处理；下一步由实物控制器与真人反馈提供新的验收证据或具体问题。保留 production 总目标，不以增加无关功能代替这些验收。
- Intel 实机与公开 Apple 签名/公证仍是交付边界；本机只有 Apple Silicon，当前交付是本地候选包，没有擅自进行公开分发。

### 2026-09-11：等待外部验收

0.9.4 完成本地验证后，连续三轮复核均未收到真人试玩反馈，原生引擎每次检测到的实体手柄数量均为 0。当前没有新的已知代码问题或尚在运行的验证任务可以继续处理。总目标标记为受阻而非完成；收到试玩反馈或连接实体手柄后，继续剩余的操作体验及难度与节奏验收。现有候选包与验证记录保留。

### 2026-09-12：恢复后的复核与试玩交付

恢复目标后重新核对工作树：运行源码、资源与测试均没有晚于已验证 0.9.4 归档的修改，ZIP 校验值仍与构建及原生检查记录一致。未重复执行已经通过且范围未变的回归套件。重新通过原生引擎检测，连接的实体手柄数量仍为 0。

通过正常应用启动路径打开并显示 `builds/macos-0.9.4/深渊协议.app`，确认实际主菜单为 0.9.4，中文、插画和窗口尺寸正常，并显示第 7 舱续玩入口。应用保留打开供用户继续试玩；没有操作继续、重开或更改已有进度。此项提供当前候选版普通启动与既有续玩入口的实机证据，真人体验反馈及实体手柄验收仍未完成。

恢复后的连续三轮复核仍缺相同的真人体验反馈与实体控制器验收证据，源码没有新增修改或待处理故障。总目标再次标记为受阻，保留完整验收范围；收到新的体验反馈、设备实测结果或明确的验收范围调整后继续。后续复核没有重启或操作用户的游戏会话。

### 2026-09-12 · 最新本地候选 0.9.5

用户要求再次尝试后，重新审查实际输入绑定发现此前自动检查的盲点：新建 Godot 手柄事件默认绑定设备 0。编号 1、3 的输入虽能切换 HUD 提示，却无法移动、攻击或操作菜单。系统为重连手柄分配其他编号时可能触发该问题。此前“没有新的已知问题”的判断不覆盖这个遗漏。

游戏按键、菜单按键和摇杆轴现统一用 `device = -1` 接受系统分配的任意编号。保持单人玩法及原有操作方式。新增检查通过 `Input.parse_input_event` 分发编号 1、3 的真实事件，验证角色位移、右摇杆瞄准、五项战斗动作、暂停、焦点确认恢复与按键释放；完整控制器菜单流程改用编号 3。原有键鼠与编号 0 检查保留。修改前 231 项检查中有 18 项失败；修改后 headless 231 项、原生 OpenGL 233 项全部通过。原生按键录入截图确认中文提示与焦点正常。

发行程序自检的移动改用编号 3 的摇杆事件，并增加暂停与确认恢复，避免通过 `Input.action_press` 绕开需要验证的设备映射。

最终 `python3 export_macos.py` 完成资源导入及九套源码检查：smoke 41、weapons 123、campaign 1672、story 943、production 231、checkpoints 913、animation 6412、presentation 523、aiming 6，全部零失败。独立发行自检 30 项、首次存档写入 11 项与第二进程读取/续玩 19 项通过；压缩包完整性和 ad-hoc 签名通过。

候选包：`builds/深渊协议-0.9.5-macOS.zip`。解压件：`builds/macos-0.9.5/深渊协议.app`。

SHA-256：`de7fd81d062e1737a37b96cd95e6159cfdb30c00a21829b09ef641ded7a3ac90`。

最终解压件又在源码目录之外，以原生 OpenGL 窗口完成 30 + 11 + 19 项验证，均正常退出且无 `ERROR:`。构建记录 `builds/深渊协议-0.9.5-macOS.json` 与原生记录 `builds/qa/release-0.9.5-native.json` 的校验值一致；源码、资源和导出配置没有晚于此归档的修改。实际平台为 Apple M4 Pro，二进制同时含 arm64 与 x86_64。没有操作或终止既有游戏进程，没有读写正常玩家存档。

本次仅修改输入绑定和验证入口，继续沿用前述视听设计与 0.9.3 的两组原生性能证据，未重复无关压力测试。没有提交、推送、公开分发或公证；其他游戏的已有修改保持不变。

实体控制器连接/映射/漂移与真人首次游玩的难度、节奏反馈仍缺证据，前述两个必选验收项继续保持未完成。模拟非零编号事件证明本次缺陷已修复，不能代替实体手柄或真人体验验收。

### 2026-09-12 · 0.9.5 交付后的受阻复核

完成非零手柄编号修复与新包交付后，连续三轮复核仍未收到新的真人试玩结果。每轮通过 macOS GameController 框架在无窗口进程中检测，连接的实体控制器数量均为 0。源码与测试没有晚于 0.9.5 归档的修改，包校验值仍与构建、原生验证记录一致；已知验证进程均已完成，没有可继续等待的验证任务。

剩余阻碍仍是实体手柄全流程与真人难度/节奏验收。目标标记为受阻，保留上述未完成项；新的试玩反馈或设备实测证据可提供下一步工作。未重复已通过的回归，没有操作用户游戏会话。

### 2026-09-12 · 0.9.6 增加 Windows 发行

用户将 Windows 支持加入目标后，新增 Windows Desktop x86_64 预设、官方模板准备脚本、Windows 导出脚本、中文玩家说明和独立 PowerShell 验证入口。Mac 保留，两个平台版本号同步为 0.9.6。游戏普通运行内容未改变；发行自检在已验证的临时存档目录内增加原生截图，用于核对跨平台字体、菜单与续玩界面。

`prepare_windows_templates.py` 使用固定 SHA-256 校验 Godot 官方 4.7.2 完整模板包，再提取 Windows 模板并记录文件校验值。`export_windows.py` 校验版本、运行九套检查、导出原生 PE32+ x86_64 程序，确认图标和中文产品/版本资源，排除开发文件，打包中文说明与完整引擎/库/字体许可。Windows 包无 Python/Godot/Wine 运行依赖。Mac ZIP 同时增加完整第三方许可文件，放在签名应用旁，不改动签名封装。

Windows 验证通过包内 `Verify-Windows.ps1` 执行，兼容 Windows 原生和明确标记的 Wine 测试；后者使用独立且带标记的 prefix，从未使用默认用户 prefix。每次游戏的存档与报告位于单独临时目录，存档路径包含空格；导出测试目录及程序路径包含中文和空格。脚本检查退出码、引擎错误和各阶段总结，并将已测 EXE/PCK 的 SHA-256 写入报告。

本轮 `python3 export_windows.py --wine ... --wine-prefix ... --rendered` 已完成资源导入、smoke 41、weapons 123、campaign 1672、story 943、production 231、checkpoints 913、animation 6412、presentation 523、aiming 6，九套零失败。随后 Windows 发行程序在 Wine 11.0 / Rosetta / Apple M4 Pro 下完成：headless 战役 30、存档写入 11、第二进程读取 19；OpenGL 窗口战役 30、写入 13、读取 22，全部零失败。后两项增加了五次保存截图检查；这不增加真人易用性证据。

Windows 原生 exe 的渲染截图已查看：标题、中文插画/文字、第三舱商店和购买后的续档信息正常。图像位于 `builds/qa/abyss-windows-791719a959e847bbb8baafae7d8e9e07/storage with spaces/`。Wine 日志存在 SDR 白电平查询警告，无引擎 `ERROR:`；此平台不能证明 Windows 显卡、DPI、音频输出或实体控制器行为。

Windows 候选包：`builds/深渊协议-0.9.6-Windows-x64.zip`（约 73.8 MiB）。

SHA-256：`4e63a8b529f2ee3b993b211d0491766dfc017de8f495f0c04acb26328d50c101`。

构建/运行记录：`builds/深渊协议-0.9.6-Windows-x64.json`；包含各文件校验值、官方模板来源以及 headless/渲染两组 PowerShell 报告路径。ZIP 完整性、所有打包文件与报告校验值均已复核。

从最终 Windows ZIP 解压后，另测试了 `ANGLE-compatibility.cmd` 对应的 `opengl3_angle` 渲染参数。本机 Wine 无法初始化 EGL display，未完成该运行，已停止这一个失败的隔离测试进程。记录见 `builds/qa/windows-0.9.6-angle-limitation.json` 与其引用日志。默认 OpenGL 路径通过；Windows 真实 Direct3D 11 兼容路径仍待实机验收，不能把 Wine 失败推断为所有 Windows 的结果，也不能宣称该路径已通过。

Mac 候选包：`builds/深渊协议-0.9.6-macOS.zip`。

SHA-256：`68446c1aded5e5f7c0bfab65033ba121f4bfb924e5d20b53d3c5d526811427cc`。

Mac 导出复用本轮刚通过的九套源码检查；独立发行 headless 30 + 11 + 19 通过，包签名/完整性通过。最终解压件又在源码之外通过原生 OpenGL 30 + 13 + 22 项，均零失败、正常退出且无 `ERROR:`；原生记录 `builds/qa/release-0.9.6-native.json` 的校验值与最终 ZIP 一致。保留 arm64/x86_64 通用二进制，本机实测 Apple Silicon。

两个候选包都没有晚于其归档的运行源码、资源或测试修改；没有提交、推送、上传或公证。已有视听设计和性能热路径保持不变。Windows 工程交付已有证据，Windows 实机验收、实体手柄和真人难度/节奏反馈继续保持未完成，production 总目标仍未达到。

### 2026-09-12 · 0.9.6 交付后的受阻复核

连续三轮复核未出现 Windows 原生验证报告或真人试玩反馈，macOS GameController 每轮均检测到 0 个实体手柄。本机虽有 QEMU 可执行程序，但未找到可用于本轮验收的 Windows 系统或正在运行的 Windows 虚拟机；已询问用户可提供的 Windows 验收条件，尚未收到答复。

0.9.6 的两个归档校验值仍与各自验证记录一致，运行源码和测试没有新增修改，已启动的验证任务均已结束。剩余条件需要 Windows 实机/设备与真人反馈提供新证据，目标标记为受阻，未标记完成。所有候选包、成功验证与 ANGLE/Wine 失败记录保留。

### 2026-09-12 · Windows SMB 交付核验

用户授权 Windows 游戏部署到 `smb://Rog-xx/Games`。已确认 `/Volumes/Games` 是该主机与共享的可写 SMB 挂载。检查时共享中已有 0.9.6 ZIP 和完整解压目录；全部内容与本地验证过的发行包一致，因此沿用现有文件，没有重复复制或覆盖。

启动路径：`\\Rog-xx\Games\深渊协议-0.9.6-Windows-x64\深渊协议 0.9.6\AbyssProtocol.exe`。

共享根目录中的 `深渊协议-0.9.6-Windows-x64.zip` 为 77,427,933 字节，SHA-256 为 `4e63a8b529f2ee3b993b211d0491766dfc017de8f495f0c04acb26328d50c101`。从 SMB 重新读取 ZIP 和全部七个解压文件，逐一核对构建记录、本地文件与归档内容，均一致。核验报告保存于 `builds/deployments/windows-0.9.6-rog-xx.json`。

本次仅核验交付文件，没有通过 SMB 执行程序，没有修改玩家存档。已有 Wine 运行证据保持有效；Rog-xx 原生 Windows、实体手柄与真人试玩验收仍待完成。


### 2026-09-12 · 0.10.0 双人手柄同屏与 1440p

新增双人手柄配对页、独立操作/武器/耐久/能量、同屏共享镜头、双人 HUD、倒地修复、断线换编号恢复和两人同一事务的安全点存档。共享菜单只接受当前主控席位，任一玩家可暂停；两人都离线时一号席恢复主控并可直接重开双人救援。配对页也支持通过手柄取消和鼠标返回。

窗口从固定比例留边改为扩展画幅，2560×1440 真实帧缓冲为完整 16:9。原生截图确认两人分处舱室两端时均在画面内，双人耐久分别位于左右上角。Mac 全屏实际输出 2992×1876，与系统报告的安全区域相符；显示器物理分辨率为 2992×1934，差异来自系统刘海安全区。未修改系统显示设置。

实现过程中修复了有续档时主菜单高度溢出，以及双人全灭后主控席位仍指向二号导致一号无法重开的缺陷。增加了对应回归检查；新增双人场景套件使用 InputEvent 设备 1、3、7，覆盖席位隔离、独立瞄准/五项技能、蓄力暂停、实际弹体/地面/近战伤害、修复、共享奖励、换编号重连、磁盘续档与完整剧情流程。

Mac 双人 2560×1440 压力场景已通过 44 项检查：900 帧，p50 16.797 ms、p95 21.204 ms、p99 21.9 ms，最大 30.09 ms，峰值 309 节点/37 弹体；40 次重开，零孤立节点。报告 `builds/qa/performance-coop.json`。完整普通数值 Input-action bot 以两把速射枪通关十二舱、六段剧情，记录 `builds/qa/playthrough-coop.json`；这是自动操作证据，不是真人体验研究。

发行程序自检增加双设备配对/独立移动/双人战役，并在同一个带标记的临时目录中用两个独立 profile 分别验证单人和双人的跨进程续玩。正式玩家存档不参与验证。构建资源的 SHA-256 清单记录于 `builds/qa/source-0.10.0.json`。本轮完整构建和共享交付结果在下方追加；Windows 实机与两只实体手柄验收仍未完成。


0.10.0 最终源码十套检查：smoke 41、weapons 123、campaign 1672、story 945、production 233、checkpoints 913、animation 6412、presentation 523、aiming 6、coop 81，全部零失败。最终源码原生双人套件 91 项通过，包含三张 2560×1440 截图、系统安全区全屏与退出全屏。相关日志 `/tmp/abyss-0100-windows-export.log`、`/tmp/abyss-0100-native-coop.log`。

Windows 0.10.0 最终原生 PE 在 Wine 11.0 下完成 headless 34 + 13 + 24 项、窗口 OpenGL 34 + 16 + 28 项，全部零失败。报告目录分别为 `builds/qa/abyss-windows-5aefca081dd24bff81d0d2ea60a7baa9/` 和 `builds/qa/abyss-windows-fa021a724de547468435ebae57e889df/`。窗口存档阶段同时保存了双人购买与续档截图。成功日志仅有 Wine SDR 白电平警告，无引擎错误；没有把兼容层证据当作 Windows 实机验收。沿用官方引擎的 ANGLE/Wine 初始化限制，未重复声明 ANGLE 通过。

Windows ZIP SHA-256：`e6f293196bebb203356aa1591bee7a503f7c1977fb5edeea503bdde005082b58`。
Mac ZIP SHA-256：`8b7139f9cad47d8d6606f0b9e1873b3e287a1fbabc3b6735c19d8d443ba4e24f`。
Mac 包为 arm64/x86_64 通用、ad-hoc 签名，导出包签名与独立 headless 34 + 13 + 24 项通过。

Windows 已交付至 `\\Rog-xx\Games\深渊协议-0.10.0-Windows-x64\深渊协议 0.10.0\AbyssProtocol.exe`；共享根目录另有 77,443,196 字节 ZIP。完整 ZIP 与七个文件从 SMB 重读并通过 SHA-256 校验，旧版 0.9.6 保留。共享端拒绝文件夹重命名，因此使用独占逐文件复制、EXE 最后写入的方式；临时数据文件已清理，服务器仍拒绝删除两个空的隐藏暂存目录。交付记录 `builds/deployments/windows-0.10.0-rog-xx.json` 包含精确路径和校验值。没有修改系统配置、正式玩家存档、其他游戏，未提交或公开发布。

最终 Mac 解压应用又通过原生窗口 34 + 16 + 28 项独立发行检查；报告 `builds/qa/release-0.10.0-native.json`，对应 ZIP 校验值与上述最终归档一致。两个系统的打包程序都已覆盖单人和双人续档。Windows 硬件、两只实体手柄和真人难度反馈仍未完成，production 总目标保持未完成。


### 2026-09-12 · 0.10.1 合作菜单收尾

检查最终发行截图发现补给站只显示一号耐久，双人时维修可用状态不够明确。现已在补给、路线和维修界面显示两人的实际耐久，商店卡片明确标注全队效果，强化页说明两人各自恢复耐久与能量。游戏内操作指南加入双人配对、救援、共享资源和断线恢复说明。

结算页增加返回船坞，双人结算可重新选择双人装备；重新派遣保留合作模式及所选装备。一号武器选择同步到下次开局的默认装备，避免武器库显示与实际出发不一致。新增检查验证第二人的数值展示与布局、以及手柄从合作结算返回模式选择。

源码完整十套再次通过，production 235 项；双人套件在完整批次通过 83 项，随后加入结算返回检查后单独通过 85 项。运行资源清单 `builds/qa/source-0.10.1.json`。本次修改菜单与开局装备选择，没有改变战斗、镜头或声音热路径，沿用 0.10.0 的 QHD 双人压力与正常数值自动通关证据。


最终 0.10.1 原生双人套件 95 项通过。Windows 发行程序在 Wine 下通过 headless 34 + 13 + 24 项、OpenGL 窗口 34 + 16 + 28 项，报告为 `builds/qa/abyss-windows-63ac7b9e147e4620b507c890ea05cf03/report.json` 与 `builds/qa/abyss-windows-e7d491f949ea4c0393bb33a35e8805a2/report.json`。窗口截图已确认双人耐久、全队补给说明与已购买状态一致。

Windows 最终 ZIP SHA-256：`f0ab8ffb438a560176ef09c255823aed4ec77fbecd95b927a2564fa88a7b469b`。
Mac 最终 ZIP SHA-256：`6f9bad2a39a7006403830affc3d06ab3b1376ffbd68779bab498c8097629b0d6`。
Mac 复用刚完成的源码回归，导出/签名和独立 headless 34 + 13 + 24 项均通过；原生窗口跨进程续档记录为 `builds/qa/release-0.10.1-native.json`。

最终 Windows 交付：`\\Rog-xx\Games\深渊协议-0.10.1-Windows-x64\深渊协议 0.10.1\AbyssProtocol.exe`。共享根目录的 ZIP 为 77,444,090 字节，七个文件和 ZIP 均重读校验通过。因已知共享端不允许目录重命名，直接采用新版本目录和 EXE 最后写入方式，未产生新的暂存目录。部署报告 `builds/deployments/windows-0.10.1-rog-xx.json`。旧版本和正式玩家存档保留，未提交、推送或公开发布。Windows 实机、实体双手柄和真人体验验收继续保持未完成。

### 2026-09-12 · Windows 实机验收交接

续轮核对：67 项运行资源与 0.10.1 清单相同，SMB 上七个游戏文件仍匹配部署记录；未发现新的 Windows 原生运行报告。本机 GameController 当前检测到 0 个实体手柄。上一轮完成了实现、构建和交付，属于实际进展；本次不重复已通过的整套游戏测试。

在共享盘版本外层添加 `一键Windows验收.cmd`、`Run-Windows-Acceptance.ps1` 与 `Windows验收说明.txt`，源文件分别位于 `tools/run_windows_acceptance.cmd`、`tools/run_windows_acceptance.ps1` 和 `docs/windows-acceptance.txt`。游戏 ZIP 和内层七个文件保持原样。

入口核对 EXE、PCK 和现有验证脚本的固定 SHA-256，要求在 Windows 执行，在本地 TEMP 下使用已有隔离自检；成功后验证报告平台、渲染标记、文件哈希和三个阶段的结果，仅复制本轮日志、七张截图和报告到同版本外层的 `验收记录/<运行编号>/`。不复制 profile 或 settings。复制校验成功后才写 `handoff.json`，以免把半份报告当成完成。

已通过 PowerShell AST 语法检查、非 Windows 启动保护以及部署字节/哈希校验。PowerShell 文件按 UTF-8 BOM 和 CRLF 交付，以兼容 Windows PowerShell 5.1 的中文路径；入口只对当前进程传入执行策略参数，没有修改机器执行策略。此处没有执行 Windows 程序，也没有声称入口已在真实 Windows 上验证。交付记录 `builds/deployments/acceptance-0.10.1-rog-xx.json`。

说明文件列出两只实体手柄的独立操作、断线重连、修复、共享镜头及 2560×1440 全屏验收步骤，并给出 `试玩反馈.txt` 的记录方式。仍需 Windows 实际运行与实体设备/真人反馈，目标未标记完成。

### 2026-09-12 · 实机验收受阻复核

交付实现、准备验收交接入口、本轮检查回传结果三个连续目标轮次，Windows 实机与实体双手柄/真人体验证据一直缺失。本轮实际检查时间为 05:18 UTC：共享盘 `验收记录/` 为空、没有 `试玩反馈.txt`，GameController 检测为 0 个实体手柄，当前工具没有 Windows 远程执行入口。源码运行资源与 0.10.1 清单一致，没有新的代码缺陷或运行报告需要处理。上一轮创建并交付验收入口属于进展；本轮的重复核对未产生新的验收证据。

目标标记为受阻而非完成。需要在 Rog 上运行一键验收入口并提供实体双手柄及 1440p 全屏试玩结果后再继续；报告尚未回传不等于已确认远端测试没有运行。没有可追踪的远端运行句柄，因此不把当前状态称为已验证的等待，也不重复启动本机的已通过套件。

### 2026-09-12 · 修复验收工具权限错误处理

用户反馈 Windows 游戏测试窗口已打开，随后终端提示对 `abyss-windows-…` 的访问被拒绝。完整路径尚未提供；现有信息倾向报告回传阶段，但不能据此认定 Windows 测试已全部通过。检查共享盘时尚无完整回传报告。

验收工具修订 2 增加本地目录创建、写入和重命名预检，TEMP 不可用时尝试当前用户 LocalAppData；成功测试的报告、日志和截图先保留到本地独立诊断目录，再尝试共享盘回传。回传失败单独提示，不再将其混同于游戏测试失败；终端显示具体阶段、本地目录和回传目标，便于继续诊断。无需管理员权限，不修改 ACL 或正式玩家存档。

原脚本在隔离只读目录中复现权限异常；修改后 6 项 PowerShell 文件系统回归通过，包含失败回传保留本地证据、缺失证据不生成凭据、已有远端报告不覆盖。Windows 编码版本的语法检查与非 Windows 保护通过，修订 2 已回传到原 SMB 入口并读回校验。详见 `builds/qa/acceptance-r2/` 与更新后的 `builds/deployments/acceptance-0.10.1-rog-xx.json`。

本轮仅修改外层验收工具、针对性测试和说明；67 项运行资源清单、EXE/PCK 和原始游戏验证脚本均未改变，因此没有重复完整游戏回归或重建发行包。目标 Windows 的权限错误是否已解除、自动验收是否通过，仍需新运行结果确认；实体双手柄和真人体验验收保持未完成。

修订 2 交付轮及随后两轮均仍缺目标机完整结果。交付轮有实际修复进展；随后两轮仅复核，未取得改变下一步行动的新证据。最后核对为 2026-09-12 05:28 UTC：共享盘修订 2 哈希一致，`验收记录/` 为空，无试玩反馈，本机实体手柄为 0；当前工具及可操作应用未提供 Windows 远程执行入口，没有已确认存活的远端测试句柄。目标再次标记受阻，需 Rog 重试结果、完整错误路径或实体设备试玩反馈才能继续。缺少回传不代表远端测试一定未运行，不再无据重建或重复本机测试。


### 2026-09-12 · 0.11.0 五区域三十舱候选

新救援包含五区域、三十舱和五场守卫战，十张本地探索/守卫插画使用对应实体碰撞，配合手工房型池、分支奖励预告与完整预警的分批增援。旧十二舱存档继续使用冻结的地图生成器与路线随机序列。祝福卡显示实际前后数值，攻击、耐久、机动和回复设有成长边界；已封顶的效果不再入选。合法奖励允许一至三项有效候选，并覆盖写盘与冷启动续玩。详细参数与局限见 `docs/balance.md`。

最终源码验证 `python3 verify.py` 完成导入与十六套场景，共 21,061 项、零失败：smoke 56、weapons 123、campaign 5228、story 1466、production 265、checkpoints 2129、animation 6412、presentation 523、aiming 6、coop 136、level_design 1897、campaign_expansion 1059、legacy_campaign 99、balance 937、reward_checkpoints 30、map_art 695。Windows 交付与验收工具的 18 项 Python/PowerShell 测试通过。

Mac 原生图册 53 项、双人输入/全屏 146 项、玩家服务/菜单 267 项通过。图册覆盖全部十张地图、共同战斗、祝福、路线与设置，帧缓冲为 2560×1440；全屏往返恢复原窗口尺寸。Mac 全屏输出为系统报告的 2992×1876 安全区域。检查过程中用户曾手动调整测试窗口，导致部分尺寸断言失败；确认来源后撤去临时窗口处理，玩家窗口实现没有因此改变。性能测试保留尺寸变化即判本次采样无效的保护。

在固定 2560×1440、双人、最终插画首领舱加 28 敌人的原生压力场景中，900 帧样本 p50 16.674 ms、p95 17.125 ms、p99 19.619 ms、最大 21.361 ms；40 次重开后无孤立节点增长，44 项检查通过。实际平台为 Apple M4 Pro / macOS / Godot 4.7.2 / OpenGL Compatibility，报告 `builds/qa/performance-coop-expanded.json`。这不是 Windows GPU 或所有硬件的性能保证。

普通规则输入机器人单人和双人都完成三十舱、168 次击杀与六段剧情，分别耗时 343.4 与 340.3 游戏秒，无导航软锁。它使用精准远程攻击及补给路线，首领约六至八秒，结果不能代替新玩家难度验收；`hp_after` 是下一舱入口值，不能当作战斗最低耐久。原始结果与该边界见 `builds/qa/playthrough-summary.md`。不依据这一条精准自动路线贸然增加伤害或缩短预警。

Windows 验收入口已改为从匹配的构建记录与 ZIP 生成，核对版本、大小、全部文件及逐文件哈希，保留已有版本。中文导出文本统一为显式 UTF-8，PowerShell 策略只影响验证进程。实际发行包、共享交付及其校验值见本节后续记录。Windows 实机、两只实体手柄与真人难度反馈继续保持未完成。

0.11.0 最终发行包验证完成。Mac universal 包通过 CRC、ad-hoc 签名、独立 headless 战役 84 项、首次存档写入 15 项和第二进程续档 70 项。Windows x86_64 包验证 PE 图标/版本、完整资源和许可，在隔离 Wine prefix 中通过 headless 84 + 15 + 70 项，以及 OpenGL 窗口 84 + 18 + 74 项。Windows 包的中文双人补给/续档截图已核对；成功日志只有 Wine SDR 白电平警告，无引擎错误。Wine 不代表 Windows 硬件，ANGLE 仍沿用已记录的 Wine 环境限制，未宣称该路径通过。

Windows ZIP：`builds/深渊协议-0.11.0-Windows-x64.zip`，121,510,029 字节。SHA-256：`a8ff8e4199f3cbcd492fbce5c8d128cc333583ebc522359f046cd5256fb7d8f0`。

Mac ZIP：`builds/深渊协议-0.11.0-macOS.zip`。SHA-256：`887d755c4621f37c0120091311d1db406cf32eec4bd2df2b923c2e479a8fa097`。Mac 未公证、Windows 未签发行者证书；二者没有上传公共发行渠道。181 项运行源码/资源清单为 `builds/qa/source-0.11.0.json`，导出后重新核对无差异。

Windows 已部署到 `\\Rog-xx\Games\深渊协议-0.11.0-Windows-x64\深渊协议 0.11.0\AbyssProtocol.exe`，同版本 ZIP 位于共享根目录。部署前核实 SMB 挂载目标，采用新目录、独占写入与 EXE 最后复制；七个游戏文件、四个验收附件和 ZIP 全部从共享盘读回，SHA-256 与本地验证产物一致。记录为 `builds/deployments/windows-0.11.0-rog-xx.json`。旧版本、其他游戏和正式玩家存档保留。

本次源代码交付采用用户授权的 GitHub PR 提交与合并流程，Godot 不在 Pages allowlist 中；Westworld 与 output 的未完成改动不纳入提交。Windows 实机、两只实体手柄、DPI/Alt-Tab 和真人难度反馈仍需新证据，未把候选包和自动检查称为 production 总目标完成。

### 2026-09-12 · Windows 通过反馈与报告补传

用户明确反馈 Windows 测试通过，但回传报告存在权限问题。本轮共享盘仍未收到该报告，完整权限错误路径尚缺，因此记录为用户通过反馈，尚不能列出已审查的原生报告哈希或断言具体 ACL 根因；实体双手柄、DPI/Alt-Tab 与真人难度反馈继续单独验收。

验收工具修订 4 增加“仅回传验收报告.cmd”，复用本版最近一次通过的本地原生报告，不启动游戏。支持指定原报告目录及可写回传目标；保留旧的完整或半截回传目录，重试写入新目录；回传前生成仅含白名单证据的本地 ZIP，网络权限失败时在资源管理器中选中 ZIP 供转交。没有更改系统/共享盘权限，也没有重打游戏包。

23 项 Python/PowerShell 工具测试通过，包含真实只读文件系统失败、旧目录重试保护、ZIP 精确内容与压缩依赖失败、当前版本报告恢复、Wine/旧版本/失败阶段拒绝、生成编码和非 Windows 入口保护。独立审查确认报告补传分支不调用游戏验证器；Mac 的函数及流程测试不作为 Windows PowerShell 5.1 实机结果。

修订 4 的五个外层附件已部署至 `smb://Rog-xx/Games/深渊协议-0.11.0-Windows-x64/`，逐字节及 SHA-256 回读一致；七个游戏文件与原 ZIP 哈希仍匹配 0.11.0，其他版本保留。交付记录为 `builds/deployments/acceptance-0.11.0-r4-rog-xx.json`。运行源码/资源未变，沿用已通过的 21,061 项场景检查及发行包验证；未重复游戏回归。目标 Windows 的新补传入口仍需运行确认。

### 2026-09-12 · 0.11.1 祝福卡按住态对比度

原生输入复现了祝福卡按住时的浅底浅字问题：子 Label 保持浅色，而 Button 切换到通用浅金背景，说明文字最低 1.10:1、标题 1.81:1。修复仅为卡片的 `pressed` 和 `hover_pressed` 设置不透明深色背景，保留元素边框；普通按钮、祝福数值与选择逻辑未变。九类祝福在普通与高对比文字下的静止/焦点、悬停、鼠标按住和设备 3 手柄按住状态均由真实输入事件验证，按住后松开才提交选择。新增对比度段 475 项，修改前 108 项对比度失败，修改后零失败；所测文字最低 5.48:1、按住态最低 6.11:1。

最终 `python3 verify.py` 完成资源导入与十六套场景，共 21,524 项、零失败；其中 production 728 项，其余套件数量同 0.11.0。Mac 原生 production 738 项通过，含上述 475 项对比度检查，未发现脚本错误。鼠标与手柄按住的 1440×900 截图位于 `builds/qa/blessing-mouse-pressed.png`、`blessing-controller-pressed.png`；只生成并查看项目外 1024×640、各约 39 KB 的 JPEG 预览。卡片标题、收益与说明在按住状态清晰，未发现裁切。23 项 Python/PowerShell 工具测试亦通过。

181 项源码/资源哈希相对 0.11.0 仅 `scripts/hud.gd`、`project.godot` 和 `export_presets.cfg` 变化，后两者为版本元数据；清单为 `builds/qa/source-0.11.1.json`。地图与战斗实现未改，沿用前版原生 QHD、双人性能与完整流程的证据；没有将静态样式修复扩展为重复压力采样。

Mac universal 发行包通过归档 CRC、ad-hoc 签名、独立战役 84 项、首次写盘 15 项和第二进程续档 70 项。Windows x86_64 发行包通过 PE 身份/图标/版本与资源检查；在带标记的隔离 Wine prefix 中通过 headless 84 + 15 + 70 项及 OpenGL 窗口 84 + 18 + 74 项。两次导出使用 `--skip-checks` 复用刚完成的全套源码验证，发行包自身的战役与存档检查均完整执行；181 项源码/资源哈希在导出后再次核对一致。Wine 不作为 Windows 硬件或 ANGLE 通过证据。

Windows ZIP 为 `builds/深渊协议-0.11.1-Windows-x64.zip`，121,510,199 字节，SHA-256 `549022a0cdefac42b78259aa499512a33c28d88559e3f99e72f296cf9a022684`。Mac ZIP 为 `builds/深渊协议-0.11.1-macOS.zip`，144,306,589 字节，SHA-256 `a1abecc71404787e1a0e163110688620cfd03789dc58fc6c46c1c7efd3dba29e`。Mac 未公证、Windows 未签发行者证书，均未上传公共发行渠道。

已核实 `//macshare@Rog-xx/Games` 挂载并部署到新的 `深渊协议-0.11.1-Windows-x64/`，七个游戏文件、修订 4 的五个验收附件及共享根目录 ZIP 均从目标读回并验证 SHA-256。部署记录为 `builds/deployments/windows-0.11.1-rog-xx.json`。旧版 ZIP 与验收附件基线保持一致，没有覆盖旧版本或其他游戏。旧版已通过报告应使用 0.11.0 外层的“仅回传验收报告.cmd”补传；0.11.1 入口只接受与新包匹配的结果。

本次交付时，0.11.0 的 Windows 通过反馈仍有效，但原生报告尚未取回；0.11.1 Windows 实机、实体双手柄、DPI/Alt-Tab 与真人难度体验仍待对应结果。本次完成可读性修复和候选包交付，不将这些自动检查称为 production 总目标完成。

### 2026-09-12 · 取回 Windows 原生报告与 0.11.1 无界面验证

已通过 SSH 取回用户先前运行的 0.11.0 原生报告、六份日志和七张截图，14 个证据文件均与 Windows 端 SHA-256 一致。EXE/PCK 哈希匹配 0.11.0 交付记录；战役 84 项、存档写入 18 项、第二进程读取 74 项全部零失败，日志无 `ERROR:`。实际平台为 Windows 11 64 位、系统版本 10.0.26200、AMD Radeon 890M，渲染器为原生 OpenGL。七张截图均为 1280×800；只查看了项目外 1024×640 的合作续档 JPEG 预览，不能据此认定 QHD 或实体手柄验收完成。

原始证据位于 `builds/qa/windows-0.11.0-rog-xx-recovered/`。白名单证据 ZIP `Windows验收报告-0.11.0-7b7de0fa.zip` 已放回共享盘 0.11.0 版本外层，与验收入口同目录，回读 SHA-256 为 `9c9a90efbae8a3f21d631b1f25a33af3150384e7b542c65fb9efa9c8ffe59214`。归档不包含测试存档或正常玩家数据。本次通过 SSH 取回后由 Mac 写回共享盘，没有改变 Windows/SMB 权限，也没有把此次恢复认定为原回传入口的权限问题已修复。

随后在同一 Windows 设备核对 0.11.1 的七个发行文件，并通过原包 `Verify-Windows.ps1` 完成战役 84 项、独立存档写入 15 项和冷启动读取 70 项，合计 169 项、零失败，日志无 `ERROR:`。该次原生执行通过 SSH，进程位于 Session 0，使用无界面模式和新的隔离存档目录。原始报告、日志及会话范围说明位于 `builds/qa/windows-0.11.1-rog-xx-headless/`；对应证据 ZIP 已放回 0.11.1 版本外层。

181 项运行源码/资源哈希保持不变，因此本轮没有重新构建发行包或重复 Mac/Wine 场景套件。文档变更通过差异检查；Windows 实际运行是本轮新增证据。0.11.1 桌面窗口渲染与输入/音频、两只实体手柄、2560×1440 无边框全屏、DPI/Alt-Tab、目标机帧时间及真人难度体验仍待验收，production 总目标保持未完成。


### 2026-09-12 · 0.11.1 Windows 报告确认与 Games 根目录回传

用户从 Windows 复制回 `diagnostics-09b77a25a2244c92b83d2cd7dbcad59e.zip`，检查报告及六份日志：原生 OpenGL 战役 84 项、存档写入 18 项、冷启动读取 74 项，合计 176 项、零失败，无引擎错误。EXE/PCK 与已交付 0.11.1 哈希匹配，`handoff.json` 绑定的报告 SHA-256 为 `0d0244befcabddc99463baa976b5137389c2059976cc1f35a7c0ea37f8241e41`。七张截图为 1280×800；只读取文件头确认尺寸，没有加载原始 PNG。用户同时明确“全部通过了，其他没有通过的测试先跳过”，本次按该验收范围收尾，实体双手柄、Windows QHD/DPI/Alt-Tab 与真人难度专项不再阻塞，不记为自动验证通过。

原始报告 ZIP 已复制到 `Games/Windows验收报告-0.11.1-7473adb3.zip`，SHA-256 为 `2d6883c28aed1a9e3cc09aa54566dd92467d7aada178d5d15f3fa31fef5e9224`。旧版 0.11.0 原生和 0.11.1 无界面报告也复制到 Games 根目录；旧文件均保留。

验收入口修订 5 将白名单证据压缩成单个 ZIP，直接写到指定已有目录；本次绑定 Games 共享对应的 Windows 本地根目录。不再创建 `验收记录/运行编号` 或要求重命名权限，重名使用新 ZIP 文件名，复制后回读 SHA-256 匹配才成功。0.11.0 和 0.11.1 五个外层附件均更新并回读核对；两版本的七个游戏文件保持不变，没有重新打包。

23 项 PowerShell/Python 工具回归通过，覆盖只读目标、本地诊断保留、ZIP 白名单、已有完整/半截报告保护、版本匹配补传和生成编码。另在 Windows 原生 PowerShell 执行修订 5 的 `-ReportOnly`，成功将已有报告直接写入 Games 根目录；Mac 经 SMB 读回同一 ZIP，完整性及报告凭据匹配，ZIP SHA-256 为 `6e50af90737980e721b00c97d804cc76286ba5556e47c5b65e42f362cee15b18`。这次执行只回传报告，没有重跑游戏。首次远端回传调用在附件更新完成前仍执行修订 4；修订 5 实测前增加脚本哈希检查并确认已更新，未将旧版调用计为新版验证。

本轮 181 项运行资源哈希仍与 0.11.1 构建时一致，因此沿用已有源码/原生/发行验证，不重复游戏测试。没有修改 Windows/SMB 权限或正式玩家数据。原生报告、恢复记录与根目录回传记录分别保存在 `builds/qa/windows-0.11.1-rog-xx-rendered/` 和 `builds/qa/windows-report-root-r5.json`。


### 2026-09-13 · 0.12.0 局内构筑与升级对比

新增局内构筑总览：实际单击/散弹/蓄力伤害、机体状态、当前符文、自带元素及战斗联动；使用原创 SVG 图标与等级条。单人和双人均可查看，奖励与商店按每席的武器和现有属性显示升级前后收益。Tab、界面按钮与手柄均可进入，返回时恢复原菜单/焦点；战斗中查看后回到暂停。查看、切换席位、重配对均不刷新随机选择，不消费、治疗或改写安全点。

源码十八套检查合计 **21,694 项、零失败**：原验证入口完成十七套，测试拆分后的 `build_menu.gd` 以同一严格验证器单独补跑，现已一并列入 `verify.py`。Mac 原生玩家服务 **742 项**、最终构筑菜单 **57 项**通过；测试使用 Dummy 音频驱动，不改动正式玩家设置。检查了 960×600 与 2560×1440 菜单、双席不同收益、文字边界、Tab 原有设置导航和非零手柄编号的重连/返回。压缩截图保存在本地验证记录，不提交原图。

Mac universal 包先完成 CRC、ad-hoc 签名、无界面战役 **87 项**、首次写盘 **15 项**和第二进程续档 **70 项**。随后增加静音原生窗口验收，发现自检退出早于音频线程释放播放资源；在完整游戏销毁后等待混音线程收尾，消除了相关资源占用错误。导出在上述源码验证后使用 `--skip-checks`，发行包自身检查完整执行。最终 Mac 发行包的原生窗口战役 **87 项**、写盘 **18 项**、第二进程续档 **74 项**全部通过，无引擎错误。归档：`builds/深渊协议-0.12.0-macOS.zip`，SHA-256 `03f4f62e5b31baed277aa2b1a360ddc9e6d960e50b86bc2e1b214b1541fdc9b6`。

打包/报告工具 **24 项**通过；Windows 验证器也指定 Dummy 音频驱动，避免开发验收发声。执行命令为 `python3 -m unittest discover -s tools -p 'test_*.py'`。

Windows x86_64 包通过 PE/图标/版本/许可与资源完整性检查，在标记的隔离 Wine 环境中完成无界面 **87 + 15 + 70 项**和原生 exe 的 OpenGL 窗口 **87 + 18 + 74 项**，全部通过；Wine 兼容性证据与 Windows 实机验收分开。日志只有既有的 Wine SDR 白电平查询警告，无引擎错误。ZIP SHA-256 `bb8e2cb921beaad63bc39b6a819c65b8f5cdc77db76ea4c09de9a8de606afe40`。绑定本版版本/哈希的修订 5 验收附件已生成，报告仍直接写到 Games 根目录。当前交付设备连接超时，因此 **0.12.0 尚未传到 Games，也没有新增 Windows 实机验收**。

该版没有调整敌人、武器或祝福的战斗数值。用户新增的每战斗房约两分钟、设置界面改版与更醒目的全屏入口将作为后续独立改动；不把本次构筑信息改进当作这些目标的完成证据。0.11.1 经用户确认跳过的实体手柄、QHD/DPI/Alt-Tab 与真人难度专项保留原记录；不改写为通过。


### 2026-09-13 · 0.13.0 终端设置

设置页拆为画面与舒适度、声音、键盘按键、手柄与辅助四个分类，配套原创 SVG 显示器、混音台、键盘、手柄和导航图标。局部震动/闪光预览不推进战斗；声道输出表响应音量与静音；全屏按钮直接位于主菜单和暂停/结算页，并与 F11 同步。设置返回保留原焦点，双人重新配对后继续停留设置。继续沿用原偏好格式和隔离存档，不改玩家数据。

十九套源码检查合计 22,668 项通过。完整入口在中文界面套件发现结算页新增按钮超宽后中断；修正后用相同严格 wrapper 从该套件继续，之前已通过的三套未重复。Mac 原生玩家服务 737 项、设置专项 974 项通过，覆盖实际鼠标/手柄事件、四页所有按钮可达、OS 全屏、音量边界、预览只读、双人恢复与小窗口/QHD 布局。原生指针测试统一把画布坐标转换为窗口像素，避免窗口伸缩造成测试误点；只查看项目外压缩 JPEG。

Mac 0.13.0 通用发行包完成签名校验、原生战役 87 项、首次存档 18 项、冷启动续档 74 项；全程使用 Dummy 音频，日志无 ERROR。ZIP SHA-256：`ef477bb3aca06fecb9298274d1ed062e6a48c3244fdde2decc2f7eb6662955c7`。源码清单 `builds/qa/source-0.13.0.json` 记录 199 项运行文件哈希。

本次同种子速射枪正常数值输入机器人基线：单人全流程 336.5 秒，15 个普通/精英战斗舱中位数 22.1 秒（3.0–31.4），五场守卫 6.7–7.9 秒。同种子双人全流程 325.9 秒，普通/精英战斗舱中位数 22.9 秒。该数据用于下一轮战斗节奏修改，明确尚未达到约两分钟的战斗舱目标；机器人和真人体验分别记录，既有专项跳过决定继续有效。


Windows 0.13.0 x64 包在隔离 Wine 环境完成无界面 87 + 15 + 70 项、OpenGL 窗口 87 + 18 + 74 项，全部通过，无引擎错误；这是兼容性验证，没有新增 Windows 实机结果。ZIP SHA-256：`6b3415a0ff11c5c224a05e38c38bd04ac0a58c050248bd7c701d784bae0f7a63`。重新解压并核对六个发行文件及校验清单；已生成绑定本版哈希的修订 5 验收附件，报告目标为 Games 根目录。Rog-xx 仍连接超时，共享未挂载，0.13.0 尚未部署；交付清单保存于 `builds/deployments/pending-0.13.0.json`，旧版保留。


### 2026-09-13 · 0.14.0 两分钟战斗节奏

新版战斗改为有限的近卫/混合/交叉火力批次，每批最多六敌；首舱保留三敌起步，第三批引入远程。增援保持完整预警，倾向战斗距离并彼此分散；清完一批为存活队员修复两点耐久。守卫耐久调整以保留完整攻击循环，攻击伤害、前摇和弹速不变。清完即可领奖，未设置最低通关时长。

新增敌人共享旧版整舱击杀废料预算，以整数差分保证余数完整；清舱/核心奖励、商店价格和已有成长属性不变。旧十二舱战役保持旧规则，新版安全点按原有入口恢复机制继续。二十套源码检查合计 **23,718 项通过**，包含逐敌/整舱奖励、有限批次、双人修复及其离线边界；首区/终区和极端/双人主武器实弹打靶 931 项通过。

正常规则速射枪输入机器人完成单人和双人三十舱流程，每次均经历 20 个战斗/守卫舱，耗时中位数 **106.6 / 108.45 秒**，范围 79.9–137.0 / 78.2–150.8 秒，整局约 36.9 / 37.3 分钟。刀锋第一大区四场战斗为 99.0 / 143.0 / 174.5 / 159.1 秒。Mac 原生速射枪第一大区为 78.9 / 104.3 / 124.0 / 109.0 秒，游戏时钟与物理步数相差 0.04%；独立首舱原生复测 79.6 秒，退出清洁。这里只把机器人作为工程基线，实体手柄与真人难度专项仍按用户要求跳过。

Mac 原生双人扩展地图压力检查 **84 项通过**：2560×1600 输出，900 帧 P50 16.673 ms、P95 21.744 ms、P99 32.819 ms；40 次通过暂停返回船坞的重开无节点增长，孤儿节点为零。首次压力夹具从战斗直接调用仅接受暂停状态的返回接口，未真正返回；已纠正夹具，保持正式游戏的状态保护。加速游玩测试退出前恢复正常时钟，给声音混音线程真实 200 ms 清理时间，最终日志无资源错误。

本轮持续使用 Dummy 音频、隔离数据和项目外压缩 JPEG；未改系统音量或玩家文件。运行源码清单为 `builds/qa/source-0.14.0.json`（199 项），游玩基线和压力报告位于忽略的 `builds/qa/`。

Mac 0.14.0 通用包完成签名校验、原生发行战役 87 项、首次存档 18 项及独立进程冷启续档 74 项，全部通过；重新解压后的应用签名有效。ZIP SHA-256：`545c7480b00828b88236fd93847af469b1b9a24a912c8fa51ce1b743ecc20e71`。包为 ad-hoc 签名，未公证；本次实际运行于 Apple M4 Pro。

Windows 0.14.0 x64 包完成隔离 Wine 无界面 87 + 15 + 70 项和 OpenGL 窗口 87 + 18 + 74 项，全部通过，无脚本/引擎错误；Wine 的 SDR 白点查询警告保留在日志中。ZIP SHA-256：`0cd4ceb37b5a7068f5cfb119d66450bb291620e2d76058f17fcb29142fd480c0`。重新解压后六个发行文件和校验清单一致。这里是兼容层结果，没有新增 Windows 实机验收。

已生成本版修订 5 验收附件并核对全部哈希，报告 ZIP 直接写到 `C:\Program Files (x86)\Games` 根目录。Rog-xx SSH 再次超时、Games 未挂载，0.14.0 尚未传送，旧版本和玩家文件保留。待交付清单为 `builds/deployments/pending-0.14.0.json`，完整本地证据为 `builds/qa/milestone-0.14.0.json`；此前获准跳过的实体手柄、Windows QHD/DPI/Alt-Tab 和真人难度专项继续标记为跳过。


### 2026-09-13 · 0.14.0 完整战役计时复核

使用未变动的 0.14.0 源码，补跑包含物理步数计数的单人/双人速射枪完整输入机器人，均以正常角色数值通过三十舱、六段剧情及结局；没有直接清敌或修改战斗数值。每次有 20 个战斗/守卫舱，统计包含最终守卫、排除十个安全舱。单人中位 **107.3 秒**、范围 **78.4–140.0 秒**；双人中位 **110.9 秒**、范围 **78.7–155.1 秒**。这是新的样本，前节保留此前样本值。

单人游戏时钟 2225.60 秒、物理步数对应 2219.00 秒，相差 6.60 秒（0.30%）；双人为 2244.22 / 2238.82 秒，相差 5.40 秒（0.24%），均在校验器 1% 容差内。16 倍加速仅缩短墙钟等待；工程时长证据仍与真人体验区分。本轮使用 Dummy 音频与禁用正式存档的夹具，日志无脚本/引擎错误。报告为 `builds/qa/pacing-0.14.0-solo-clock.json`、`builds/qa/pacing-0.14.0-coop-clock.json`，没有变更运行资源或重新生成发行包。

逐项目标核对结果保存于 `builds/qa/goal-completion-audit-0.14.0.json`。当前尚缺把最新 Windows 包复制到已授权的 Games 共享：DNS 仍指向已知设备，SSH 与 SMB 端口返回主机不可达，没有共享挂载。已获准跳过的专项继续保持跳过状态。

### 2026-09-13 · 离线 Windows 的本轮跳过规则

用户新增规则：“如果 Windows 测试没有开机，可以先跳过 Windows 部分”。再次连接 Rog-xx 仍超时，因此本轮先跳过 0.14.0 Windows 实机测试和 Games 共享交付；保留已验证的 Windows 包、版本绑定的验收入口和待复制清单，报告仍写入 Games 根目录。跳过不等于交付或实机通过，也不影响此前明确跳过的实体手柄、Windows 显示专项和真人难度验收。

完成核对重新确认 199 个运行文件哈希、二十套源码检查的 23,718 项通过记录、Mac 原生发行与签名、两个发行 ZIP、单/双人完整战役计时报告和验收附件一致；无需修改游戏或重复打包。上述授权范围内的开发与本地交付已完成，Windows 设备恢复在线后的复制保留为后续可执行事项。

### 2026-09-13 · 0.15.0 元素、守卫与紧凑 HUD

新增热冲击/三层蓄毒爆燃的可合作范围反应；五名守卫的专属危险多边形与伤害共用几何；战斗界面使用原创 SVG 操作图标，底部由 164 缩到 104 逻辑像素，完整构筑保留在按需页面。具体机制和三十舱输入机器人证据见 `balance.md`。

源码共 23 套 / 23,980 项通过：`verify.py` 前 22 套通过，最后 HUD 快照检查最初因自动修复继续前进而未固定在 50% 失败；仅在测试快照前冻结测试游戏的处理时钟后，通过 `verify.run_check` 重跑 HUD，无界面 49 项、原生 52 项通过。上述 22 套通过后未再改动运行时代码。原生守卫 175 项通过；画面只检查压缩 JPEG。记录为合并验证结果，不将首次完整命令的失败隐去。证据在 `builds/qa/source-0150-compact-hud.json`。

新增 HUD 之前的原生双人 QHD 密集守卫压力测试为 84 项通过：900 帧，P50 / P95 / P99 = 16.683 / 24.622 / 32.13 ms，最慢 44.06 ms；峰值 313 节点 / 29 弹体，40 次真实暂停返回船坞重开，零孤儿节点。该时间样本未包含后续纯 HUD 精简，不能称为最终 HUD 的重复压力实测。

Mac 通用发行包原生战役 87、写入 18、第二进程读取 74 项通过，签名校验通过；保持 ad-hoc、未公证，实际硬件为 Apple M4 Pro，Intel 尚未实测。解压件为 `builds/macos-0.15.0/深渊协议.app`，旧版本保留。Windows x64 包在独立 Wine prefix 通过无界面 87 + 15 + 70 和窗口 87 + 18 + 74 项。两平台运行资源与当前源码哈希一致；所有开发、导出和自检均使用 Dummy 音频。

- Mac ZIP：`深渊协议-0.15.0-macOS.zip`，144,415,889 字节，SHA-256 `97bf90af858bfbdc5cf3ae0aa14c961324671b7c70285fbdf718350d9a69294c`。
- Windows ZIP：`深渊协议-0.15.0-Windows-x64.zip`，121,619,719 字节，SHA-256 `5811978e94ca2dc9c6f90760e4022a0057ccd44978c2608dd31c877a34409be3`。

验收附件为 `builds/acceptance-0.15.0/`，从匹配 ZIP 与构建报告生成，报告直接写入 `C:\Program Files (x86)\Games`。Windows 本轮共享交付和实机专项因离线跳过；实体手柄、DPI/Alt-Tab 与真人难度继续按用户要求跳过。Wine 检查不替代这些证据。

### 2026-09-13 · 0.16.0 实时分层剧情动画

抵达穹灯、恢复泵组、乘客归航各有一段八秒的原创 SVG 分层过场，由 Godot 控制位置、缩放、旋转与透明度。图层是本地矢量插画，没有视频文件。继续按钮即时可用，暂停/重播不影响战斗、随机数或存档；失焦暂停，隐藏停止处理，下一次菜单释放节点。新增“剧情动画”设置，关闭时使用最终静态画面；旧设置通过默认值继续兼容。

完整 `verify.py` 通过 24 套 / 24,057 项。原生过场 59 项通过，覆盖三节点、两种尺寸、非零设备编号的手柄操作、即时继续、暂停/重播、战斗隔离、静态偏好、失焦/隐藏停止和释放。原生设置 1,004 项通过，包括新开关的手柄操作和冷读。另一次带性能的过场运行 61 项通过：2560×1440，360 帧活动动画，P50 / P95 / P99 = 11.611 / 13.700 / 13.788 ms，最大 13.932 ms，Apple M4 Pro。该样本只覆盖剧情画面，不替代战斗压力测试或其他硬件结果。源码哈希与记录见 `builds/qa/source-0160-story-animation.json`。

Mac 原生发行程序通过战役 88、存档写入 18、第二进程冷读 75 项，以及通用包与签名校验；新增检查确认包内矢量资源可用、静态剧情偏好跨进程保留。Mac 解压件为 `builds/macos-0.16.0/深渊协议.app`，ad-hoc 签名、未公证，Intel 尚未实测。Windows 在带标记的独立 Wine prefix 通过无界面 88 + 15 + 71 和窗口 88 + 18 + 75 项；Wine 证据与目标 Windows 原生硬件验收分开记录。所有开发与验证使用 Dummy 音频，旧包和普通玩家存档保留。

- Mac ZIP：`深渊协议-0.16.0-macOS.zip`，144,512,032 字节，SHA-256 `7ca07d71dd890ef59a21be40481efd70974419eabfb9b6295d98cab7afab2d1a`。
- Windows ZIP：`深渊协议-0.16.0-Windows-x64.zip`，121,715,858 字节，SHA-256 `6ebf050a20d829babddbca66f4371c84916c456c649fa9c2b571f5c993ab5391`。

验收附件为 `builds/acceptance-0.16.0/`，由匹配 ZIP 和构建报告生成，输出仍直接指向 `C:\Program Files (x86)\Games`。本轮不重试已离线的设备；共享交付、Windows 显示专项、实体双手柄与真人难度保持已授权的跳过状态。纯过场与设置展示改动没有重跑三十舱战斗计时，最近正常数值完整流程仍是 0.15.0 的单/双人样本。


### 2026-09-13 · 0.17.0 近战动作与固定出手反馈

七种近战的残影改为记录出手时的世界位置与朝向，移动和新瞄准不会拖走已经结算的范围。角色骨架在 180 ms 的收招内保留出手方向，移动、瞄准输入和下一次攻击均不被阻塞；长枪直刺、重锤砸击、锁链回收、双牙交替挥刃与第三击交叉标记表达原有区别。地面轮廓按掩体裁切，材质效果带固定握持偏移。暂停冻结，换舱和重开清理，关闭闪光保留静态形状。伤害、冷却、武器/祝福数值与存档格式未改变。

最终固定源码的 `python3 verify.py` 完整通过 25 套 / 26,886 项，运行文件清单 229 项保存在 `builds/qa/source-0170-melee-motion.json`。原生动作专项 2,827 项通过，含实际物理输入反向、移动、双人和近墙渲染；源码原生发行入口 95 项通过。首次完整检查在新增发行检查的数组类型推断处失败，显式声明类型后从头重跑通过。原生动作测试最初退出时仍有音频引用，确认是 Dummy 混音线程回收延迟后在测试清理阶段等待 250 ms，后续原生日志无资源错误。

新增正常数值长枪输入机器人检查先后发现：测试只固定房间随机种子，且每次物理步后等待渲染帧会降低加速时的输入采样率。现在同时固定初始全局种子、按每个物理步更新，并记录输入时钟。修正后的 0.17 样本完成首六舱、三段剧情和第一名守卫；四个战斗舱用时 85.9 / 128.9 / 151.7 / 137.2 秒，503.63 秒游戏时钟与 503.58 秒物理时钟相差约 0.009%。这是输入机器人，非真人难度验收。

初始 0.17 样本在第二舱阵亡；隔离的 0.16 对照第一次完成首守卫，修正采样后的对照又在第二舱阵亡。全部记录分开保留在 `builds/qa/lance-01{6,7}0-*.json`，未把失败隐藏或标为通过。即使固定初态，镜头震动与敌人时机仍共用全局随机源、跨进程渲染时序不同，所以这些少量样本不能证明难度改善或机械回退。近战长期生存和更可复现的战斗证据仍需后续打磨。此前 0.15 速射枪三十舱计时是旧驱动历史样本，不与这次结果直接作速度对比。

Mac 原生双人密集守卫压力检查 85 项通过，2560×1440 / M4 Pro / 900 帧：P50 16.277 ms、P95 22.160 ms、P99 24.092 ms，最慢 26.373 ms。739 帧有近战效果，最多同时四个残影；40 次通过暂停返回船坞重开，孤儿节点为零。报告为 `builds/qa/performance-coop-expanded-guardians-melee.json`；采样时没有其他 Godot 验证负载。这是本机压力范围，不代表所有设备锁定 60 FPS。


### 2026-09-13 · 0.17.0 双平台候选包

Mac 通用包通过签名校验、原生完整战役 95 项、存档写入 18 项和独立进程冷启动读取 75 项。可运行解压件为 `builds/macos-0.17.0/深渊协议.app`，再次校验签名通过；保持 ad-hoc 签名、未公证，实际设备 M4 Pro，Intel 未实测。

Windows x64 包验证架构、资源、版本与许可，并在独立且带标记的 Wine prefix 中通过无界面 95 + 15 + 71 项、窗口 95 + 18 + 75 项。新增七项确认每种近战的打击效果确实包含在发行资源中。Wine 是兼容层证据，未新增 Windows 原生设备结果。

- Mac ZIP：`深渊协议-0.17.0-macOS.zip`，144,516,518 字节，SHA-256 `3be5f890b9c6c3b7ddd7a8f2950047fcdfe6808316cfc628187f20a1ede6a91b`。
- Windows ZIP：`深渊协议-0.17.0-Windows-x64.zip`，121,720,348 字节，SHA-256 `ad9f035282ef45d62ee40574b07e6942681602e5c6b596e262022c5cd0ad2e87`。

ZIP 校验和、Windows 六个内容文件与校验清单、四个验收附件与清单、229 个运行源文件哈希全部核对。`builds/acceptance-0.17.0/` 绑定本版版本/哈希；报告目标仍是 `C:\Program Files (x86)\Games` 根目录。Games 共享未挂载，本轮没有重复连接尝试或删除旧版，0.17.0 保留本地待交付。完整记录为 `builds/qa/milestone-0.17.0.json`。所有测试和发行自检均使用 Dummy 音频与隔离数据，玩家存档和设置未参与验证。

### 2026-09-13 · 0.18.0 弹反返弹

新版成功弹反将一枚敌方弹丸沿来路返还，伤害取来弹与本局基础攻击的较高值，携带防御者自带元素与符文。返弹复用原弹体与绘制偏移，延迟修改物理碰撞掩码，保持双人归属、暂停与回收。掩体同时阻挡返弹和近身弹反冲击波，旧十二舱保留原行为。镜头使用独立随机源，画面刷新不再影响后来生成的敌人初始冷却或枪械散布；不声称整个物理战斗跨帧率确定。

`python3 verify.py` 完整通过 26 套 / 27,035 项；Mac 原生返弹专项 144 项、源码原生发行入口 97 项通过，日志无引擎/脚本错误。227 个运行文件哈希匹配，记录为 `builds/qa/source-0180-countershots.json`；该清单排除 .DS_Store 和资源目录说明文档，数量与旧清单不同。画面仅查看项目外 1024×640 压缩 JPEG。

最初的掩体失败确认是原近身冲击波穿墙造成 26 点伤害，修复后真实返弹与掩体用例通过。新增发行检查最初结束于战斗场景，导致后续双人大厅未进入；将它移到原失败场景检查之前，保留正式菜单状态保护，再次原生运行 97 项通过。

正常数值长枪六舱输入机器人未完成目标：首舱 88.3 秒，第二舱约 101.1 秒时阵亡；累计 116 击杀，游戏/物理时钟差 0.181 秒。失败样本为 `builds/qa/lance-0180-physics-input.json`，没有用专项检查替代近战生存证据。下一步需要继续定位近战受伤来源与防御节奏。既有实体手柄、Windows 显示与真人专项继续按用户授权跳过；本轮不重跑无直接性能变化的 QHD 密集近战基准，最近证据仍是 0.17.0。


### 2026-09-13 · 0.18.0 双平台候选包

Mac 通用发行包通过原生战役 97 项、首次存档 18 项和独立进程冷启动续档 75 项。ZIP 与解压后的应用签名再次核验；可运行应用为 `builds/macos-0.18.0/深渊协议.app`。ad-hoc 签名、未公证，实际硬件为 M4 Pro，Intel 尚未实测。

Windows 原始正式源码在独立 Wine prefix 通过无界面 97 + 15 + 71 项、窗口 97 + 18 + 75 项。最初两次分别触发 55 秒战役期限及增援检查期限，日志保留。临时加入逐舱时钟/物理步计数后，两模式战役约 34–35 秒通过；诊断源码与包移到 `builds/qa/` 单独保存。随后恢复与源码完整验证、Mac 包完全匹配的原始源码，重新导出，两模式全通过。没有放宽期限或改变游戏规则；前两次超时原因尚未证实，不宣称已修复。窗口日志保留 Wine 的 SDR 白点查询警告。

- Mac ZIP：`深渊协议-0.18.0-macOS.zip`，144,518,829 字节，SHA-256 `4ff5603186bc045bf44564245d2fddf5b2d602f96ef2f7cb0d0ede6a9f8bba5e`。
- Windows ZIP：`深渊协议-0.18.0-Windows-x64.zip`，121,722,655 字节，SHA-256 `a97202062dd1c5e36dec14b3ebd7e3148428a8fc28595c87221f5aac34e1a64e`。

两个 ZIP、Windows 六个内容文件与校验清单、四个验收附件及其版本绑定、227 个运行源文件哈希全部一致。验收目录为 `builds/acceptance-0.18.0/`，报告仍直接写到 `C:\Program Files (x86)\Games`；共享交付因未挂载跳过。所有开发与验证使用 Dummy 音频和隔离存档，旧版本保留。完整记录为 `builds/qa/milestone-0.18.0.json`。

功能提交 `290fdf7` 已进入 main，PR #73 已标记合并，主分支工作流 34748655956 全部通过。GitHub GraphQL/REST 合并接口返回服务异常后，确认 main 与分支只有一个提交差异，用非强制快进推送完成相同的线性合并；未强推或改变无关内容。

### 2026-09-13 · 0.18.0 游玩输入与完整战役复核

修正测试专用输入适配器，防止逐物理步释放/按下覆盖技能边沿；不调用角色技能、不修改游戏规则。`--projectile-defense` 将弹道预测作为独立可选策略，`--trace-damage` 只读记录受击来源；旧策略和历史失败保持可追溯。具体决策见 `.agents/notes/implemented/2026-09-13-abyss-bot-action-edges.md`。

`python3 verify.py` 完整通过 27 套 / 27,086 项，日志 `/tmp/abyss-bot-input-verify.log` 无脚本/引擎错误。新增输入套件无界面、Mac 原生各 57 项通过；其最初四项蓄力门槛失败源于夹具误设 0.9 秒，纠正为武器定义的 0.8 秒，不计作游戏修复。

正常规则单人长枪、双人火焰法杖各完成三十舱、五位守卫和全部六段剧情。二十个战斗舱中位 134.4 / 121.15 秒，范围 84.6–170.0 / 73.5–158.0 秒；分别记录 85 / 93 次返弹及 10 次热冲击。机器人拥有精确战斗信息，这些是防御机制可用、成长可推进的证据，不能推断所有武器或真人首局难度。完整统计和驱动边界见 `balance.md`，机器记录为 `builds/qa/bot-input-0180-verification.json`。

227 个运行文件仍与 `source-0180-countershots.json` 全部匹配；本轮只改测试和文档，没有提高版本或重复导出。既有 Mac/Windows 0.18.0 候选包继续适用。Windows 共享未挂载，实体手柄、Windows 显示专项和真人难度继续按授权跳过；最近原生密集近战性能证据仍为 0.17.0。

### 2026-09-13 · 0.19.0 弹反的可播放图解

新版操作指南加入原创 SVG 炮台/机体与有限五秒动画，展示来弹、接近时出手和原路返还。初始静态、主动播放，支持暂停/继续/重播，失焦暂停，隐藏停止，离开释放。当前按键随手柄/键鼠与重绑更新；“剧情与指南动画”沿用原 `story_motion` 设置，关闭后保留完整静态图。旧十二舱不显示未实现的返弹机制，战斗、成长和存档格式不变。

`python3 verify.py` 完整通过 28 套 / 27,107 项；新增图解检查无界面 24 项、Mac 原生含画面和性能 30 项通过。测试从真实菜单和非零手柄事件进入，验证战斗/资源/随机状态隔离、暂停重播、动态按键、多尺寸、旧版与静态边界。初始原生取样在第一动画帧前返回，记录显示处理已启动但时钟为零；测试等待实际帧后通过。早期夹具误读 `settings.bindings`，已改为现有 `settings.keys`；两项均为测试修正，没有改变游戏时钟或输入。

原生动画性能：M4 Pro / 2560×1440，240 帧全为活动动画，P50 / P95 / P99 为 8.041 / 12.663 / 15.963 ms，最大 18.009 ms。没有并发 Godot 测试负载；这是指南性能，不能替代密集战斗或其他硬件验证。画面仅查看已校验的 1024×576/640 项目外 JPEG，单张低于 40 KiB。231 个运行源文件哈希与源码验证记录位于 `builds/qa/source-0190-defense-guide.json`。

机器人通关不能证明真人理解弹反，本轮仅改善指南表达。祝福页已有数值对比和通用联动条件；下一步可进一步区分组合立即可用还是尚缺另一种元素。实体手柄、Windows 显示与真人难度专项仍保持获准跳过。

### 2026-09-13 · 0.19.0 双平台候选包

Mac 通用包通过原生战役 99 项、首次存档 18 项、第二进程续档 75 项，额外两项验证发行资源中的图解和战斗隔离。解压后的 `builds/macos-0.19.0/深渊协议.app` 签名再次验证通过；ad-hoc、未公证，实际硬件 M4 Pro，Intel 未实测。导出复用本轮已完成的 28 套源码验证，独立包内自检没有跳过。

Windows x64 包在独立且带标记的 Wine prefix 通过无界面 99 + 15 + 71 项、OpenGL 窗口 99 + 18 + 75 项，六份引擎日志无脚本/引擎错误。这是 Wine 兼容性证据，没有新增 Windows 原生硬件验收。

- Mac ZIP：`深渊协议-0.19.0-macOS.zip`，144,526,672 字节，SHA-256 `3a75686b474fa9bd0cba9751809a037eeeb3ec12b97c5207ea5635ba0f4d8b97`。
- Windows ZIP：`深渊协议-0.19.0-Windows-x64.zip`，121,730,497 字节，SHA-256 `6b38179f4b07e440bbc75e3b69f974082d6f4a56b64bc1fdf69794c61d7ce35a`。

两个 ZIP、Windows 内容文件、四个验收附件与版本绑定、231 个运行文件哈希再次一致。验收目录 `builds/acceptance-0.19.0/` 的报告目标为 `C:\Program Files (x86)\Games` 根目录；Games 未挂载，本轮不重试离线设备，共享交付与实机专项按授权跳过。旧包和玩家文件保留，全程使用 Dummy 音频和隔离数据。完整记录为 `builds/qa/milestone-0.19.0.json`。

功能 PR #76 已合并，main 提交 `5392a5a`，主分支工作流 34750268052 成功。275 个无关工作树文件与原始哈希一致，没有纳入提交。

### 2026-09-13 · 0.20.0 祝福组合可用性与双平台候选包

祝福卡和商店使用只读构筑信息识别武器自带元素、当前符文、共享新星和队友元素，给出选后可用、已有联动、队友接力或还缺火焰的提示。相同的两席数值与配方只展示一次，不同部分保留席位标记。奖励卡的高度跟随换行内容，修复新增长文本伸出卡片和商店底部越界的问题。战斗、成长数值和存档格式未改动。

源码完整回归 **28 套 / 27,178 项，零失败**；Mac 原生构筑菜单 **107 项**通过，覆盖 960×600 和 2560×1440、非零手柄确认，以及实际选择毒素后由两席叠毒与火焰引爆邻敌。最初缺少功能的测试记录为 69 项 / 13 失败；最初扩展布局为 96 项 / 9 失败，修复后无界面 96 项全部通过。画面只查看项目外、最长边 1024、低于 50 KiB 的 JPEG 预览。231 个运行文件哈希与验证记录一致。

Mac 原生发行自检为战役 **101**、存档写入 **18**、第二进程续档 **75**，签名与解压应用再验证通过。Windows Wine 无界面为 **101 + 15 + 71**，OpenGL 窗口为 **101 + 18 + 75**，六份引擎日志无错误；这是兼容性证据，不是 Windows 硬件实测。导出复用本轮完整源码回归，未跳过独立包自检。

- Mac ZIP：`深渊协议-0.20.0-macOS.zip`，144,528,033 字节，SHA-256 `a926a9642989ced559b1213f600d07a688a0c8d77141ad973fb730c63ce4c33d`。应用位于 `builds/macos-0.20.0/深渊协议.app`；通用包、ad-hoc 未公证，M4 Pro 已测，Intel 未测。
- Windows ZIP：`深渊协议-0.20.0-Windows-x64.zip`，121,731,859 字节，SHA-256 `ab199488c64709aa485d572767ed69a2ff410c88f39ebf37baa9dd1e46827182`。

`builds/acceptance-0.20.0/` 绑定本版 EXE/PCK/验证脚本和 ZIP，报告直接写入 `C:\Program Files (x86)\Games`。Games 未挂载，共享交付、Windows 显示专项、实体手柄及真人难度继续按授权跳过。旧包、玩家文件与 275 个无关工作树文件保留；完整记录为 `builds/qa/milestone-0.20.0.json`。功能提交 `ba7c593`。

### 2026-09-13 · 0.21.0 毒素层数与引爆反馈

用原创 SVG 六格毒槽替换新版敌人的地面毒素圆弧，前三格与后三格分组，三至六层显示火焰图形，便于选择叠毒或接力引爆。图槽直接读取真实毒层，跟随火焰消耗、到期和失效消失，暂停时与战斗一起冻结。没有增加每敌节点或循环动画；关闭闪光仍可辨，镜头拉远时做有上限的尺寸补偿。指南复用三层图形，旧十二舱不展示三层门槛。

源码 **29 套 / 27,226 项**通过。毒槽专项无界面 **48**、原生 **53** 项通过，包含实际命中与邻敌爆燃、两层/三层/六层、消耗/到期/击杀、暂停、重开、旧版、1280×800 与 2560×1440、1.0/0.6 镜头缩放及共用指南。最初缺失功能用例为 1 项 / 1 失败，实现后通过。画面仅查看项目外、最长边 1024、低于 64 KiB 的 JPEG。

M4 Pro 原生密集双人/扩大地图/守卫招式压力检查 **85 项**通过：2560×1440，900 个取样帧均有可见毒槽，峰值 28 个。帧时间 P50 / P95 / P99 为 **16.743 / 22.413 / 24.426 ms**，最大 27.047 ms；峰值 414 节点、28 弹体，40 次重开后孤立节点为零。运行时没有并发 Godot 测试负载。此夹具刷新零伤害毒素以测量绘制开销，不是正常难度或真人可读性验收；战斗伤害、层数门槛和成长数值未改动。

Mac 原生发行通过战役 **102**、存档写入 **18**、第二进程续档 **75** 项；Windows Wine 无界面为 **102 + 15 + 71**，OpenGL 窗口为 **102 + 18 + 75**，六份引擎日志无错误。两个导出复用本轮完整源码回归，包内自检未跳过；233 个运行文件哈希与已验证源码一致。

- Mac ZIP：`深渊协议-0.21.0-macOS.zip`，144,530,025 字节，SHA-256 `84b42f8aed735976c3cc07e691187f56318f6c2761870a10a18542a430d3c34b`。解压应用 `builds/macos-0.21.0/深渊协议.app` 签名再次通过；通用包、ad-hoc 未公证，Intel 未实测。
- Windows ZIP：`深渊协议-0.21.0-Windows-x64.zip`，121,733,846 字节，SHA-256 `050b93180dd6237e4f9351563d45b98341ad92472a85d448964905aa5ccb34bc`。

验收附件 `builds/acceptance-0.21.0/` 绑定新版包，报告目标保持 `C:\Program Files (x86)\Games`。Games 未挂载，共享交付、Windows 显示专项、实体手柄及真人难度按授权跳过。275 个无关文件、旧包与玩家存档保留。功能提交 `3bfa1a1`，完整本地记录为 `builds/qa/milestone-0.21.0.json`。


### 2026-09-13 · 0.21.1 近战身体接触

新版七种近战按敌人碰撞圆与原扇形接触判定命中，解决侧边已经接触但中心仍在角度外导致的空挥。有限边界线段保留尖端/外角的真实落空间隙，实体掩体仍阻挡；伤害、冷却、击退、连击与符文单次应用保持原值，旧十二舱保留原判定。

源码完整回归 **30 套 / 28,246 项**零失败；新增接触专项最初 **1,020 项 / 437 失败**，修正后全部通过。Mac 原生最终 **1,022 项**通过，包含双席真实 Input 动作和实际符文应用。完整回归后加强了暂停断言（先清零冷却），最终原生专项再次通过。首次原生退出报告两个未回收对象，详细日志重跑未复现；测试退出沿用发行检查的 0.2 秒混音线程等待后最终日志干净，没有把它宣称为游戏修复。画面只查看最长边 1024、低于 64 KiB 的项目外 JPEG。

正常规则长枪输入机器人完成三十舱、五守卫和六段剧情。20 个战斗舱中位 **133.95 秒**，范围 **83.1–179.2 秒**，记录 72 枚返弹、389 次冲刺、12 次新星和 359 次弹反；本次未记录元素爆发。无人工生命/伤害/成长补给，游戏与物理时钟差 0.70 秒。报告 `builds/qa/lance-0211-full-projectile-defense.json`，策略和样本限制见 `balance.md`。这不是所有武器或真人难度验收。

首次 Mac 发行检查 **104 项 / 1 失败**：新增接触断言接在成功弹反后，漏算仍有效的 1.5 倍超载增伤。独立复现确认实际伤害 42.9 = 26 × 1.1 × 1.5、毒层为 1；修正测试预期后四项聚焦检查通过。仅发行自检断言变化，游戏逻辑与完整回归/通关机器人验证的版本一致。失败包保留于 `builds/qa/failed-0211-macos-release-assertion.zip`，未放宽命中范围或改变伤害以通过检查。


Mac 原生发行 **104 + 18 + 75 项**通过，解压应用 `builds/macos-0.21.1/深渊协议.app` 的签名再次通过；通用包、ad-hoc 未公证，M4 Pro 已测，Intel 未测。Windows Wine 无界面 **104 + 15 + 71 项**、OpenGL 窗口 **104 + 18 + 75 项**通过，六份引擎日志无错误。源码完整回归复用，仅在修正发行自检预期后重跑相关检查和完整包内自检；未改变已实测的战斗逻辑。

- Mac ZIP：`深渊协议-0.21.1-macOS.zip`，144,531,247 字节，SHA-256 `5f82a7ca9fa128512fa82f11db81d6fcafaf5a334b2072a60ee7a80081a546bc`。
- Windows ZIP：`深渊协议-0.21.1-Windows-x64.zip`，121,735,071 字节，SHA-256 `3946643c0de1c21d550806453b5d2aba542552cc06584b6dd662d514baabe1d2`。

233 个运行文件、两个 ZIP、Windows 六个内容文件、四个验收附件及其版本/哈希绑定一致。`builds/acceptance-0.21.1/` 报告目标保持 `C:\Program Files (x86)\Games`。275 个无关文件与基线哈希一致；旧版、失败诊断包和玩家文件保留。Games 共享未挂载，共享交付、Windows 显示专项、实体双手柄和真人难度继续按授权跳过。此次不重复密集战斗压力基准，最近证据为 0.21.0，未据此新增性能保证。完整记录为 `builds/qa/milestone-0.21.1.json`，功能提交 `bb1af69`，发行测试预期修正 `9fd90e6`。


### 2026-09-13 · 0.22.0 冰电霜链与双平台候选

电击命中真正冻结的目标后，向 220 距离内最近两名可见敌人各传导 45% 电击元素伤害，保留冻结。沿用源目标 0.8 秒电击间隔，不追加符文、汲取或递归传导；致命一击、掩体、守卫抗性和旧十二舱边界都有独立覆盖。所有十八武器与冰霜/电击双席输入可触发，普通硬直和未达到冻结的寒冷层数不能替代条件。没有新增存档字段。

原创元素图示增加第三行，低闪光模式保留真实两条传导线。祝福和商店根据自带元素、新星、当前符文及队友显示组合条件，构筑页列出实际数值。商店的继续救援和局内构筑改在同一行，修复新增说明在小窗口挤出底部按钮的问题。

验证与证据：

- `python3 verify.py`：导入及 31 套、28,343 项全部通过，无脚本错误。新增机制的最初核心复现为 23 项中 12 项失败，修改后通过。
- Mac 原生 `tests/frost_conduction.gd`：101 项全部通过；包括 960×600 / 2560×1440 菜单、两席输入、低闪光、暂停/恢复与重开。商店、构筑、指南和实战只查看外部压缩 JPEG，最长边不超过 1024、每张小于 256 KiB。
- 原生性能 `--coop --expanded --guardians --conduction`：85 项通过，M4 Pro / 2560×1440 / 900 帧；P50 16.678 ms、P95 21.269 ms、P99 24.628 ms、最大 36.113 ms。483 帧有可见传导，峰值 14 条连线、329 节点、22 弹体；40 次重开后孤立节点基线为零。该夹具主动周期施加冻结/电击，会影响敌人活动，不用于普通战斗节奏或性能改善的对照结论。
- 正常规则闪电法杖 `--projectile-defense --frost-build`：全部三十舱和六段剧情完成，20 个战斗/守卫舱中位 111.85 秒，范围 78.5–147.6 秒。五守卫分别 147.6 / 116.4 / 105.0 / 111.0 / 136.8 秒。最终攻击 130.69，冰霜 3 / 电击 3 / 汲取 3 来自实际选项；3 次霜链、124 次冲刺、7 次新星、69 次弹反、50 枚返弹。游戏 2221.27 秒，物理时钟差 0.89 秒。报告 `builds/qa/storm-0220-full-frost-build.json`；出现频率和真人配合体验仍需继续观察。
- Mac `export_macos.py --skip-checks --rendered`：复用上述当前源码回归；发行包仍实际执行 107 项战役、18 项首次存档和 75 项冷启动续档，全部通过。新增发行自检通过真实速射枪确认两邻敌传导、冻结保留、连线和图示打包。独立解压后的 `.app` 再次通过严格签名验证。
- Windows `export_windows.py --skip-checks --wine … --wine-prefix builds/qa/wine-probe --rendered`：Wine 无界面 107 + 15 + 71、窗口 107 + 18 + 75 全部通过。六份引擎日志无脚本错误或泄漏；三份窗口日志各有一次 Wine 的 `Failed to get SDR white level` 显示查询警告，测试仍正常退出。该证据属于兼容层，不是 Windows 实机。

Mac ZIP：`builds/深渊协议-0.22.0-macOS.zip`，144,543,371 字节，SHA-256 `1b37e180611f2ec2dd0fed2fd6a5914e58ebeeed57ffcfa0db91ca0712c79128`。解压件 `builds/macos-0.22.0/深渊协议.app`。仍是 ad-hoc 签名、未公证；Intel Mac 未实测。

Windows ZIP：`builds/深渊协议-0.22.0-Windows-x64.zip`，121,747,204 字节，SHA-256 `84de0213c05087859953cd8ed76ed758db23331fc7a41cd42eb06a075cb307eb`。生成器已核对完整 ZIP、逐文件清单与嵌入校验表；`builds/acceptance-0.22.0/` 的四个验收附件再次按清单验证，绑定本版及报告目标 `C:\Program Files (x86)\Games`。

源码、原生、性能和游玩日志保存在 `builds/qa/`，233 个运行文件哈希及平台报告汇总在 `source-0220-frost-conduction.json`。Games 共享本轮仍未挂载，没有传送新版本或改动已有游戏；Windows 实机、QHD/DPI、实体双手柄、真人难度继续按用户授权跳过。开发与测试均使用 Dummy 音频及隔离存档。


### 2026-09-13 · 0.22.1 真实冻结反馈

此前 `frozen` 同时承载电击硬直和真正冰冻，绘制却一律使用蓝色冰冻轮廓。新版从 `ice_frozen_left` 读取真正冻结：寒冷一/二层用三格菱形，实际冻结后显示雪花和冰冻轮廓；普通硬直使用不同机体颜色并隐藏冰冻轮廓。火焰消耗、自然解冻和死亡及时移除雪花，霜链保留它。原地寒冷圆弧被替换，毒槽和冰霜标记分行，两个标记都随镜头缩放补偿。指南直接复用三帧 SVG。旧十二舱保持原显示和规则，伤害、输入、成长、敌人和存档格式均未改变。

- 全量 `python3 verify.py`：资源导入与 32 套、28,418 项通过，无脚本错误；新增冻结反馈无界面 70 项。
- Mac 原生 `tests/freeze_feedback.gd`：75 项通过；真实元素命中、守卫抵抗、硬直排除、到期/消耗、零闪光、960×600 / QHD、双状态布局、暂停/恢复、重开与共用指南。只查看尺寸/字节数已核验的外部 JPEG 预览，未直接查看原始 PNG。
- 原生压力 `--coop --expanded --guardians --poison --conduction`：86 项通过，M4 Pro / QHD / 900 帧，P50 16.726 ms、P95 21.470 ms、P99 23.648 ms、最大 30.733 ms。全部 900 帧有毒槽、485 帧有传导，峰值 22 个毒槽、14 条传导线、356 节点、22 弹体；40 次重开后孤立节点基线零。该主动冻结/叠毒夹具验证绘制开销与回收，不是普通战斗节奏或帧率改善的对照。
- 只读诊断 `--weapon=storm --speed=16 --projectile-defense --frost-build --trace-freeze`：修改显示前的正常战斗逻辑完成三十舱，战斗中位 112.5 秒、范围 79.7–147.6 秒，最终冰霜/电击/汲取三级。第十三舱结束记录首次出现冰霜；5368 个冻结敌人帧只有 457 帧有近邻，314 帧同时电击间隔结束，实际 5 次霜链。报告 `builds/qa/storm-0221-freeze-observations.json` 保留帧采样限制，游戏/物理时钟差 0.88 秒。显示修复没有改战斗规则，因此未为纯显示修改重复整局计时。
- Mac 发行独立窗口：109 项战役 + 18 项首次存档 + 75 项冷启动续档全部通过；新增发行断言验证 SVG 打包及冻结源/未冻结邻敌的标记区别，独立解压件严格签名校验通过。
- Windows Wine：无界面 109 + 15 + 71，窗口 109 + 18 + 75 全部通过；六份引擎日志无脚本错误或泄漏。三个窗口进程各有一次非阻塞的 SDR white-level 查询警告。该结果不代表 Windows 实机验证。

Mac：`builds/深渊协议-0.22.1-macOS.zip`，144,546,167 字节，SHA-256 `83462c0a2131600f02f4c6c357c299c7d5fced80df375469c00b7b052650fea3`；解压件 `builds/macos-0.22.1/深渊协议.app`，ad-hoc 签名、未公证，Intel 未实测。

Windows：`builds/深渊协议-0.22.1-Windows-x64.zip`，121,749,992 字节，SHA-256 `14d515343d616f1b5882c1be81be1a206537d1f428e08f6ed95eb7cfa93b591b`。完整 ZIP、逐文件清单、嵌入校验表和 `builds/acceptance-0.22.1/` 四份验收附件均已校验，报告目标仍为 `C:\Program Files (x86)\Games` 根目录。

235 个运行文件哈希及全部日志/报告引用记录在 `builds/qa/source-0221-freeze-readability.json`。两次导出复用本轮完整源码检查，仍实际运行发行包与两进程存档检查。Games 未挂载，未传送新版本；Windows 显示、实体双手柄、真人难度仍按授权跳过。所有开发/测试使用 Dummy 音频及隔离存档，275 个无关文件保留。


### 2026-09-14 · 0.23.0 武器改装与双平台候选

十八种武器按攻击方式获得两项互斥的局内改装。首舱奖励优先显示两种方案及一项常规强化，后续仍可选择尚未装配的改装。近战改变距离和角度，弹体可贯穿/速蓄或增加两枚弱侧弹，回旋刃改变回程时间与伤害，闪电改变距离和目标数，引力井改变范围和聚怪时长。双人同选后各自按武器生效；已有改装的席位不被替换。原创 SVG、动作扫掠、实际弹道与构筑数值保持对应。

新战役 checkpoint schema 3 显式保存合作状态及每席改装，旧单/双人及十二舱战役仍可继续；旧程序会拒绝新格式并保留原档案。奖励和路线均通过真实隔离写盘冷启动，设备编号继续不入档。功能提交 `0494d1c`。

- `python3 verify.py`：33 套、28,771 项通过，无脚本错误；改装无界面 345 项。239 个运行文件哈希在打包后保持一致。
- Mac 原生改装：349 项通过。真实命中、贯穿、回程、连锁、引力范围、掩体、暂停、键盘选卡、双人不同收益、未来档案保护与旧档读取均有覆盖。只查看最长边 ≤1024、≤256 KiB 的外部 JPEG。测试退出等待音频混音线程释放已停止的播放对象后，详细日志无泄漏警告。
- 正常规则输入机器人：穿透速射枪与双人分裂速射弓都完成三十舱；20 个战斗/守卫舱中位分别 121.75 / 114.2 秒，范围 73.9–155.1 / 75.5–145.4 秒。分裂方案从“全部弹丸减伤”修订为保留主弹伤害、两枚侧弹各 35%、攻击间隔 +20%，修订前双人中位约 168 秒。未改玩家基础属性或敌人来保证胜利，真人与其他构筑验收仍未完成。
- 原生 QHD 双人改装压力：900 帧，P50 16.677 ms、P95 25.669 ms、P99 27.325 ms、最大 42.498 ms；峰值 38 弹体 / 377 节点。85 项中 1 项失败：P95 超过 25 ms 门槛。40 次重开和孤立节点基线零通过。按用户“其他未通过的测试先跳过”要求记录为未达标并暂跳过；不能声明全面性能验收通过。前一次中途窗口缩放造成的 866 帧样本单独保留为无效证据。
- Mac 独立发行程序：113 项战役 + 18 项首次存档 + 77 项冷启动续档通过；独立解压件严格签名验证通过。
- Windows Wine：无界面 113 + 15 + 73，窗口 113 + 18 + 77 全部通过。三个窗口进程各出现一次非阻塞 SDR white-level 查询警告，无脚本错误或泄漏。该结果属于兼容层，不代表 Windows 实机。

Mac：`builds/深渊协议-0.23.0-macOS.zip`，144,569,048 字节，SHA-256 `4f2c7c7dca4ed351a9e5042399d017bc0bdb38872e13061b169a955c7ab5d0ae`。解压件 `builds/macos-0.23.0/深渊协议.app`，ad-hoc 签名、未公证，Intel 未实测。

Windows：`builds/深渊协议-0.23.0-Windows-x64.zip`，121,772,874 字节，SHA-256 `2110096c69ac1a19a643289ef046aff5e83b37c2961e04e435a21ed55d4b4747`。完整 ZIP、逐文件清单与 `builds/acceptance-0.23.0/` 四份验收附件均已核验，报告目录绑定 `C:\Program Files (x86)\Games` 根目录。

两次导出复用本轮完整源码回归，仍实际运行发行包及两进程存档检查。全部记录位于 `builds/qa/source-0230-weapon-refits.json` 与 `builds/qa/milestone-0.23.0.json`；功能、性能、正常流程和平台日志由记录引用。Games 仍未挂载，未传送新版本；Windows 实机显示、实体双手柄和真人难度继续跳过。开发/测试全部使用 Dummy 音频与隔离存档，275 个无关文件保持原样。


### 2026-09-14 · 0.23.0 原生帧节奏诊断

本轮只更新压力测试与说明，239 个运行文件与已交付的 0.23.0 完全一致。没有进行寻路缓存、降低敌人数、修改画面、改变垂直同步默认值或重新打包。上一节的性能失败和双平台发行证据保持原样。

原压力测试的 `frame_ms_*` 来自每次等待物理帧再等待处理帧的墙钟间隔；本机显示器为 120 Hz，物理帧率为 60 Hz，两次观察之间可能有多个绘制帧。新增 `rendered_frame_ms` 统计引擎 `frame_post_draw` 回调间隔，保留原来的 P95 ≤25 / P99 ≤50 ms 门槛并额外应用到绘制间隔。回调不是操作系统或屏幕实际呈现时间，不能据此证明显示器端无卡顿。

临时函数计时中，寻路平均约 0.033 ms、全部敌人物理回调约 0.488 ms；没有证据支持原先考虑的寻路缓存。两秒原生栈采样的主线程 1,474 次采样中，644 次位于 OpenGL 换帧下的 AppKit 同步等待。临时插桩已移除，原件及采样保存在忽略的 `builds/qa/profile-0230-instrumentation/` 和 `profile-0230-cpu-sample.txt`，该样本只用于定位，不作为发行性能验收。

- 默认垂直同步、启用 `--trace-frames`：900 次观察 / 906 物理帧 / 1,675 处理帧；旧间隔 P95 25.117 ms、P99 27.937 ms，仍有 1 项失败。1,674 个绘制间隔 P95 14.710 ms、P99 25.158 ms，绘制 CPU P95 3.081 ms。完整压力与 40 次重开共 88 项，87 项通过，孤立节点基线零。报告 `builds/qa/performance-coop-expanded-guardians-refits-1789346735-95616.json`。
- 仅隔离测试进程加引擎参数 `--disable-vsync` 作对照：900 次观察 / 900 物理帧 / 4,821 处理帧；旧间隔 P95 20.720 ms、P99 22.841 ms；4,820 个绘制间隔 P95 7.520 ms、P99 9.425 ms，绘制 CPU P95 2.380 ms；88 项通过。报告 `builds/qa/performance-coop-expanded-guardians-refits-1789346770-95707.json`。该对照改变了测试显示配置，不能替代默认配置验收；未写玩家偏好，也未修改系统设置。

Python 独立重算上述有序原始数据，确认样本数及所有分位数与摘要一致。每次报告加时间戳/进程编号并打印实际路径，避免覆盖早先的失败证据。完整记录为 `builds/qa/frame-timing-evidence.json`。当前证据支持换帧等待参与波动，尚未形成经验证的游戏性能优化；原性能失败按先前授权继续保留为未达标/暂跳过。Windows 实机、实体双手柄、真人难度仍未新增验证。

最终源码回归 `python3 verify.py`：导入及 33 套、28,767 项通过，无脚本错误或警告，日志 `builds/qa/frame-timing-verify.log`。不带 `--trace-frames` 的默认垂直同步原生复查完成 900 次观察 / 901 物理帧 / 1,702 处理帧，88 项通过；旧间隔 P95 23.197 ms、P99 25.479 ms，1,701 个绘制间隔 P95 14.527 ms、P99 24.992 ms，40 次重开后孤立节点基线零。报告 `builds/qa/performance-coop-expanded-guardians-refits-1789347323-98446.json` 不包含可选 CPU 计时和原始追踪。默认配置本轮有通过也有失败，不能用最后一次通过覆盖旧失败或声称已经改善帧率。239 个运行文件同时与 Git HEAD 及 0.23.0 发行哈希一致；因此继续使用上一节已验证的 Mac/Windows 包。


### 2026-09-14 · 0.24.0 霜火回响

新增新星专属局内祝福：施放 0.8 秒后，在原位置产生 45% 基础攻击伤害的火焰回响，范围 270，可引发已有热冲击与毒素爆燃。保留原新星的初次伤害、冻结和十秒冷却；回响由世界持有，暂停冻结、换房/重开清理，掩体阻挡回响及二次反应。它不继承主武器自带元素、符文、汲取或终结。原创 SVG 同时用于奖励、构筑及实际回响，HUD 按席显示“霜火”。功能提交 `97e44e3`。

祝福来自正常奖励，每席不可叠加；已装备的一席不会消耗重复成长，未装备的队友仍可从共享选择获得。现有 schema 3 在每席字典中接受显式 ID 与整数 1，旧 schema 1/2 保持原规则。旧 0.23.0 校验器代码的只读探针接受普通旧数据、拒绝已装备或待选的新 ID；此证据是旧校验代码检查，不是旧发行程序实测。原有未来档案只读保护继续适用。

- 完整 `python3 verify.py`：资源导入及 34 套、28,865 项通过，无脚本错误。首次回归发现旧格式测试夹具错误携带新增祝福，已从旧夹具中移除；单独 30 项续档及完整重跑通过，旧存档校验没有放宽。两次运行日志分别保留。
- Mac 原生回响：96 项通过，覆盖真实施放、延迟、移动后原位爆发、暂停、掩体、不叠加、无祝福/旧版边界、真实键盘选卡及设备 7 独立新星、部分席位资格、两次隔离冷加载、960×600 / QHD 图解与构筑。只查看最长边 ≤1024、≤256 KiB 的外部 JPEG，未直接查看原始 PNG。
- 正常规则输入机器人：长枪聚焦与双人速射弓分裂均完成三十舱、五守卫和六段剧情。20 个战斗/守卫舱中位 126.75 / 115.8 秒，范围 83.1–155.4 / 76.6–143.8 秒；实际爆发 87 / 32 次回响。祝福来自实际奖励，施放通过输入遵循正常冷却，没有注入属性或清敌。时钟差 0.69 / 0.60 秒；详细成长及计数边界见 `docs/balance.md`。
- 原生 QHD 双人改装/回响压力：89 项中 1 项失败，900 次观察的 P50 16.344 ms、P95 29.331 ms、P99 31.752 ms、最大 35.404 ms。1,378 个绘制回调间隔的 P95 15.393 ms、P99 30.347 ms。146 个观察帧有回响、最多同时两枚，峰值 471 节点 / 38 弹体；40 次重开及孤立节点基线零通过。原物理后处理间隔 P95 超过 25 ms，按授权记录失败/暂跳过，不改门槛、不以绘制间隔通过覆盖它。冻结会改变敌人活动，不能作为性能改善对照。
- Mac 独立发行程序：118 项战役 + 18 项首次存档 + 78 项冷启动续档通过；导出检查和再次独立解压的严格签名验证均通过。发行续档特意只给第二席回响，确认按席持久化。

Mac：`builds/深渊协议-0.24.0-macOS.zip`，144,593,824 字节，SHA-256 `de5bf537f3d55c8c4ed6143ba8cd4c02985ad75a3959428c20de7135c8a1cbeb`。解压件 `builds/macos-0.24.0/深渊协议.app`，ad-hoc 签名、未公证，Intel 未实测。

Windows：`builds/深渊协议-0.24.0-Windows-x64.zip`，121,797,654 字节，SHA-256 `ca5ba7ab69faea7c0461ff722b1eda9f36428c68cce7e8ea0e55aacc97c0811f`。独立 Wine prefix 无界面 118 + 15 + 74、窗口 118 + 18 + 78 项全部通过。六份引擎日志无脚本错误或泄漏，三个窗口进程各有一次非阻塞 SDR white-level 查询警告；控制台日志中的重复警告单独保留。Wine 通过不代表 Windows 实机通过。

两个 ZIP 完整性、Mac 解压文件、Windows 逐文件清单与嵌入校验表均已复核；`builds/acceptance-0.24.0/` 四份验收附件和绑定清单的哈希匹配，报告目标为 `C:\Program Files (x86)\Games` 根目录。先完成 Mac 验证再导出 Windows，两次导出复用本轮已通过的完整源码回归，仍实际运行独立发行与存档检查。所有开发/测试使用 Dummy 音频和隔离存档。

243 个运行文件在打包后与已测源码一致，日志、正常流程、压力、源代码/测试哈希及包校验记录于 `builds/qa/source-0240-nova-echo.json`。Games 未挂载，Windows 实机与共享交付按授权跳过；275 个无关文件保持原样，旧发行包和正常玩家存档保留。本轮没有修改其他游戏或 Pages 部署清单。


### 2026-09-14 · 0.24.0 特殊武器与地面预警测试策略

原机器人对普通爆炸只检查距离，一进入预警圈就请求冲刺；无敌会在爆炸前结束。引力锤在首场守卫阵亡，不能直接据此调整游戏生存数值。只读诊断借已有同步伤害特效和调用栈归类，首场守卫 88 / 90 次引力井命中，该场六次玩家受伤全来自普通地面爆炸。仅将测试侧请求改为预警剩余不足 0.23 秒后，六舱对照在正常成长下完成，守卫 236.5 秒、全程未受伤。

测试侧 `bot_input.gd` 统一该判定，`input_clock.gd` 用真实角色、掩体和实际爆炸验证：角色被掩体挡在危险圈内，必须把冲刺无敌留到爆发时。在 1 / 4 / 12 / 16 倍模拟下，旧策略 73 项中 8 项失败，修正后 73 项通过；蓄力、边沿与双席独立输入继续通过。没有修改玩家输入规则、无敌窗口、伤害、冷却、敌人或存档。

正常规则完整重测：回旋刃重返与引力锤长时聚拢均完成三十舱、五守卫与六段剧情，20 个战斗/守卫舱中位 180.95 / 164.4 秒，范围 104.3–322.3 / 95.3–235.3 秒；时钟差 0.69 / 0.70 秒。这些较慢结果已加入 `docs/balance.md`，下一阶段仍需改善特殊武器节奏。机器人使用精确预警与弹道，不能替代真人难度验收；未启用回响观察标志的零计数不能解释为没有回响。

全部原始失败、慢速、只读诊断、红/绿回归及重测引用记录于 `builds/qa/special-0240-evidence.json`。243 个运行文件保持与 0.24.0 发行包一致，因此继续使用已验证的 Mac/Windows 包，不为测试策略修改重新导出。原性能失败、Windows 实机/实体手柄/真人专项以及未挂载的 Games 交付继续保留原状态。

最终 `python3 verify.py`：资源导入和 34 套、28,888 项通过，无脚本错误，日志 `builds/qa/hazard-bot-0240-verify.log`。本轮原生画面、发行包、Windows/Wine 与性能测试没有重跑，因为 243 个运行文件与 0.24.0 完全一致；既有发行证据和未达标项保持原样。


### 2026-09-14 · 0.24.1 引力锤调校与桌面交付

实际爆炸伤害、原战役、共享技能、构筑显示及双席续档由 `tests/weapon_mods.gd` 覆盖；该套件 401 项通过，原生引力井专项 52 项通过。新增伤害检查先在旧代码上得到 51 项 / 12 失败，再转为 51 项 / 零失败。首次全量运行发现构筑测试读取旧目录而非装备后的定义，修正测试入口后，`python3 verify.py` 的 34 套 / 28,940 项全部通过。原失败日志与最终日志分别保留，未降低断言或跳过失败套件。

单人长时聚拢、双人速爆的正常输入三十舱均胜利，包含五守卫和六段剧情；实际成长与上一版单人基线一致。节奏及回旋刃失败诊断详见 `docs/balance.md`。采样不替代真人体验；没有开启回响统计的报告仍将零计数解释为未观察。

Mac 先完成导出、ZIP 校验及严格 ad-hoc 签名验证，源码目录外的静音原生进程通过战役 118、首次存档 18、冷启动读取 78 项。随后 Windows x64 包完成 PE/资源包/许可检查，独立 Wine prefix 的无界面进程通过 118 + 15 + 74 项，窗口进程通过 118 + 18 + 78 项。六份 Windows 引擎日志无脚本错误或资源泄漏；每份窗口日志各有一次已知 SDR 白电平警告。没有进行 Windows 实机、实体手柄或 Intel Mac 验收。

| 本地包 | 字节数 | SHA-256 |
| --- | ---: | --- |
| `builds/深渊协议-0.24.1-macOS.zip` | 144593917 | `2d436bea8e6124f37c10ce915e0682264124b3382a51acdcfd437641606c5426` |
| `builds/深渊协议-0.24.1-Windows-x64.zip` | 121797746 | `e2b6894f52130a4ce0eac2e8cf9daa40e5019948862f920113d3f1c6d6becee9` |

`builds/acceptance-0.24.1/` 的四份附件及清单绑定本版 EXE/PCK/验证器与 ZIP 哈希，报告目的地为 `C:\Program Files (x86)\Games` 根目录；已回读校验附件。共享未挂载，本轮不连接或替换已有版本，包和附件仍在本地。Mac 未 Apple 公证，Windows 未使用发行者证书签名，未上传公共发行渠道。

运行资源 243 项哈希及测试/打包输入记录在 `builds/qa/source-0241-gravity.json`；前后诊断、通关、源码/原生验证和发行记录的证据索引为 `builds/qa/gravity-0241-evidence.json`。游戏代码提交为 `55f27ad`；交付文档另行提交，PR #87 合并后核对主分支。


### 2026-09-14 · 0.24.2 回旋刃调校、飞行提示与桌面交付

受控主武器探针先确认稳定往返命中时的伤害不足；实际去程/回程、收回前不能重投、原战役、共享新星、构筑显示及双席冷续档由 `tests/weapon_mods.gd` 覆盖，最终该套件 440 项通过。回旋刃专项先得到 33 项 / 10 失败，修正后 33 项零失败；HUD 专项先得到 57 项 / 3 失败，随后 57 项通过。首次全量运行发现旧武器测试写死 0.85 系数，改为实际装备定义验证每程一次命中；独立专项仍精确断言新版系数。最终 `python3 verify.py` 的 34 套 / 28,994 项全部通过，原失败日志保留，没有跳过套件。

Mac 原生 HUD 62 项通过，验证真实飞刃去程、回程、正常冷却提前结束、倒地优先、收回提示及另一席独立状态。只查看最长边 1024、64,796 字节的 JPEG 预览，未直接查看原始 PNG。单/双人正常输入三十舱均胜利，包含五守卫与六段剧情；节奏、实际成长、受控探针与普通流程的区别见 `docs/balance.md`。第三守卫与普通舱仍偏慢；零回响计数表示未开启观察。

先完成 Mac 导出、ZIP 完整性、独立解压逐文件哈希及严格 ad-hoc 签名检查，源码目录外的原生进程通过战役 118、首次存档 18、冷启动读取 78 项。随后 Windows x64 包完成 PE、许可与逐文件清单检查，独立 Wine prefix 无界面通过 118 + 15 + 74 项，窗口通过 118 + 18 + 78 项。六份引擎日志无脚本错误或资源泄漏；三份窗口日志各有一次已知非阻塞 SDR 白电平查询警告。全部开发/测试使用 Dummy 音频和隔离存档。

| 本地包 | 字节数 | SHA-256 |
| --- | ---: | --- |
| `builds/深渊协议-0.24.2-macOS.zip` | 144594130 | `927446c15c7bfa1b8905280d822b789762b9ac1471c67227e4de9edfede10574` |
| `builds/深渊协议-0.24.2-Windows-x64.zip` | 121797960 | `3f950f245174aaaa4281454b037ad435a8b7ef4000152fff950d8278496a616a` |

Mac 解压件为 `builds/macos-0.24.2/深渊协议.app`，未 Apple 公证，Intel 未实测。Windows 未使用发行者证书签名，未上传公共发行渠道。`builds/acceptance-0.24.2/` 四份附件与清单均已回读校验，绑定本版 EXE/PCK/验证器及 ZIP，报告目标为 `C:\Program Files (x86)\Games` 根目录。Games 未挂载，实机与共享交付继续按授权暂跳过。

243 个运行文件与已验证源码一致；测试、打包输入和桌面报告记录于 `builds/qa/source-0242-glaive.json`，原始诊断、通关、原生检查、失败与最终回归日志的证据索引为 `builds/qa/glaive-0242-evidence.json`。代码提交 `9e39d45`，交付文档单独提交；PR #88 合并后核对主分支。275 个无关文件保持原样，没有修改其他游戏或 Pages 部署清单。旧性能失败与真人/实体双手柄/Windows 显示专项保持未完成，整体产品目标继续开启。


### 2026-09-14 · 0.24.3 飞刃边缘命中与桌面交付

原飞刃的可见旋转半径为 21，通用碰撞圆为 7，高速射线只检查中心；受控探针复现距离敌人身体边缘 12 / 18 时完全漏伤。新战役使用独立圆形与连续形状扫掠，检测初始重叠、每程唯一命中和沿途第一处掩体。旧战役与其他弹体保持原规则。新增 `tests/glaive_contact.gd` 纳入全量验证，67 项 / 33 失败转为 67 项全部通过；原生 70 项通过。首次原生退出混音释放延迟的日志保留，按现有套件等待 Dummy 混音退出后复测，无资源泄漏。

完整 `python3 verify.py`：导入及 35 套 / 29,057 项通过，无脚本错误。单/双人正常输入均完成三十舱、五守卫与六段剧情；十八舱前后诊断与完整流程分别记录，第三守卫慢样本不被快样本覆盖，详见 `docs/balance.md`。原生只查看 1024×640、49,976 字节的 JPEG，未直接查看原始 PNG。

Mac 先完成导出、完整 ZIP、独立解压逐文件哈希及严格 ad-hoc 签名检查，源码目录外原生战役 118、首次存档 18、冷启动读取 78 项通过。随后 Windows x64 包完成 PE、资源与许可校验，独立 Wine prefix 无界面通过 118 + 15 + 74，窗口通过 118 + 18 + 78 项。六份引擎日志无脚本错误或泄漏；三份窗口日志各保留一次已知非阻塞 SDR 查询警告。全部开发/测试使用 Dummy 音频与隔离存档。

| 本地包 | 字节数 | SHA-256 |
| --- | ---: | --- |
| `builds/深渊协议-0.24.3-macOS.zip` | 144595410 | `cd7f6e38c02aaa6f0ba90a49a9278e5ee00a4603ddd21b487827b4bc58763c38` |
| `builds/深渊协议-0.24.3-Windows-x64.zip` | 121799238 | `d3c835b1571303720fec33ad85cdd7a0ac427d74b61318b7a3f1cdb88ca1414e` |

Mac 解压件为 `builds/macos-0.24.3/深渊协议.app`，未 Apple 公证，Intel 未实测。Windows 未使用发行者证书签名，未上传公共发行渠道。`builds/acceptance-0.24.3/` 四份验收附件与清单已回读校验，绑定本版 EXE/PCK/验证器与 ZIP，报告目标为 `C:\Program Files (x86)\Games` 根目录；Games 未挂载，Windows 实机和共享交付按授权跳过。

243 个运行文件与已测源码一致；源码、测试、打包输入及桌面报告记录于 `builds/qa/source-0243-glaive-contact.json`，诊断、失败/通过日志、预览与正常流程哈希收录于 `builds/qa/glaive-0243-evidence.json`。代码提交 `c44b547`；交付文档单独提交，PR #89 合并后核对主分支。275 个无关文件保持原样，其他游戏与 Pages 部署清单未改动。原性能失败、真人/实体双手柄/Windows 显示专项保持未完成。


### 2026-09-14 · 0.24.4 回程地面指引与桌面交付

回程提示在角色下方绘制，显示飞刃到投掷机体的当前方向；移动会改变终点，掩体按同一半径 21 扫掠查询裁切，并画短横线。蓝色单箭头与金色双箭头各自归属，沿用高对比偏好。去程不预测未来路线，暂停冻结、倒地隐藏、复活恢复，飞刃销毁时回收提示；重开与旧战役边界均覆盖。原架构与只读约束见 `2026-09-14-abyss-return-floor-guide` Note，代码提交 `a8041b7`。

完整 `python3 verify.py` 导入及 36 套 / 29,089 项通过，无脚本错误。新增指引专项从初始 4 项 / 2 失败转为最终无界面 28 项通过；旧碰撞专项 67 项通过。Mac 原生 33 项验证双席归属、20 次只读查询、真实擦墙撞点、暂停/复活/清理、设备 3 的实际摇杆事件及同一物理步位置同步；设备事件由测试生成，不代表实体手柄验收。原生窗口分别断言 1280×800、960×600，指南与普通/高对比画面只查看最长边 ≤1024、≤256 KiB 的 JPEG，未直接查看原始 PNG。

先完成 Mac 导出、ZIP 完整性、独立解压 8 个文件哈希和严格 ad-hoc 签名检查，源码目录外原生进程通过战役 118、首次存档 18、冷启动续档 78 项。随后 Windows x64 包完成 PE、资源、许可与逐文件清单检查；独立 Wine prefix 无界面通过 118 + 15 + 74，窗口通过 118 + 18 + 78 项。六份引擎日志无脚本错误或泄漏；三个窗口进程各保留一次已知非阻塞 SDR 查询警告。所有开发/测试使用 Dummy 音频与隔离存档。

| 本地包 | 字节数 | SHA-256 |
| --- | ---: | --- |
| `builds/深渊协议-0.24.4-macOS.zip` | 144601363 | `3e3ec5b8aa21b47f30d562e94ec16bd8c67cc2247e968f53c14962b3c9fa3249` |
| `builds/深渊协议-0.24.4-Windows-x64.zip` | 121805190 | `e268757846d9af610d9146612dbee588e02126496f84338184575e4dc95c5c9c` |

Mac 解压件为 `builds/macos-0.24.4/深渊协议.app`，未 Apple 公证，Intel 未实测。Windows 未使用发行者证书签名，未上传公共发行渠道。`builds/acceptance-0.24.4/` 的四份附件与清单已回读校验，绑定本版 EXE/PCK/验证器与 ZIP，报告目标为 `C:\Program Files (x86)\Games` 根目录。Games 未挂载，Windows 实机和共享传送按授权跳过。

247 个运行文件与已测源码一致；源码、测试、打包输入及桌面报告记录于 `builds/qa/source-0244-glaive-guide.json`，失败/通过日志、预览与校验索引为 `builds/qa/glaive-0244-evidence.json`。交付文档单独提交，PR #90 合并后核对主分支。275 个无关文件保持原样，其他游戏和 Pages 部署清单未改动。本轮没有新的正常通关、真人易用性或性能达标证据；旧性能失败与跳过的实机专项继续保留，整体目标仍在进行。


### 2026-09-14 · 0.25.0 收尾增援预告与桌面交付

普通与精英遭遇在剩两敌时预约下一批落点，以收尾战斗覆盖部分预警时间。完整警告到期仍须等旧敌全部清除；双席占位会重新选址并重走完整预警。补修只在真实清场时发放一次且不超过耐久上限。初始三敌教学仍清场后才开始预警，取消、重开和旧战役边界保持原约束。敌人总数、每批上限、成长经济和守卫数值不变。行为与权衡见 `2026-09-14-abyss-primed-reinforcements` Note，代码提交 `4f4f3ca`。

完整 `python3 verify.py` 导入及 36 套 / 29,122 项通过，无脚本错误。遭遇专项先得到 1,058 项 / 2 失败；首次实现的类型推断解析失败保留，改为显式整数后 1,081 项通过，扩充后 1,088 项通过。全量运行中的同套检查为 1,091 项，循环采样计数随运行略有变化。Mac 原生专项 1,087 项通过；覆盖提前预约、完整警告仍等待、快速清场的剩余警告、暂停、双席占位、补修一次、首舱、重开与总预算。

正常输入单人回旋刃与双人速射枪完成三十舱，战斗中位 141.65 / 112.85 秒；单人第三守卫 222.5 秒保留为慢样本。六舱前后与完整流程分别记录，详见 `docs/balance.md`。额外原生观察首个进程意外暂停，恢复后持续按住攻击未经过松键帧，最终终止；暂停原因未确定，不作为完成证据。观察脚本通过正常释放输入处理防误触，不直接修改 `input_armed` 标志。随后完成的原生六舱退出存在两个 WAV 播放对象释放警告；本地独立测试驱动仅把退出等待从 0.2 延长至 0.5 秒后，最终六舱详细日志无错误或泄漏。没有修改生产音频实现。所有失败、中断和通过记录保留，最终原生样本无自动恢复暂停或松键介入。画面仅查看最长边 ≤1024、≤256 KiB 的 JPEG，未直接查看原始 PNG。

先完成 Mac 导出、ZIP 完整性、独立解压 8 个文件哈希和严格 ad-hoc 签名检查，源码目录外原生进程通过战役 118、首次存档 18、冷启动续档 78 项。随后 Windows x64 包完成 PE、资源、许可与逐文件清单检查；独立 Wine prefix 无界面通过 118 + 15 + 74，窗口通过 118 + 18 + 78 项。六份引擎日志无脚本错误或泄漏；三个窗口进程各保留一次已知非阻塞 SDR 查询警告。所有开发/测试使用 Dummy 音频与隔离存档。

| 本地包 | 字节数 | SHA-256 |
| --- | ---: | --- |
| `builds/深渊协议-0.25.0-macOS.zip` | 144601571 | `1f79b9bf09be7b88288d50bbdb7d50739a217bb2e2fd7ae81a743591248ca70d` |
| `builds/深渊协议-0.25.0-Windows-x64.zip` | 121805398 | `fc135a65fb91e5f8279df145d9d617e039ddff4ca849d8986b99ebe100417bca` |

Mac 解压件为 `builds/macos-0.25.0/深渊协议.app`，未 Apple 公证，Intel 未实测。Windows 未使用发行者证书签名，未上传公共发行渠道。`builds/acceptance-0.25.0/` 的四份附件与清单已回读校验，绑定本版 EXE/PCK/验证器与 ZIP，报告目标为 `C:\Program Files (x86)\Games` 根目录。Games 未挂载，Windows 实机和共享传送按授权跳过。

247 个运行文件与已测源码一致；源码、测试、打包输入及桌面报告记录于 `builds/qa/source-0250-encounter.json`，前后诊断、失败/通过日志、正常流程与预览校验索引为 `builds/qa/encounter-0250-evidence.json`。交付文档单独提交，PR #91 合并后核对主分支。275 个无关文件保持原样，其他游戏和 Pages 部署清单未改动。旧性能失败与跳过的真人/实体手柄/实机专项继续保留，整体目标仍在进行。


### 2026-09-14 · 0.26.0 主动回收飞刃与桌面交付

出手冷却恢复且飞刃仍在去程时，一次新的攻击按下边沿让原飞刃进入现有回收流程。不重置冷却、不创建第二枚飞刃、不重复乘算回程伤害；按住、冷却中的提前按键、键盘重复和暂停恢复的旧按键不触发。每席仅操作自己的飞刃，短程快返与旧战役保留自动返回。HUD 复用原主武器位置显示回程箭头和实际键位，指南、构筑与 Windows 游玩说明同步更新。实现记录见 `2026-09-14-abyss-manual-glaive-recall` Note，功能提交 `1792aae`。

完整 `python3 verify.py`：导入及 37 套 / 29,157 项通过，无脚本错误。回收专项先记录缺少功能的 7 项 / 1 失败，最终无界面 33 项、原生 37 项通过；HUD 和指南也先验证缺失再实现。初始夹具未创建机体就释放输入、键位文案预期不完整及混合了鼠标瞄准的断言失败均保留。最终鼠标回收专项验证实际点击与飞行状态，现有独立瞄准套件 6 项通过。原生覆盖鼠标、重绑键和设备 1/3 肩键，断言 1280×800 / 960×600 窗口；只查看 1024×640、51,424 / 42,790 字节 JPEG 预览，未直接查看原始 PNG。最终原生日志无错误或资源泄漏。

测量驱动复核发现原观察器同帧按下再松开，可能额外触发回收；相关节奏数字已撤回，原始 11 份文件连同哈希保留。更正驱动只改变原输入函数中的攻击时机，每帧提交一次最终状态。独立 0.25.0 副本的 247 个运行文件与基线逐项匹配，旧/新单人使用相同输入和只读伤害观察，双人只记录请求与全局结果。三条流程均完成三十舱、五守卫、六段剧情和正常成长；旧版单人战斗中位 137.85 秒，新版单/双人 122.8 / 114.1 秒。20 个战斗舱包含 `last_room` 中的最终守卫。第三守卫 222.5 → 76.7 秒，但新版最终守卫仅 48.6 / 46.5 秒；单种子机器人不能作为全部舱室、真人或实体手柄验收。详情与测量分类边界见 `docs/balance.md`。

Mac 先完成导出、ZIP 完整性、独立解压逐文件哈希与严格 ad-hoc 签名检查；源码目录外原生战役 118、首次存档 18、冷启动续档 78 项通过。随后 Windows x64 包完成 PE、资源、许可与逐文件清单检查，独立 Wine prefix 无界面通过 118 + 15 + 74，窗口通过 118 + 18 + 78 项。六份引擎日志无脚本错误或泄漏，三个窗口进程各保留一次已知非阻塞 SDR 查询警告。所有开发/测试使用 Dummy 音频与隔离存档。

| 本地包 | 字节数 | SHA-256 |
| --- | ---: | --- |
| `builds/深渊协议-0.26.0-macOS.zip` | 144603230 | `9f70283debe2ef6f6e53a4ad3475425caa0158c5cdc512e29f67e7174a4ca5e6` |
| `builds/深渊协议-0.26.0-Windows-x64.zip` | 121807188 | `4127e61a08240f9cf4529a234e355eccc046140878c72fca44cfb5374a15217a` |

Mac 解压件为 `builds/macos-0.26.0/深渊协议.app`，未 Apple 公证，Intel 未实测。Windows 未使用发行者证书签名，未上传公共发行渠道。`builds/acceptance-0.26.0/` 的四份附件与清单已回读校验，绑定本版 EXE/PCK/验证器与 ZIP，报告目标为 `C:\Program Files (x86)\Games` 根目录。Games 未挂载，Windows 实机和共享传送按授权跳过。

249 个运行文件与已测源码一致；源码、测试和打包输入见 `builds/qa/source-0260-recall.json`，更正三十舱对照见 `recall-0260-corrected-comparison.json`，失败/通过日志与预览索引见 `recall-0260-evidence.json`。交付文档单独提交，PR #92 合并后的主分支与完整工作流结果写入本地 `milestone-0.26.0.json` 和工作日志。275 个无关文件保持原样，其他游戏与 Pages 部署清单未改动。旧性能失败与跳过的真人/实体手柄/实机专项继续保留，整体目标仍在进行。


### 2026-09-14 · 0.26.1 元素到期修复与桌面交付

燃烧与毒素完全结束后，新的低伤命中曾继承旧最高强度与剩余跳伤计时；毒素在到期帧还可被火焰引爆。新战役在到期、重新施加和爆燃消耗处统一结束旧状态，下一次施加从首个物理步重新跳伤。持续刷新保持较强效果和半秒节奏，不能靠刷新额外跳伤。旧十二舱保持原行为，无需改存档或成长数值。功能提交 `c4ade0b`，边界决策见 `2026-09-14-abyss-element-expiry` Note。

扩展原元素套件的 36 项检查先在旧代码上复现 12 项失败；修复后无界面 81 项、Mac 原生 82 项通过，覆盖两席强弱命中、自然到期、暂停、重新施加、爆燃消耗、毒槽及同帧引爆。完整 `python3 verify.py` 导入与 37 套 / 29,189 项通过；未改测试驱动或跳过原套件。原失败日志 `builds/qa/expiry-0261-red.log` 保留，最终源码与原生日志无脚本错误或泄漏。运行静音且使用隔离存档，本轮没有直接查看任何原始 PNG。

沿用 0.26.0 更正输入策略，单/双人完整三十舱胜利，成长、五守卫、六段剧情和物理时钟检查通过。单/双人战斗中位 122.55 / 115.95 秒，最终守卫 48.9 / 46.9 秒；未开启回响实例观察，零计数不当作没有触发。输入只改变报告文件名，单人另用原只读伤害分类器，源码差异和前后四条报告哈希见 `builds/qa/expiry-0261-comparison.json`。状态修复没有解决整体节奏，单种子机器人也不是所有武器、真人或实体手柄验收。

在当前完整源码通过后，Mac 先使用 `export_macos.py --skip-checks --rendered` 完成独立战役 118、首次存档 18、冷启动续档 78 项；ZIP 完整性、独立解压的 8 个文件哈希与严格 ad-hoc 签名检查通过。随后 Windows x64 包通过 PE/版本/许可/资源检查，独立 Wine prefix 无界面 118 + 15 + 74、窗口 118 + 18 + 78 项通过。六份引擎日志无错误或泄漏；窗口进程各保留一次已知非阻塞 SDR 查询警告。所有测试使用 Dummy 音频，不读写普通玩家存档。

| 本地包 | 字节数 | SHA-256 |
| --- | ---: | --- |
| `builds/深渊协议-0.26.1-macOS.zip` | 144603526 | `c7b7d60a3beb3bc91ada961e8a9d4063f3c782229b522ddb8ad635208c9fc34b` |
| `builds/深渊协议-0.26.1-Windows-x64.zip` | 121807540 | `400bc9d74bf6f90dae662921ceb26ff8133dda65ea6fa895bf50de3e3cf0c71a` |

Mac 解压件为 `builds/macos-0.26.1/深渊协议.app`，未 Apple 公证，Intel 未实测。Windows 未使用发行者证书签名。`builds/acceptance-0.26.1/` 的四份附件与清单已回读校验，绑定本版 EXE/PCK/验证器与 ZIP，报告直接写到 `C:\Program Files (x86)\Games` 根目录。Games 未挂载，实机与共享传送按授权跳过，未上传公开发行包。

249 个运行文件及测试/打包输入与已测源码一致；证据索引为 `builds/qa/expiry-0261-evidence.json`，源码记录为 `source-0261-expiry.json`。PR #93 合并后的主分支与完整工作流记录写入本地 `milestone-0.26.1.json` 和 Second Brain 工作日志。已完成的旧 WIP 同步改为 DONE，持续的产品目标保留 WIP；275 个无关文件与其他游戏保持原样，Pages 部署范围不变。旧性能失败、后期偏短和跳过的硬件/真人专项继续保留。


### 2026-09-14 · 0.27.0 重返飞刃蓄势与桌面交付

功能提交 `9076cd7` 为重返飞刃加入线性去程蓄势：0 / 0.375 / 0.75 秒对应 1 / 2 / 3 倍回程，提前回收与撞墙共用转向结算，回程不能继续蓄势。细弧、构筑最大伤害、改装说明与小窗口指南已在原生 JPEG 压缩预览核对。旧战役、正常/短程飞刃、共享技能、玩家成长与守卫耐久保持原样，决策见 `2026-09-14-abyss-glaive-momentum` Note。

边界回归先复现 6 项失败，最终回收套件 51 项、Mac 原生 60 项通过；完整 `python3 verify.py` 导入与 37 套 / 29,201 项通过。刀锋、速射枪的原版正常三十舱补测也完成。匹配主动回收参数的新单/双人全程胜利，五守卫、六段剧情与最终实际构筑一致，物理时钟差小于一秒。新战斗中位 116.8 / 118.3 秒，最终守卫 57.5 / 52.2 秒仍偏短。首轮漏加 `--manual-recall` 的报告明确命名为 `momentum-0270-auto-*`，未混入主动回收前后比较；详细对照见 `builds/qa/momentum-0270-comparison.json`。

Mac 首次导出通过独立战役 118 项与首次存档 18 项，读档阶段触发 55 秒超时并有退出泄漏警告。失败日志保留为 `builds/qa/macos-0270-export.log`。随后对**同一 ZIP**独立解压，8 个文件哈希与严格 ad-hoc 签名一致；在新的标记目录中分别运行写入/读取进程，18 / 78 项通过，无错误或泄漏。读取时将测试窗口置前后完成；初次超时的根因未确定，不将首轮失败重写为通过。重验日志与存档保留于 `macos-0270-recheck/`，发行报告明确记录两次结果。没有改验证超时、跳过续档或修改打包内容。

Mac 验证完成后再构建 Windows x64。同一个隔离 Wine prefix 的无界面 118 + 15 + 74、窗口 118 + 18 + 78 项通过；六份引擎日志无错误或泄漏，窗口进程各保留一次已知 SDR 查询警告。原生与兼容层结果分别记录，所有启动使用 Dummy 音频和独立存档。

| 本地包 | 字节数 | SHA-256 |
| --- | ---: | --- |
| `builds/深渊协议-0.27.0-macOS.zip` | 144604353 | `13378184e45d08edc4a55ecc848ded62cb3a472b0b77fedfad65f81ccf19a942` |
| `builds/深渊协议-0.27.0-Windows-x64.zip` | 121808448 | `3459da9c7d06e4cb04a8ba09bec97cb8b45d8621c9cc5126cf110cf974dd9835` |

Mac 解压件为 `builds/macos-0.27.0/深渊协议.app`，未 Apple 公证，Intel 未实测；Windows 未使用发行者证书签名。`builds/acceptance-0.27.0/` 的四份附件和清单已核对，绑定本版 EXE/PCK/验证器与 ZIP，报告目标为 `C:\Program Files (x86)\Games` 根目录。Games 未挂载，Windows 实机和共享交付按授权暂跳过；未上传公开发行包。

249 个运行文件和相应测试/打包输入保持与已测版本一致。源码记录为 `builds/qa/source-0270-momentum.json`，证据索引为 `momentum-0270-evidence.json`；PR #94 的最终合并、主分支完整工作流与包元数据归档到 `milestone-0.27.0.json` 和工作日志。已完成 WIP 同步更新为 DONE，275 个无关文件保留，原生项目仍不属于 Pages 部署范围。后期守卫偏短、其他武器/成长、旧性能失败与跳过的真人/硬件专项继续保持未完成。

### 2026-09-14 · 0.28.0 熔炉双重热浪与桌面验证

熔炉原热环只覆盖 125–290，现有远程输入策略会在约 300 距离绕圈。第二拍覆盖 285–460，在 1.75 秒引爆；第一拍保留 1.05 秒。两圈缓存多边形同时用于绘制和判定，界面/图标/指南与顺序一致，暂停、掩体、冲刺、双人独立受伤与旧战役保持原边界。

旧实现定向回归为 176 项 / 2 个预期失败；实现后专属检查 256 项通过，完整源码 37 套 / 29,290 项通过。Mac 原生专属 257 项与滚动指南 2 项通过；低闪光截图仅查看 1024×640、均小于 256 KiB 的 JPEG 预览。完整回归后仅将项目与导出版本元数据从 0.27.0 更新为 0.28.0；运行脚本、资源和测试保持相同，最终独立包检查验证实际版本与内容。

单/双人正常输入均完成三十舱；最终构筑与 0.27.0 一致，物理时钟差小于一秒。熔炉出现十组 / 九组热浪，并有临近危险时的实际冲刺；最终 Boss 仍偏短。此数据不证明所有武器、真人难度或完整两分钟节奏已经达标。

Mac 首次 `export_macos.py --skip-checks --rendered` **失败**：独立战役 118 项通过，写入阶段为 `ERROR: Release verification timed out` 与 `WARNING: 2 ObjectDB instances were leaked at exit (run with --verbose for details).`。原始日志保存在 `builds/qa/macos-0280-export.log`，没有重写为通过。对同一 ZIP 独立解压，在新的已标记隔离目录运行两个进程，写入 18 / 读取 78 项通过，没有错误或泄漏；8 个解压文件哈希、版本与严格签名一致后才补全构建报告和 SHA。未改超时、未跳过续档、未修改 ZIP。重验期间 AX 窗口查询均失败，截图文件仍正常推进，因此不能将重验通过归因于置前窗口，也未确定首轮超时根因。

Windows x64 包的 PE/资源/哈希通过，Wine 无界面 118 + 15 + 74 项、窗口 118 + 18 + 78 项通过。六份引擎日志均无脚本错误或退出泄漏，窗口模式各保留一条已知 SDR 提示。Wine 结果不代表 Windows 实机验收。

| 本地包 | 字节数 | SHA-256 |
| --- | ---: | --- |
| `深渊协议-0.28.0-macOS.zip` | 144604970 | `58284880096c77a99ead0dcd70356141ce55bccda35b76ee2f61aff5ecea9373` |
| `深渊协议-0.28.0-Windows-x64.zip` | 121809142 | `5524187983e068be12112999f386a7d3a5ca48087699019b5dc88f012f2e730b` |

验收附件位于 `builds/acceptance-0.28.0/`，四个附件与清单已核对 ZIP、EXE、PCK 和验证脚本哈希；报告目标为 `C:\Program Files (x86)\Games` 根目录。Games 未挂载，本轮共享交付、Windows 实机/显示与实体手柄按授权暂跳过。Mac 包仍为 ad-hoc 未公证；Intel 未实测，旧性能失败保留。

源码与打包输入共 300 个文件的哈希保持一致，索引为 `builds/qa/source-0280-furnace.json`；正常输入对照、原生预览、首次 Mac 失败与同包重验、Windows 日志和验收附件记录于 `furnace-0280-evidence.json`。合并/主分支工作流状态另存 `milestone-0.28.0.json` 并更新工作日志。275 个无关文件保留，原生项目仍不在 Pages 部署清单内，整体目标继续。

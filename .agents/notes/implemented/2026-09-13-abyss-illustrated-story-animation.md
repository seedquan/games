# 实时分层剧情动画

状态：implemented

用户要求加入 AI 绘制的 animation，明确不要视频，并继续保持简洁界面。三个必经节点使用本地原创 SVG 分层：抵达穹灯、恢复泵组、乘客归航。`story_scene.gd` 在 Godot 内绘制并变换空间站、接驳船、泵轮和机器人手臂；没有视频解码、网络依赖、离线影片或自动播放的声音。

每段时长八秒，完成后停在最终画面。继续按钮从第一帧起可用，沿用原有故事返回状态；暂停/重播只控制视觉时间，不调用战斗、随机数、奖励或存档。完整日志在同一页可滚动阅读。失焦暂停动画，隐藏页面立即停止处理，下一次菜单释放旧节点。

设置新增 `story_motion` 布尔值，沿用 schema 1 的默认值和类型验证；缺少该字段的旧设置仍可读取。关闭时直接显示最终静态画面，播放控件隐藏，叙事与进度不变。发行版冷读检查包含该偏好。没有借用屏幕震动强度控制全部动画。原生设置检查 1,004 项通过，包含非零手柄操作新开关与完整设置冷读。

原生三节点功能/布局/控制检查 59 项通过；另一次含性能的检查为 61 项。QHD 360 帧全部为活动动画，P50 / P95 / P99 = 11.611 / 13.700 / 13.788 ms，最大 13.932 ms，Apple M4 Pro；只代表本机的剧情画面。使用非零手柄编号的合成输入，实体手柄和真人验收仍保留此前的跳过边界。画面只查看经尺寸与字节检查的压缩 JPEG。

完整 `verify.py` 通过 24 套 / 24,057 项，无脚本错误。来源记录为 `builds/qa/source-0160-story-animation.json`。
